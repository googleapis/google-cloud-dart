// Copyright 2025 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// The Google Cloud client for the Cloud Logging API.
///
/// Writes log entries and manages your Cloud Logging configuration.
library;

// ignore_for_file: avoid_unused_constructor_parameters
// ignore_for_file: camel_case_types
// ignore_for_file: comment_references
// ignore_for_file: implementation_imports
// ignore_for_file: lines_longer_than_80_chars
// ignore_for_file: unintended_html_in_doc_comment

import 'package:google_cloud_api/api.dart';
import 'package:google_cloud_logging_type/logging_type.dart' as logging_type;
import 'package:google_cloud_longrunning/longrunning.dart';
import 'package:google_cloud_protobuf/protobuf.dart';
import 'package:google_cloud_protobuf/src/encoding.dart';
import 'package:google_cloud_rpc/rpc.dart';
import 'package:google_cloud_rpc/service_client.dart';
import 'package:http/http.dart' as http;

const _apiKeys = ['GOOGLE_API_KEY'];

/// Service for ingesting and querying logs.
final class LoggingServiceV2 {
  static const _host = 'logging.googleapis.com';

  final ServiceClient _client;

  /// Creates a `LoggingServiceV2` using [client] for transport.
  ///
  /// The provided [http.Client] must be configured to provide whatever
  /// authentication is required by `LoggingServiceV2`. You can do that using
  /// [`package:googleapis_auth`](https://pub.dev/packages/googleapis_auth).
  LoggingServiceV2({required http.Client client})
    : _client = ServiceClient(client: client);

  /// Creates a `LoggingServiceV2` that does authentication through an API key.
  ///
  /// If called without arguments, the API key is taken from these environment
  /// variables:
  ///
  /// - `GOOGLE_API_KEY`
  ///
  /// Throws [ConfigurationException] if called without arguments and none of
  /// the above environment variables are set. On the web,
  /// always throws [ConfigurationException] if called without arguments.
  ///
  /// See [API Keys Overview](https://cloud.google.com/api-keys/docs/overview).
  factory LoggingServiceV2.fromApiKey([String? apiKey]) =>
      LoggingServiceV2(client: httpClientFromApiKey(apiKey, _apiKeys));

  /// Deletes all the log entries in a log for the _Default Log Bucket. The log
  /// reappears if it receives new entries. Log entries written shortly before
  /// the delete operation might not be deleted. Entries received after the
  /// delete operation with a timestamp before the operation will be deleted.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<void> deleteLog(DeleteLogRequest request) async {
    final url = Uri.https(_host, '/v2/${request.logName}');
    await _client.delete(url);
  }

  /// Writes log entries to Logging. This API method is the
  /// only way to send log entries to Logging. This method
  /// is used, directly or indirectly, by the Logging agent
  /// (fluentd) and all logging libraries configured to use Logging.
  /// A single request may contain log entries for a maximum of 1000
  /// different resources (projects, organizations, billing accounts or
  /// folders)
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<WriteLogEntriesResponse> writeLogEntries(
    WriteLogEntriesRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/entries:write');
    final response = await _client.post(url, body: request);
    return WriteLogEntriesResponse.fromJson(response);
  }

  /// Lists log entries.  Use this method to retrieve log entries that originated
  /// from a project/folder/organization/billing account.  For ways to export log
  /// entries, see [Exporting
  /// Logs](https://cloud.google.com/logging/docs/export).
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<ListLogEntriesResponse> listLogEntries(
    ListLogEntriesRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/entries:list');
    final response = await _client.post(url, body: request);
    return ListLogEntriesResponse.fromJson(response);
  }

  /// Lists the descriptors for monitored resource types used by Logging.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<ListMonitoredResourceDescriptorsResponse>
  listMonitoredResourceDescriptors(
    ListMonitoredResourceDescriptorsRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/monitoredResourceDescriptors', {
      if (request.pageSize case final $1 when $1.isNotDefault)
        'pageSize': '${$1}',
      if (request.pageToken case final $1 when $1.isNotDefault) 'pageToken': $1,
    });
    final response = await _client.get(url);
    return ListMonitoredResourceDescriptorsResponse.fromJson(response);
  }

  /// Lists the logs in projects, organizations, folders, or billing accounts.
  /// Only logs that have entries are listed.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<ListLogsResponse> listLogs(ListLogsRequest request) async {
    final url = Uri.https(_host, '/v2/${request.parent}/logs', {
      if (request.resourceNames case final $1 when $1.isNotDefault)
        'resourceNames': $1,
      if (request.pageSize case final $1 when $1.isNotDefault)
        'pageSize': '${$1}',
      if (request.pageToken case final $1 when $1.isNotDefault) 'pageToken': $1,
    });
    final response = await _client.get(url);
    return ListLogsResponse.fromJson(response);
  }

  /// Provides the `Operations` service functionality in this service.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<ListOperationsResponse> listOperations(
    ListOperationsRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/${request.name}/operations', {
      if (request.filter case final $1 when $1.isNotDefault) 'filter': $1,
      if (request.pageSize case final $1 when $1.isNotDefault)
        'pageSize': '${$1}',
      if (request.pageToken case final $1 when $1.isNotDefault) 'pageToken': $1,
      if (request.returnPartialSuccess case final $1 when $1.isNotDefault)
        'returnPartialSuccess': '${$1}',
    });
    final response = await _client.get(url);
    return ListOperationsResponse.fromJson(response);
  }

  /// Provides the `Operations` service functionality in this service.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  ///
  /// This method can be used to get the current status of a long-running
  /// operation.
  Future<Operation<T, S>> getOperation<
    T extends ProtoMessage,
    S extends ProtoMessage
  >(Operation<T, S> request) async {
    final url = Uri.https(_host, '/v2/${request.name}');
    final response = await _client.get(url);
    return Operation.fromJson(response, request.operationHelper);
  }

  /// Provides the `Operations` service functionality in this service.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<void> cancelOperation(CancelOperationRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}:cancel');
    await _client.post(url, body: request);
  }

  /// Closes the client and cleans up any resources associated with it.
  ///
  /// Once [close] is called, no other methods should be called.
  void close() => _client.close();
}

/// Service for configuring sinks used to route log entries.
final class ConfigServiceV2 {
  static const _host = 'logging.googleapis.com';

  final ServiceClient _client;

  /// Creates a `ConfigServiceV2` using [client] for transport.
  ///
  /// The provided [http.Client] must be configured to provide whatever
  /// authentication is required by `ConfigServiceV2`. You can do that using
  /// [`package:googleapis_auth`](https://pub.dev/packages/googleapis_auth).
  ConfigServiceV2({required http.Client client})
    : _client = ServiceClient(client: client);

  /// Creates a `ConfigServiceV2` that does authentication through an API key.
  ///
  /// If called without arguments, the API key is taken from these environment
  /// variables:
  ///
  /// - `GOOGLE_API_KEY`
  ///
  /// Throws [ConfigurationException] if called without arguments and none of
  /// the above environment variables are set. On the web,
  /// always throws [ConfigurationException] if called without arguments.
  ///
  /// See [API Keys Overview](https://cloud.google.com/api-keys/docs/overview).
  factory ConfigServiceV2.fromApiKey([String? apiKey]) =>
      ConfigServiceV2(client: httpClientFromApiKey(apiKey, _apiKeys));

  /// Lists log buckets.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<ListBucketsResponse> listBuckets(ListBucketsRequest request) async {
    final url = Uri.https(_host, '/v2/${request.parent}/buckets', {
      if (request.pageToken case final $1 when $1.isNotDefault) 'pageToken': $1,
      if (request.pageSize case final $1 when $1.isNotDefault)
        'pageSize': '${$1}',
    });
    final response = await _client.get(url);
    return ListBucketsResponse.fromJson(response);
  }

  /// Gets a log bucket.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogBucket> getBucket(GetBucketRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}');
    final response = await _client.get(url);
    return LogBucket.fromJson(response);
  }

  /// Creates a log bucket asynchronously that can be used to store log entries.
  ///
  /// After a bucket has been created, the bucket's location cannot be changed.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  ///
  /// Returns an [Operation] representing the status of the long-running
  /// operation.
  ///
  /// When complete, [Operation.done] will be `true`. If successful,
  /// [Operation.responseAsMessage] will contain the operation's result.
  Future<Operation<LogBucket, BucketMetadata>> createBucketAsync(
    CreateBucketRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/${request.parent}/buckets:createAsync', {
      if (request.bucketId case final $1 when $1.isNotDefault) 'bucketId': $1,
    });
    final response = await _client.post(url, body: request.bucket);
    return Operation.fromJson(
      response,
      OperationHelper(LogBucket.fromJson, BucketMetadata.fromJson),
    );
  }

  /// Updates a log bucket asynchronously.
  ///
  /// If the bucket has a `lifecycle_state` of `DELETE_REQUESTED`, then
  /// `FAILED_PRECONDITION` will be returned.
  ///
  /// After a bucket has been created, the bucket's location cannot be changed.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  ///
  /// Returns an [Operation] representing the status of the long-running
  /// operation.
  ///
  /// When complete, [Operation.done] will be `true`. If successful,
  /// [Operation.responseAsMessage] will contain the operation's result.
  Future<Operation<LogBucket, BucketMetadata>> updateBucketAsync(
    UpdateBucketRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/${request.name}:updateAsync', {
      if (request.updateMask case final $1?) 'updateMask': $1.toJson(),
    });
    final response = await _client.post(url, body: request.bucket);
    return Operation.fromJson(
      response,
      OperationHelper(LogBucket.fromJson, BucketMetadata.fromJson),
    );
  }

  /// Creates a log bucket that can be used to store log entries. After a bucket
  /// has been created, the bucket's location cannot be changed.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogBucket> createBucket(CreateBucketRequest request) async {
    final url = Uri.https(_host, '/v2/${request.parent}/buckets', {
      if (request.bucketId case final $1 when $1.isNotDefault) 'bucketId': $1,
    });
    final response = await _client.post(url, body: request.bucket);
    return LogBucket.fromJson(response);
  }

  /// Updates a log bucket.
  ///
  /// If the bucket has a `lifecycle_state` of `DELETE_REQUESTED`, then
  /// `FAILED_PRECONDITION` will be returned.
  ///
  /// After a bucket has been created, the bucket's location cannot be changed.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogBucket> updateBucket(UpdateBucketRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}', {
      if (request.updateMask case final $1?) 'updateMask': $1.toJson(),
    });
    final response = await _client.patch(url, body: request.bucket);
    return LogBucket.fromJson(response);
  }

  /// Deletes a log bucket.
  ///
  /// Changes the bucket's `lifecycle_state` to the `DELETE_REQUESTED` state.
  /// After 7 days, the bucket will be purged and all log entries in the bucket
  /// will be permanently deleted.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<void> deleteBucket(DeleteBucketRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}');
    await _client.delete(url);
  }

  /// Undeletes a log bucket. A bucket that has been deleted can be undeleted
  /// within the grace period of 7 days.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<void> undeleteBucket(UndeleteBucketRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}:undelete');
    await _client.post(url, body: request);
  }

  /// Lists views on a log bucket.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<ListViewsResponse> listViews(ListViewsRequest request) async {
    final url = Uri.https(_host, '/v2/${request.parent}/views', {
      if (request.pageToken case final $1 when $1.isNotDefault) 'pageToken': $1,
      if (request.pageSize case final $1 when $1.isNotDefault)
        'pageSize': '${$1}',
    });
    final response = await _client.get(url);
    return ListViewsResponse.fromJson(response);
  }

  /// Gets a view on a log bucket..
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogView> getView(GetViewRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}');
    final response = await _client.get(url);
    return LogView.fromJson(response);
  }

  /// Creates a view over log entries in a log bucket. A bucket may contain a
  /// maximum of 30 views.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogView> createView(CreateViewRequest request) async {
    final url = Uri.https(_host, '/v2/${request.parent}/views', {
      if (request.viewId case final $1 when $1.isNotDefault) 'viewId': $1,
    });
    final response = await _client.post(url, body: request.view);
    return LogView.fromJson(response);
  }

  /// Updates a view on a log bucket. This method replaces the following fields
  /// in the existing view with values from the new view: `filter`.
  /// If an `UNAVAILABLE` error is returned, this indicates that system is not in
  /// a state where it can update the view. If this occurs, please try again in a
  /// few minutes.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogView> updateView(UpdateViewRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}', {
      if (request.updateMask case final $1?) 'updateMask': $1.toJson(),
    });
    final response = await _client.patch(url, body: request.view);
    return LogView.fromJson(response);
  }

  /// Deletes a view on a log bucket.
  /// If an `UNAVAILABLE` error is returned, this indicates that system is not in
  /// a state where it can delete the view. If this occurs, please try again in a
  /// few minutes.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<void> deleteView(DeleteViewRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}');
    await _client.delete(url);
  }

  /// Lists sinks.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<ListSinksResponse> listSinks(ListSinksRequest request) async {
    final url = Uri.https(_host, '/v2/${request.parent}/sinks', {
      if (request.pageToken case final $1 when $1.isNotDefault) 'pageToken': $1,
      if (request.pageSize case final $1 when $1.isNotDefault)
        'pageSize': '${$1}',
    });
    final response = await _client.get(url);
    return ListSinksResponse.fromJson(response);
  }

  /// Gets a sink.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogSink> getSink(GetSinkRequest request) async {
    final url = Uri.https(_host, '/v2/${request.sinkName}');
    final response = await _client.get(url);
    return LogSink.fromJson(response);
  }

  /// Creates a sink that exports specified log entries to a destination. The
  /// export of newly-ingested log entries begins immediately, unless the sink's
  /// `writer_identity` is not permitted to write to the destination. A sink can
  /// export log entries only from the resource owning the sink.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogSink> createSink(CreateSinkRequest request) async {
    final url = Uri.https(_host, '/v2/${request.parent}/sinks', {
      if (request.uniqueWriterIdentity case final $1 when $1.isNotDefault)
        'uniqueWriterIdentity': '${$1}',
    });
    final response = await _client.post(url, body: request.sink);
    return LogSink.fromJson(response);
  }

  /// Updates a sink. This method replaces the following fields in the existing
  /// sink with values from the new sink: `destination`, and `filter`.
  ///
  /// The updated sink might also have a new `writer_identity`; see the
  /// `unique_writer_identity` field.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogSink> updateSink(UpdateSinkRequest request) async {
    final url = Uri.https(_host, '/v2/${request.sinkName}', {
      if (request.uniqueWriterIdentity case final $1 when $1.isNotDefault)
        'uniqueWriterIdentity': '${$1}',
      if (request.updateMask case final $1?) 'updateMask': $1.toJson(),
    });
    final response = await _client.put(url, body: request.sink);
    return LogSink.fromJson(response);
  }

  /// Deletes a sink. If the sink has a unique `writer_identity`, then that
  /// service account is also deleted.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<void> deleteSink(DeleteSinkRequest request) async {
    final url = Uri.https(_host, '/v2/${request.sinkName}');
    await _client.delete(url);
  }

  /// Asynchronously creates a linked dataset in BigQuery which makes it possible
  /// to use BigQuery to read the logs stored in the log bucket. A log bucket may
  /// currently only contain one link.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  ///
  /// Returns an [Operation] representing the status of the long-running
  /// operation.
  ///
  /// When complete, [Operation.done] will be `true`. If successful,
  /// [Operation.responseAsMessage] will contain the operation's result.
  Future<Operation<Link, LinkMetadata>> createLink(
    CreateLinkRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/${request.parent}/links', {
      if (request.linkId case final $1 when $1.isNotDefault) 'linkId': $1,
    });
    final response = await _client.post(url, body: request.link);
    return Operation.fromJson(
      response,
      OperationHelper(Link.fromJson, LinkMetadata.fromJson),
    );
  }

  /// Deletes a link. This will also delete the corresponding BigQuery linked
  /// dataset.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  ///
  /// Returns an [Operation] representing the status of the long-running
  /// operation.
  ///
  /// When complete, [Operation.done] will be `true`. If successful,
  /// [Operation.responseAsMessage] will contain the operation's result.
  Future<Operation<Empty, LinkMetadata>> deleteLink(
    DeleteLinkRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/${request.name}');
    final response = await _client.delete(url);
    return Operation.fromJson(
      response,
      OperationHelper(Empty.fromJson, LinkMetadata.fromJson),
    );
  }

  /// Lists links.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<ListLinksResponse> listLinks(ListLinksRequest request) async {
    final url = Uri.https(_host, '/v2/${request.parent}/links', {
      if (request.pageToken case final $1 when $1.isNotDefault) 'pageToken': $1,
      if (request.pageSize case final $1 when $1.isNotDefault)
        'pageSize': '${$1}',
    });
    final response = await _client.get(url);
    return ListLinksResponse.fromJson(response);
  }

  /// Gets a link.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<Link> getLink(GetLinkRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}');
    final response = await _client.get(url);
    return Link.fromJson(response);
  }

  /// Lists all the exclusions on the _Default sink in a parent resource.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<ListExclusionsResponse> listExclusions(
    ListExclusionsRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/${request.parent}/exclusions', {
      if (request.pageToken case final $1 when $1.isNotDefault) 'pageToken': $1,
      if (request.pageSize case final $1 when $1.isNotDefault)
        'pageSize': '${$1}',
    });
    final response = await _client.get(url);
    return ListExclusionsResponse.fromJson(response);
  }

  /// Gets the description of an exclusion in the _Default sink.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogExclusion> getExclusion(GetExclusionRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}');
    final response = await _client.get(url);
    return LogExclusion.fromJson(response);
  }

  /// Creates a new exclusion in the _Default sink in a specified parent
  /// resource. Only log entries belonging to that resource can be excluded. You
  /// can have up to 10 exclusions in a resource.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogExclusion> createExclusion(CreateExclusionRequest request) async {
    final url = Uri.https(_host, '/v2/${request.parent}/exclusions');
    final response = await _client.post(url, body: request.exclusion);
    return LogExclusion.fromJson(response);
  }

  /// Changes one or more properties of an existing exclusion in the _Default
  /// sink.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogExclusion> updateExclusion(UpdateExclusionRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}', {
      if (request.updateMask case final $1?) 'updateMask': $1.toJson(),
    });
    final response = await _client.patch(url, body: request.exclusion);
    return LogExclusion.fromJson(response);
  }

  /// Deletes an exclusion in the _Default sink.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<void> deleteExclusion(DeleteExclusionRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}');
    await _client.delete(url);
  }

  /// Gets the Logging CMEK settings for the given resource.
  ///
  /// Note: CMEK for the Log Router can be configured for Google Cloud projects,
  /// folders, organizations and billing accounts. Once configured for an
  /// organization, it applies to all projects and folders in the Google Cloud
  /// organization.
  ///
  /// See [Enabling CMEK for Log
  /// Router](https://cloud.google.com/logging/docs/routing/managed-encryption)
  /// for more information.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<CmekSettings> getCmekSettings(GetCmekSettingsRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}/cmekSettings');
    final response = await _client.get(url);
    return CmekSettings.fromJson(response);
  }

  /// Updates the Log Router CMEK settings for the given resource.
  ///
  /// Note: CMEK for the Log Router can currently only be configured for Google
  /// Cloud organizations. Once configured, it applies to all projects and
  /// folders in the Google Cloud organization.
  ///
  /// `UpdateCmekSettings`
  /// will fail if 1) `kms_key_name` is invalid, or 2) the associated service
  /// account does not have the required
  /// `roles/cloudkms.cryptoKeyEncrypterDecrypter` role assigned for the key, or
  /// 3) access to the key is disabled.
  ///
  /// See [Enabling CMEK for Log
  /// Router](https://cloud.google.com/logging/docs/routing/managed-encryption)
  /// for more information.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<CmekSettings> updateCmekSettings(
    UpdateCmekSettingsRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/${request.name}/cmekSettings', {
      if (request.updateMask case final $1?) 'updateMask': $1.toJson(),
    });
    final response = await _client.patch(url, body: request.cmekSettings);
    return CmekSettings.fromJson(response);
  }

  /// Gets the Log Router settings for the given resource.
  ///
  /// Note: Settings for the Log Router can be get for Google Cloud projects,
  /// folders, organizations and billing accounts. Currently it can only be
  /// configured for organizations. Once configured for an organization, it
  /// applies to all projects and folders in the Google Cloud organization.
  ///
  /// See [Enabling CMEK for Log
  /// Router](https://cloud.google.com/logging/docs/routing/managed-encryption)
  /// for more information.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<Settings> getSettings(GetSettingsRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}/settings');
    final response = await _client.get(url);
    return Settings.fromJson(response);
  }

  /// Updates the Log Router settings for the given resource.
  ///
  /// Note: Settings for the Log Router can currently only be configured for
  /// Google Cloud organizations. Once configured, it applies to all projects and
  /// folders in the Google Cloud organization.
  ///
  /// `UpdateSettings`
  /// will fail if 1) `kms_key_name` is invalid, or 2) the associated service
  /// account does not have the required
  /// `roles/cloudkms.cryptoKeyEncrypterDecrypter` role assigned for the key, or
  /// 3) access to the key is disabled. 4) `location_id` is not supported by
  /// Logging. 5) `location_id` violate OrgPolicy.
  ///
  /// See [Enabling CMEK for Log
  /// Router](https://cloud.google.com/logging/docs/routing/managed-encryption)
  /// for more information.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<Settings> updateSettings(UpdateSettingsRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}/settings', {
      if (request.updateMask case final $1?) 'updateMask': $1.toJson(),
    });
    final response = await _client.patch(url, body: request.settings);
    return Settings.fromJson(response);
  }

  /// Copies a set of log entries from a log bucket to a Cloud Storage bucket.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  ///
  /// Returns an [Operation] representing the status of the long-running
  /// operation.
  ///
  /// When complete, [Operation.done] will be `true`. If successful,
  /// [Operation.responseAsMessage] will contain the operation's result.
  Future<Operation<CopyLogEntriesResponse, CopyLogEntriesMetadata>>
  copyLogEntries(CopyLogEntriesRequest request) async {
    final url = Uri.https(_host, '/v2/entries:copy');
    final response = await _client.post(url, body: request);
    return Operation.fromJson(
      response,
      OperationHelper(
        CopyLogEntriesResponse.fromJson,
        CopyLogEntriesMetadata.fromJson,
      ),
    );
  }

  /// Provides the `Operations` service functionality in this service.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<ListOperationsResponse> listOperations(
    ListOperationsRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/${request.name}/operations', {
      if (request.filter case final $1 when $1.isNotDefault) 'filter': $1,
      if (request.pageSize case final $1 when $1.isNotDefault)
        'pageSize': '${$1}',
      if (request.pageToken case final $1 when $1.isNotDefault) 'pageToken': $1,
      if (request.returnPartialSuccess case final $1 when $1.isNotDefault)
        'returnPartialSuccess': '${$1}',
    });
    final response = await _client.get(url);
    return ListOperationsResponse.fromJson(response);
  }

  /// Provides the `Operations` service functionality in this service.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  ///
  /// This method can be used to get the current status of a long-running
  /// operation.
  Future<Operation<T, S>> getOperation<
    T extends ProtoMessage,
    S extends ProtoMessage
  >(Operation<T, S> request) async {
    final url = Uri.https(_host, '/v2/${request.name}');
    final response = await _client.get(url);
    return Operation.fromJson(response, request.operationHelper);
  }

  /// Provides the `Operations` service functionality in this service.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<void> cancelOperation(CancelOperationRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}:cancel');
    await _client.post(url, body: request);
  }

  /// Closes the client and cleans up any resources associated with it.
  ///
  /// Once [close] is called, no other methods should be called.
  void close() => _client.close();
}

/// Service for configuring logs-based metrics.
final class MetricsServiceV2 {
  static const _host = 'logging.googleapis.com';

  final ServiceClient _client;

  /// Creates a `MetricsServiceV2` using [client] for transport.
  ///
  /// The provided [http.Client] must be configured to provide whatever
  /// authentication is required by `MetricsServiceV2`. You can do that using
  /// [`package:googleapis_auth`](https://pub.dev/packages/googleapis_auth).
  MetricsServiceV2({required http.Client client})
    : _client = ServiceClient(client: client);

  /// Creates a `MetricsServiceV2` that does authentication through an API key.
  ///
  /// If called without arguments, the API key is taken from these environment
  /// variables:
  ///
  /// - `GOOGLE_API_KEY`
  ///
  /// Throws [ConfigurationException] if called without arguments and none of
  /// the above environment variables are set. On the web,
  /// always throws [ConfigurationException] if called without arguments.
  ///
  /// See [API Keys Overview](https://cloud.google.com/api-keys/docs/overview).
  factory MetricsServiceV2.fromApiKey([String? apiKey]) =>
      MetricsServiceV2(client: httpClientFromApiKey(apiKey, _apiKeys));

  /// Lists logs-based metrics.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<ListLogMetricsResponse> listLogMetrics(
    ListLogMetricsRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/${request.parent}/metrics', {
      if (request.pageToken case final $1 when $1.isNotDefault) 'pageToken': $1,
      if (request.pageSize case final $1 when $1.isNotDefault)
        'pageSize': '${$1}',
    });
    final response = await _client.get(url);
    return ListLogMetricsResponse.fromJson(response);
  }

  /// Gets a logs-based metric.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogMetric> getLogMetric(GetLogMetricRequest request) async {
    final url = Uri.https(_host, '/v2/${request.metricName}');
    final response = await _client.get(url);
    return LogMetric.fromJson(response);
  }

  /// Creates a logs-based metric.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogMetric> createLogMetric(CreateLogMetricRequest request) async {
    final url = Uri.https(_host, '/v2/${request.parent}/metrics');
    final response = await _client.post(url, body: request.metric);
    return LogMetric.fromJson(response);
  }

  /// Creates or updates a logs-based metric.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<LogMetric> updateLogMetric(UpdateLogMetricRequest request) async {
    final url = Uri.https(_host, '/v2/${request.metricName}');
    final response = await _client.put(url, body: request.metric);
    return LogMetric.fromJson(response);
  }

  /// Deletes a logs-based metric.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<void> deleteLogMetric(DeleteLogMetricRequest request) async {
    final url = Uri.https(_host, '/v2/${request.metricName}');
    await _client.delete(url);
  }

  /// Provides the `Operations` service functionality in this service.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<ListOperationsResponse> listOperations(
    ListOperationsRequest request,
  ) async {
    final url = Uri.https(_host, '/v2/${request.name}/operations', {
      if (request.filter case final $1 when $1.isNotDefault) 'filter': $1,
      if (request.pageSize case final $1 when $1.isNotDefault)
        'pageSize': '${$1}',
      if (request.pageToken case final $1 when $1.isNotDefault) 'pageToken': $1,
      if (request.returnPartialSuccess case final $1 when $1.isNotDefault)
        'returnPartialSuccess': '${$1}',
    });
    final response = await _client.get(url);
    return ListOperationsResponse.fromJson(response);
  }

  /// Provides the `Operations` service functionality in this service.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  ///
  /// This method can be used to get the current status of a long-running
  /// operation.
  Future<Operation<T, S>> getOperation<
    T extends ProtoMessage,
    S extends ProtoMessage
  >(Operation<T, S> request) async {
    final url = Uri.https(_host, '/v2/${request.name}');
    final response = await _client.get(url);
    return Operation.fromJson(response, request.operationHelper);
  }

  /// Provides the `Operations` service functionality in this service.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [StatusException] if the API failed with a
  /// [Status] message. Throws a [ServiceException] for any other failure.
  Future<void> cancelOperation(CancelOperationRequest request) async {
    final url = Uri.https(_host, '/v2/${request.name}:cancel');
    await _client.post(url, body: request);
  }

  /// Closes the client and cleans up any resources associated with it.
  ///
  /// Once [close] is called, no other methods should be called.
  void close() => _client.close();
}

/// An individual entry in a log.
final class LogEntry extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.LogEntry';

  /// Required. The resource name of the log to which this log entry belongs:
  ///
  ///     "projects/[PROJECT_ID]/logs/[LOG_ID]"
  ///     "organizations/[ORGANIZATION_ID]/logs/[LOG_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/logs/[LOG_ID]"
  ///     "folders/[FOLDER_ID]/logs/[LOG_ID]"
  ///
  /// A project number may be used in place of PROJECT_ID. The project number is
  /// translated to its corresponding PROJECT_ID internally and the `log_name`
  /// field will contain PROJECT_ID in queries and exports.
  ///
  /// `[LOG_ID]` must be URL-encoded within `log_name`. Example:
  /// `"organizations/1234567890/logs/cloudresourcemanager.googleapis.com%2Factivity"`.
  ///
  /// `[LOG_ID]` must be less than 512 characters long and can only include the
  /// following characters: upper and lower case alphanumeric characters,
  /// forward-slash, underscore, hyphen, and period.
  ///
  /// For backward compatibility, if `log_name` begins with a forward-slash, such
  /// as `/projects/...`, then the log entry is ingested as usual, but the
  /// forward-slash is removed. Listing the log entry will not show the leading
  /// slash and filtering for a log name with a leading slash will never return
  /// any results.
  final String logName;

  /// Required. The monitored resource that produced this log entry.
  ///
  /// Example: a log entry that reports a database error would be associated with
  /// the monitored resource designating the particular database that reported
  /// the error.
  final MonitoredResource? resource;

  /// The log entry payload, represented as a protocol buffer. Some Google
  /// Cloud Platform services use this field for their log entry payloads.
  ///
  /// The following protocol buffer types are supported; user-defined types
  /// are not supported:
  ///
  ///   "type.googleapis.com/google.cloud.audit.AuditLog"
  ///   "type.googleapis.com/google.appengine.logging.v1.RequestLog"
  final Any? protoPayload;

  /// The log entry payload, represented as a Unicode string (UTF-8).
  final String? textPayload;

  /// The log entry payload, represented as a structure that is
  /// expressed as a JSON object.
  final Struct? jsonPayload;

  /// Optional. The time the event described by the log entry occurred. This time
  /// is used to compute the log entry's age and to enforce the logs retention
  /// period. If this field is omitted in a new log entry, then Logging assigns
  /// it the current time. Timestamps have nanosecond accuracy, but trailing
  /// zeros in the fractional seconds might be omitted when the timestamp is
  /// displayed.
  ///
  /// Incoming log entries must have timestamps that don't exceed the
  /// [logs retention
  /// period](https://cloud.google.com/logging/quotas#logs_retention_periods) in
  /// the past, and that don't exceed 24 hours in the future. Log entries outside
  /// those time boundaries aren't ingested by Logging.
  final Timestamp? timestamp;

  /// Output only. The time the log entry was received by Logging.
  final Timestamp? receiveTimestamp;

  /// Optional. The severity of the log entry. The default value is
  /// `LogSeverity.DEFAULT`.
  final logging_type.LogSeverity severity;

  /// Optional. A unique identifier for the log entry. If you provide a value,
  /// then Logging considers other log entries in the same project, with the same
  /// `timestamp`, and with the same `insert_id` to be duplicates which are
  /// removed in a single query result. However, there are no guarantees of
  /// de-duplication in the export of logs.
  ///
  /// If the `insert_id` is omitted when writing a log entry, the Logging API
  /// assigns its own unique identifier in this field.
  ///
  /// In queries, the `insert_id` is also used to order log entries that have
  /// the same `log_name` and `timestamp` values.
  final String insertId;

  /// Optional. Information about the HTTP request associated with this log
  /// entry, if applicable.
  final logging_type.HttpRequest? httpRequest;

  /// Optional. A map of key, value pairs that provides additional information
  /// about the log entry. The labels can be user-defined or system-defined.
  ///
  /// User-defined labels are arbitrary key, value pairs that you can use to
  /// classify logs.
  ///
  /// System-defined labels are defined by GCP services for platform logs.
  /// They have two components - a service namespace component and the
  /// attribute name. For example: `compute.googleapis.com/resource_name`.
  ///
  /// Cloud Logging truncates label keys that exceed 512 B and label
  /// values that exceed 64 KB upon their associated log entry being
  /// written. The truncation is indicated by an ellipsis at the
  /// end of the character string.
  final Map<String, String> labels;

  /// Optional. Information about an operation associated with the log entry, if
  /// applicable.
  final LogEntryOperation? operation;

  /// Optional. The REST resource name of the trace being written to
  /// [Cloud Trace](https://cloud.google.com/trace) in
  /// association with this log entry. For example, if your trace data is stored
  /// in the Cloud project "my-trace-project" and if the service that is creating
  /// the log entry receives a trace header that includes the trace ID "12345",
  /// then the service should use "projects/my-tracing-project/traces/12345".
  ///
  /// The `trace` field provides the link between logs and traces. By using
  /// this field, you can navigate from a log entry to a trace.
  final String trace;

  /// Optional. The ID of the [Cloud Trace](https://cloud.google.com/trace) span
  /// associated with the current operation in which the log is being written.
  /// For example, if a span has the REST resource name of
  /// "projects/some-project/traces/some-trace/spans/some-span-id", then the
  /// `span_id` field is "some-span-id".
  ///
  /// A
  /// [Span](https://cloud.google.com/trace/docs/reference/v2/rest/v2/projects.traces/batchWrite#Span)
  /// represents a single operation within a trace. Whereas a trace may involve
  /// multiple different microservices running on multiple different machines,
  /// a span generally corresponds to a single logical operation being performed
  /// in a single instance of a microservice on one specific machine. Spans
  /// are the nodes within the tree that is a trace.
  ///
  /// Applications that are [instrumented for
  /// tracing](https://cloud.google.com/trace/docs/setup) will generally assign a
  /// new, unique span ID on each incoming request. It is also common to create
  /// and record additional spans corresponding to internal processing elements
  /// as well as issuing requests to dependencies.
  ///
  /// The span ID is expected to be a 16-character, hexadecimal encoding of an
  /// 8-byte array and should not be zero. It should be unique within the trace
  /// and should, ideally, be generated in a manner that is uniformly random.
  ///
  /// Example values:
  ///
  ///   - `000000000000004a`
  ///   - `7a2190356c3fc94b`
  ///   - `0000f00300090021`
  ///   - `d39223e101960076`
  final String spanId;

  /// Optional. The sampling decision of the trace associated with the log entry.
  ///
  /// True means that the trace resource name in the `trace` field was sampled
  /// for storage in a trace backend. False means that the trace was not sampled
  /// for storage when this log entry was written, or the sampling decision was
  /// unknown at the time. A non-sampled `trace` value is still useful as a
  /// request correlation identifier. The default is False.
  final bool traceSampled;

  /// Optional. Source code location information associated with the log entry,
  /// if any.
  final LogEntrySourceLocation? sourceLocation;

  /// Optional. Information indicating this LogEntry is part of a sequence of
  /// multiple log entries split from a single LogEntry.
  final LogSplit? split;

  LogEntry({
    required this.logName,
    required this.resource,
    this.protoPayload,
    this.textPayload,
    this.jsonPayload,
    this.timestamp,
    this.receiveTimestamp,
    this.severity = logging_type.LogSeverity.$default,
    this.insertId = '',
    this.httpRequest,
    this.labels = const {},
    this.operation,
    this.trace = '',
    this.spanId = '',
    this.traceSampled = false,
    this.sourceLocation,
    this.split,
  }) : super(fullyQualifiedName);

  factory LogEntry.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return LogEntry(
      logName: switch (json['logName']) {
        null => '',
        Object $1 => decodeString($1),
      },
      resource: switch (json['resource']) {
        null => null,
        Object $1 => MonitoredResource.fromJson($1),
      },
      protoPayload: switch (json['protoPayload']) {
        null => null,
        Object $1 => Any.fromJson($1),
      },
      textPayload: switch (json['textPayload']) {
        null => null,
        Object $1 => decodeString($1),
      },
      jsonPayload: switch (json['jsonPayload']) {
        null => null,
        Object $1 => Struct.fromJson($1),
      },
      timestamp: switch (json['timestamp']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      receiveTimestamp: switch (json['receiveTimestamp']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      severity: switch (json['severity']) {
        null => logging_type.LogSeverity.$default,
        Object $1 => logging_type.LogSeverity.fromJson($1),
      },
      insertId: switch (json['insertId']) {
        null => '',
        Object $1 => decodeString($1),
      },
      httpRequest: switch (json['httpRequest']) {
        null => null,
        Object $1 => logging_type.HttpRequest.fromJson($1),
      },
      labels: switch (json['labels']) {
        null => {},
        Map<String, Object?> $1 => {
          for (final e in $1.entries)
            decodeString(e.key): decodeString(e.value),
        },
        _ => throw const FormatException('"labels" is not an object'),
      },
      operation: switch (json['operation']) {
        null => null,
        Object $1 => LogEntryOperation.fromJson($1),
      },
      trace: switch (json['trace']) {
        null => '',
        Object $1 => decodeString($1),
      },
      spanId: switch (json['spanId']) {
        null => '',
        Object $1 => decodeString($1),
      },
      traceSampled: switch (json['traceSampled']) {
        null => false,
        Object $1 => decodeBool($1),
      },
      sourceLocation: switch (json['sourceLocation']) {
        null => null,
        Object $1 => LogEntrySourceLocation.fromJson($1),
      },
      split: switch (json['split']) {
        null => null,
        Object $1 => LogSplit.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'logName': logName,
    if (resource != null) 'resource': resource!.toJson(),
    if (protoPayload != null) 'protoPayload': protoPayload!.toJson(),
    if (textPayload != null) 'textPayload': textPayload,
    if (jsonPayload != null) 'jsonPayload': jsonPayload!.toJson(),
    if (timestamp != null) 'timestamp': timestamp!.toJson(),
    if (receiveTimestamp != null)
      'receiveTimestamp': receiveTimestamp!.toJson(),
    if (severity.isNotDefault) 'severity': severity.toJson(),
    if (insertId.isNotDefault) 'insertId': insertId,
    if (httpRequest != null) 'httpRequest': httpRequest!.toJson(),
    if (labels.isNotDefault) 'labels': labels,
    if (operation != null) 'operation': operation!.toJson(),
    if (trace.isNotDefault) 'trace': trace,
    if (spanId.isNotDefault) 'spanId': spanId,
    if (traceSampled.isNotDefault) 'traceSampled': traceSampled,
    if (sourceLocation != null) 'sourceLocation': sourceLocation!.toJson(),
    if (split != null) 'split': split!.toJson(),
  };

  @override
  String toString() {
    final contents = [
      'logName=$logName',
      if (textPayload != null) 'textPayload=$textPayload',
      'severity=$severity',
      'insertId=$insertId',
      'trace=$trace',
      'spanId=$spanId',
      'traceSampled=$traceSampled',
    ].join(',');
    return 'LogEntry($contents)';
  }
}

/// Additional information about a potentially long-running operation with which
/// a log entry is associated.
final class LogEntryOperation extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.LogEntryOperation';

  /// Optional. An arbitrary operation identifier. Log entries with the same
  /// identifier are assumed to be part of the same operation.
  final String id;

  /// Optional. An arbitrary producer identifier. The combination of `id` and
  /// `producer` must be globally unique. Examples for `producer`:
  /// `"MyDivision.MyBigCompany.com"`, `"github.com/MyProject/MyApplication"`.
  final String producer;

  /// Optional. Set this to True if this is the first log entry in the operation.
  final bool first;

  /// Optional. Set this to True if this is the last log entry in the operation.
  final bool last;

  LogEntryOperation({
    this.id = '',
    this.producer = '',
    this.first = false,
    this.last = false,
  }) : super(fullyQualifiedName);

  factory LogEntryOperation.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return LogEntryOperation(
      id: switch (json['id']) {
        null => '',
        Object $1 => decodeString($1),
      },
      producer: switch (json['producer']) {
        null => '',
        Object $1 => decodeString($1),
      },
      first: switch (json['first']) {
        null => false,
        Object $1 => decodeBool($1),
      },
      last: switch (json['last']) {
        null => false,
        Object $1 => decodeBool($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (id.isNotDefault) 'id': id,
    if (producer.isNotDefault) 'producer': producer,
    if (first.isNotDefault) 'first': first,
    if (last.isNotDefault) 'last': last,
  };

  @override
  String toString() {
    final contents = [
      'id=$id',
      'producer=$producer',
      'first=$first',
      'last=$last',
    ].join(',');
    return 'LogEntryOperation($contents)';
  }
}

/// Additional information about the source code location that produced the log
/// entry.
final class LogEntrySourceLocation extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.LogEntrySourceLocation';

  /// Optional. Source file name. Depending on the runtime environment, this
  /// might be a simple name or a fully-qualified name.
  final String file;

  /// Optional. Line within the source file. 1-based; 0 indicates no line number
  /// available.
  final int line;

  /// Optional. Human-readable name of the function or method being invoked, with
  /// optional context such as the class or package name. This information may be
  /// used in contexts such as the logs viewer, where a file and line number are
  /// less meaningful. The format can vary by language. For example:
  /// `qual.if.ied.Class.method` (Java), `dir/package.func` (Go), `function`
  /// (Python).
  final String function;

  LogEntrySourceLocation({this.file = '', this.line = 0, this.function = ''})
    : super(fullyQualifiedName);

  factory LogEntrySourceLocation.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return LogEntrySourceLocation(
      file: switch (json['file']) {
        null => '',
        Object $1 => decodeString($1),
      },
      line: switch (json['line']) {
        null => 0,
        Object $1 => decodeInt64($1),
      },
      function: switch (json['function']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (file.isNotDefault) 'file': file,
    if (line.isNotDefault) 'line': encodeInt64(line),
    if (function.isNotDefault) 'function': function,
  };

  @override
  String toString() {
    final contents = [
      'file=$file',
      'line=$line',
      'function=$function',
    ].join(',');
    return 'LogEntrySourceLocation($contents)';
  }
}

/// Additional information used to correlate multiple log entries. Used when a
/// single LogEntry would exceed the Google Cloud Logging size limit and is
/// split across multiple log entries.
final class LogSplit extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.LogSplit';

  /// A globally unique identifier for all log entries in a sequence of split log
  /// entries. All log entries with the same |LogSplit.uid| are assumed to be
  /// part of the same sequence of split log entries.
  final String uid;

  /// The index of this LogEntry in the sequence of split log entries. Log
  /// entries are given |index| values 0, 1, ..., n-1 for a sequence of n log
  /// entries.
  final int index;

  /// The total number of log entries that the original LogEntry was split into.
  final int totalSplits;

  LogSplit({this.uid = '', this.index = 0, this.totalSplits = 0})
    : super(fullyQualifiedName);

  factory LogSplit.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return LogSplit(
      uid: switch (json['uid']) {
        null => '',
        Object $1 => decodeString($1),
      },
      index: switch (json['index']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
      totalSplits: switch (json['totalSplits']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (uid.isNotDefault) 'uid': uid,
    if (index.isNotDefault) 'index': index,
    if (totalSplits.isNotDefault) 'totalSplits': totalSplits,
  };

  @override
  String toString() {
    final contents = [
      'uid=$uid',
      'index=$index',
      'totalSplits=$totalSplits',
    ].join(',');
    return 'LogSplit($contents)';
  }
}

/// The parameters to DeleteLog.
final class DeleteLogRequest extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.DeleteLogRequest';

  /// Required. The resource name of the log to delete:
  ///
  /// * `projects/[PROJECT_ID]/logs/[LOG_ID]`
  /// * `organizations/[ORGANIZATION_ID]/logs/[LOG_ID]`
  /// * `billingAccounts/[BILLING_ACCOUNT_ID]/logs/[LOG_ID]`
  /// * `folders/[FOLDER_ID]/logs/[LOG_ID]`
  ///
  /// `[LOG_ID]` must be URL-encoded. For example,
  /// `"projects/my-project-id/logs/syslog"`,
  /// `"organizations/123/logs/cloudaudit.googleapis.com%2Factivity"`.
  ///
  /// For more information about log names, see
  /// `LogEntry`.
  final String logName;

  DeleteLogRequest({required this.logName}) : super(fullyQualifiedName);

  factory DeleteLogRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return DeleteLogRequest(
      logName: switch (json['logName']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'logName': logName};

  @override
  String toString() {
    final contents = ['logName=$logName'].join(',');
    return 'DeleteLogRequest($contents)';
  }
}

/// The parameters to WriteLogEntries.
final class WriteLogEntriesRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.WriteLogEntriesRequest';

  /// Optional. A default log resource name that is assigned to all log entries
  /// in `entries` that do not specify a value for `log_name`:
  ///
  /// * `projects/[PROJECT_ID]/logs/[LOG_ID]`
  /// * `organizations/[ORGANIZATION_ID]/logs/[LOG_ID]`
  /// * `billingAccounts/[BILLING_ACCOUNT_ID]/logs/[LOG_ID]`
  /// * `folders/[FOLDER_ID]/logs/[LOG_ID]`
  ///
  /// `[LOG_ID]` must be URL-encoded. For example:
  ///
  ///     "projects/my-project-id/logs/syslog"
  ///     "organizations/123/logs/cloudaudit.googleapis.com%2Factivity"
  ///
  /// The permission `logging.logEntries.create` is needed on each project,
  /// organization, billing account, or folder that is receiving new log
  /// entries, whether the resource is specified in `logName` or in an
  /// individual log entry.
  final String logName;

  /// Optional. A default monitored resource object that is assigned to all log
  /// entries in `entries` that do not specify a value for `resource`. Example:
  ///
  ///     { "type": "gce_instance",
  ///       "labels": {
  ///         "zone": "us-central1-a", "instance_id": "00000000000000000000" }}
  ///
  /// See `LogEntry`.
  final MonitoredResource? resource;

  /// Optional. Default labels that are added to the `labels` field of all log
  /// entries in `entries`. If a log entry already has a label with the same key
  /// as a label in this parameter, then the log entry's label is not changed.
  /// See `LogEntry`.
  final Map<String, String> labels;

  /// Required. The log entries to send to Logging. The order of log
  /// entries in this list does not matter. Values supplied in this method's
  /// `log_name`, `resource`, and `labels` fields are copied into those log
  /// entries in this list that do not include values for their corresponding
  /// fields. For more information, see the
  /// `LogEntry` type.
  ///
  /// If the `timestamp` or `insert_id` fields are missing in log entries, then
  /// this method supplies the current time or a unique identifier, respectively.
  /// The supplied values are chosen so that, among the log entries that did not
  /// supply their own values, the entries earlier in the list will sort before
  /// the entries later in the list. See the `entries.list` method.
  ///
  /// Log entries with timestamps that are more than the
  /// [logs retention period](https://cloud.google.com/logging/quotas) in
  /// the past or more than 24 hours in the future will not be available when
  /// calling `entries.list`. However, those log entries can still be [exported
  /// with
  /// LogSinks](https://cloud.google.com/logging/docs/api/tasks/exporting-logs).
  ///
  /// To improve throughput and to avoid exceeding the
  /// [quota limit](https://cloud.google.com/logging/quotas) for calls to
  /// `entries.write`, you should try to include several log entries in this
  /// list, rather than calling this method for each individual log entry.
  final List<LogEntry> entries;

  /// Optional. Whether a batch's valid entries should be written even if some
  /// other entry failed due to a permanent error such as INVALID_ARGUMENT or
  /// PERMISSION_DENIED. If any entry failed, then the response status is the
  /// response status of one of the failed entries. The response will include
  /// error details in `WriteLogEntriesPartialErrors.log_entry_errors` keyed by
  /// the entries' zero-based index in the `entries`. Failed requests for which
  /// no entries are written will not include per-entry errors.
  final bool partialSuccess;

  /// Optional. If true, the request should expect normal response, but the
  /// entries won't be persisted nor exported. Useful for checking whether the
  /// logging API endpoints are working properly before sending valuable data.
  final bool dryRun;

  WriteLogEntriesRequest({
    this.logName = '',
    this.resource,
    this.labels = const {},
    required this.entries,
    this.partialSuccess = false,
    this.dryRun = false,
  }) : super(fullyQualifiedName);

  factory WriteLogEntriesRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return WriteLogEntriesRequest(
      logName: switch (json['logName']) {
        null => '',
        Object $1 => decodeString($1),
      },
      resource: switch (json['resource']) {
        null => null,
        Object $1 => MonitoredResource.fromJson($1),
      },
      labels: switch (json['labels']) {
        null => {},
        Map<String, Object?> $1 => {
          for (final e in $1.entries)
            decodeString(e.key): decodeString(e.value),
        },
        _ => throw const FormatException('"labels" is not an object'),
      },
      entries: switch (json['entries']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) LogEntry.fromJson(i)],
        _ => throw const FormatException('"entries" is not a list'),
      },
      partialSuccess: switch (json['partialSuccess']) {
        null => false,
        Object $1 => decodeBool($1),
      },
      dryRun: switch (json['dryRun']) {
        null => false,
        Object $1 => decodeBool($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (logName.isNotDefault) 'logName': logName,
    if (resource != null) 'resource': resource!.toJson(),
    if (labels.isNotDefault) 'labels': labels,
    'entries': encodeList(entries),
    if (partialSuccess.isNotDefault) 'partialSuccess': partialSuccess,
    if (dryRun.isNotDefault) 'dryRun': dryRun,
  };

  @override
  String toString() {
    final contents = [
      'logName=$logName',
      'partialSuccess=$partialSuccess',
      'dryRun=$dryRun',
    ].join(',');
    return 'WriteLogEntriesRequest($contents)';
  }
}

/// Result returned from WriteLogEntries.
final class WriteLogEntriesResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.WriteLogEntriesResponse';

  WriteLogEntriesResponse() : super(fullyQualifiedName);

  factory WriteLogEntriesResponse.fromJson(Object? j) =>
      WriteLogEntriesResponse();

  @override
  Object toJson() => {};

  @override
  String toString() => 'WriteLogEntriesResponse()';
}

/// Error details for WriteLogEntries with partial success.
final class WriteLogEntriesPartialErrors extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.WriteLogEntriesPartialErrors';

  /// When `WriteLogEntriesRequest.partial_success` is true, records the error
  /// status for entries that were not written due to a permanent error, keyed
  /// by the entry's zero-based index in `WriteLogEntriesRequest.entries`.
  ///
  /// Failed requests for which no entries are written will not include
  /// per-entry errors.
  final Map<int, Status> logEntryErrors;

  WriteLogEntriesPartialErrors({this.logEntryErrors = const {}})
    : super(fullyQualifiedName);

  factory WriteLogEntriesPartialErrors.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return WriteLogEntriesPartialErrors(
      logEntryErrors: switch (json['logEntryErrors']) {
        null => {},
        Map<String, Object?> $1 => {
          for (final e in $1.entries)
            decodeInt(e.key): Status.fromJson(e.value),
        },
        _ => throw const FormatException('"logEntryErrors" is not an object'),
      },
    );
  }

  @override
  Object toJson() => {
    if (logEntryErrors.isNotDefault)
      'logEntryErrors': encodeMap(logEntryErrors),
  };

  @override
  String toString() => 'WriteLogEntriesPartialErrors()';
}

/// The parameters to `ListLogEntries`.
final class ListLogEntriesRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListLogEntriesRequest';

  /// Required. Names of one or more parent resources from which to
  /// retrieve log entries:
  ///
  /// *  `projects/[PROJECT_ID]`
  /// *  `organizations/[ORGANIZATION_ID]`
  /// *  `billingAccounts/[BILLING_ACCOUNT_ID]`
  /// *  `folders/[FOLDER_ID]`
  ///
  /// May alternatively be one or more views:
  ///
  ///  * `projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]`
  ///  * `organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]`
  ///  * `billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]`
  ///  * `folders/[FOLDER_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]`
  ///
  /// Projects listed in the `project_ids` field are added to this list.
  /// A maximum of 100 resources may be specified in a single request.
  final List<String> resourceNames;

  /// Optional. Only log entries that match the filter are returned.  An empty
  /// filter matches all log entries in the resources listed in `resource_names`.
  /// Referencing a parent resource that is not listed in `resource_names` will
  /// cause the filter to return no results. The maximum length of a filter is
  /// 20,000 characters.
  final String filter;

  /// Optional. How the results should be sorted.  Presently, the only permitted
  /// values are `"timestamp asc"` (default) and `"timestamp desc"`. The first
  /// option returns entries in order of increasing values of
  /// `LogEntry.timestamp` (oldest first), and the second option returns entries
  /// in order of decreasing timestamps (newest first).  Entries with equal
  /// timestamps are returned in order of their `insert_id` values.
  final String orderBy;

  /// Optional. The maximum number of results to return from this request.
  /// Default is 50. If the value is negative or exceeds 1000, the request is
  /// rejected. The presence of `next_page_token` in the response indicates that
  /// more results might be available.
  final int pageSize;

  /// Optional. If present, then retrieve the next batch of results from the
  /// preceding call to this method.  `page_token` must be the value of
  /// `next_page_token` from the previous response.  The values of other method
  /// parameters should be identical to those in the previous call.
  final String pageToken;

  ListLogEntriesRequest({
    required this.resourceNames,
    this.filter = '',
    this.orderBy = '',
    this.pageSize = 0,
    this.pageToken = '',
  }) : super(fullyQualifiedName);

  factory ListLogEntriesRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListLogEntriesRequest(
      resourceNames: switch (json['resourceNames']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) decodeString(i)],
        _ => throw const FormatException('"resourceNames" is not a list'),
      },
      filter: switch (json['filter']) {
        null => '',
        Object $1 => decodeString($1),
      },
      orderBy: switch (json['orderBy']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageSize: switch (json['pageSize']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
      pageToken: switch (json['pageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    'resourceNames': resourceNames,
    if (filter.isNotDefault) 'filter': filter,
    if (orderBy.isNotDefault) 'orderBy': orderBy,
    if (pageSize.isNotDefault) 'pageSize': pageSize,
    if (pageToken.isNotDefault) 'pageToken': pageToken,
  };

  @override
  String toString() {
    final contents = [
      'filter=$filter',
      'orderBy=$orderBy',
      'pageSize=$pageSize',
      'pageToken=$pageToken',
    ].join(',');
    return 'ListLogEntriesRequest($contents)';
  }
}

/// Result returned from `ListLogEntries`.
final class ListLogEntriesResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListLogEntriesResponse';

  /// A list of log entries.  If `entries` is empty, `nextPageToken` may still be
  /// returned, indicating that more entries may exist.  See `nextPageToken` for
  /// more information.
  final List<LogEntry> entries;

  /// If there might be more results than those appearing in this response, then
  /// `nextPageToken` is included.  To get the next set of results, call this
  /// method again using the value of `nextPageToken` as `pageToken`.
  ///
  /// If a value for `next_page_token` appears and the `entries` field is empty,
  /// it means that the search found no log entries so far but it did not have
  /// time to search all the possible log entries.  Retry the method with this
  /// value for `page_token` to continue the search.  Alternatively, consider
  /// speeding up the search by changing your filter to specify a single log name
  /// or resource type, or to narrow the time range of the search.
  final String nextPageToken;

  ListLogEntriesResponse({this.entries = const [], this.nextPageToken = ''})
    : super(fullyQualifiedName);

  factory ListLogEntriesResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListLogEntriesResponse(
      entries: switch (json['entries']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) LogEntry.fromJson(i)],
        _ => throw const FormatException('"entries" is not a list'),
      },
      nextPageToken: switch (json['nextPageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (entries.isNotDefault) 'entries': encodeList(entries),
    if (nextPageToken.isNotDefault) 'nextPageToken': nextPageToken,
  };

  @override
  String toString() {
    final contents = ['nextPageToken=$nextPageToken'].join(',');
    return 'ListLogEntriesResponse($contents)';
  }
}

/// The parameters to ListMonitoredResourceDescriptors
final class ListMonitoredResourceDescriptorsRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListMonitoredResourceDescriptorsRequest';

  /// Optional. The maximum number of results to return from this request.
  /// Non-positive values are ignored.  The presence of `nextPageToken` in the
  /// response indicates that more results might be available.
  final int pageSize;

  /// Optional. If present, then retrieve the next batch of results from the
  /// preceding call to this method.  `pageToken` must be the value of
  /// `nextPageToken` from the previous response.  The values of other method
  /// parameters should be identical to those in the previous call.
  final String pageToken;

  ListMonitoredResourceDescriptorsRequest({
    this.pageSize = 0,
    this.pageToken = '',
  }) : super(fullyQualifiedName);

  factory ListMonitoredResourceDescriptorsRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListMonitoredResourceDescriptorsRequest(
      pageSize: switch (json['pageSize']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
      pageToken: switch (json['pageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (pageSize.isNotDefault) 'pageSize': pageSize,
    if (pageToken.isNotDefault) 'pageToken': pageToken,
  };

  @override
  String toString() {
    final contents = ['pageSize=$pageSize', 'pageToken=$pageToken'].join(',');
    return 'ListMonitoredResourceDescriptorsRequest($contents)';
  }
}

/// Result returned from ListMonitoredResourceDescriptors.
final class ListMonitoredResourceDescriptorsResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListMonitoredResourceDescriptorsResponse';

  /// A list of resource descriptors.
  final List<MonitoredResourceDescriptor> resourceDescriptors;

  /// If there might be more results than those appearing in this response, then
  /// `nextPageToken` is included.  To get the next set of results, call this
  /// method again using the value of `nextPageToken` as `pageToken`.
  final String nextPageToken;

  ListMonitoredResourceDescriptorsResponse({
    this.resourceDescriptors = const [],
    this.nextPageToken = '',
  }) : super(fullyQualifiedName);

  factory ListMonitoredResourceDescriptorsResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListMonitoredResourceDescriptorsResponse(
      resourceDescriptors: switch (json['resourceDescriptors']) {
        null => [],
        List<Object?> $1 => [
          for (final i in $1) MonitoredResourceDescriptor.fromJson(i),
        ],
        _ => throw const FormatException('"resourceDescriptors" is not a list'),
      },
      nextPageToken: switch (json['nextPageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (resourceDescriptors.isNotDefault)
      'resourceDescriptors': encodeList(resourceDescriptors),
    if (nextPageToken.isNotDefault) 'nextPageToken': nextPageToken,
  };

  @override
  String toString() {
    final contents = ['nextPageToken=$nextPageToken'].join(',');
    return 'ListMonitoredResourceDescriptorsResponse($contents)';
  }
}

/// The parameters to ListLogs.
final class ListLogsRequest extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.ListLogsRequest';

  /// Required. The resource name to list logs for:
  ///
  /// *  `projects/[PROJECT_ID]`
  /// *  `organizations/[ORGANIZATION_ID]`
  /// *  `billingAccounts/[BILLING_ACCOUNT_ID]`
  /// *  `folders/[FOLDER_ID]`
  final String parent;

  /// Optional. List of resource names to list logs for:
  ///
  ///  * `projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]`
  ///  * `organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]`
  ///  * `billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]`
  ///  * `folders/[FOLDER_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]`
  ///
  /// To support legacy queries, it could also be:
  ///
  /// *  `projects/[PROJECT_ID]`
  /// *  `organizations/[ORGANIZATION_ID]`
  /// *  `billingAccounts/[BILLING_ACCOUNT_ID]`
  /// *  `folders/[FOLDER_ID]`
  ///
  /// The resource name in the `parent` field is added to this list.
  final List<String> resourceNames;

  /// Optional. The maximum number of results to return from this request.
  /// Non-positive values are ignored.  The presence of `nextPageToken` in the
  /// response indicates that more results might be available.
  final int pageSize;

  /// Optional. If present, then retrieve the next batch of results from the
  /// preceding call to this method.  `pageToken` must be the value of
  /// `nextPageToken` from the previous response.  The values of other method
  /// parameters should be identical to those in the previous call.
  final String pageToken;

  ListLogsRequest({
    required this.parent,
    this.resourceNames = const [],
    this.pageSize = 0,
    this.pageToken = '',
  }) : super(fullyQualifiedName);

  factory ListLogsRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListLogsRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      resourceNames: switch (json['resourceNames']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) decodeString(i)],
        _ => throw const FormatException('"resourceNames" is not a list'),
      },
      pageSize: switch (json['pageSize']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
      pageToken: switch (json['pageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (resourceNames.isNotDefault) 'resourceNames': resourceNames,
    if (pageSize.isNotDefault) 'pageSize': pageSize,
    if (pageToken.isNotDefault) 'pageToken': pageToken,
  };

  @override
  String toString() {
    final contents = [
      'parent=$parent',
      'pageSize=$pageSize',
      'pageToken=$pageToken',
    ].join(',');
    return 'ListLogsRequest($contents)';
  }
}

/// Result returned from ListLogs.
final class ListLogsResponse extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.ListLogsResponse';

  /// A list of log names. For example,
  /// `"projects/my-project/logs/syslog"` or
  /// `"organizations/123/logs/cloudresourcemanager.googleapis.com%2Factivity"`.
  final List<String> logNames;

  /// If there might be more results than those appearing in this response, then
  /// `nextPageToken` is included.  To get the next set of results, call this
  /// method again using the value of `nextPageToken` as `pageToken`.
  final String nextPageToken;

  ListLogsResponse({this.logNames = const [], this.nextPageToken = ''})
    : super(fullyQualifiedName);

  factory ListLogsResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListLogsResponse(
      logNames: switch (json['logNames']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) decodeString(i)],
        _ => throw const FormatException('"logNames" is not a list'),
      },
      nextPageToken: switch (json['nextPageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (logNames.isNotDefault) 'logNames': logNames,
    if (nextPageToken.isNotDefault) 'nextPageToken': nextPageToken,
  };

  @override
  String toString() {
    final contents = ['nextPageToken=$nextPageToken'].join(',');
    return 'ListLogsResponse($contents)';
  }
}

/// The parameters to `TailLogEntries`.
final class TailLogEntriesRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.TailLogEntriesRequest';

  /// Required. Name of a parent resource from which to retrieve log entries:
  ///
  /// *  `projects/[PROJECT_ID]`
  /// *  `organizations/[ORGANIZATION_ID]`
  /// *  `billingAccounts/[BILLING_ACCOUNT_ID]`
  /// *  `folders/[FOLDER_ID]`
  ///
  /// May alternatively be one or more views:
  ///
  ///  * `projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]`
  ///  * `organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]`
  ///  * `billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]`
  ///  * `folders/[FOLDER_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]`
  final List<String> resourceNames;

  /// Optional. Only log entries that match the filter are returned.  An empty
  /// filter matches all log entries in the resources listed in `resource_names`.
  /// Referencing a parent resource that is not listed in `resource_names` will
  /// cause the filter to return no results. The maximum length of a filter is
  /// 20,000 characters.
  final String filter;

  /// Optional. The amount of time to buffer log entries at the server before
  /// being returned to prevent out of order results due to late arriving log
  /// entries. Valid values are between 0-60000 milliseconds. Defaults to 2000
  /// milliseconds.
  final Duration? bufferWindow;

  TailLogEntriesRequest({
    required this.resourceNames,
    this.filter = '',
    this.bufferWindow,
  }) : super(fullyQualifiedName);

  factory TailLogEntriesRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return TailLogEntriesRequest(
      resourceNames: switch (json['resourceNames']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) decodeString(i)],
        _ => throw const FormatException('"resourceNames" is not a list'),
      },
      filter: switch (json['filter']) {
        null => '',
        Object $1 => decodeString($1),
      },
      bufferWindow: switch (json['bufferWindow']) {
        null => null,
        Object $1 => Duration.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'resourceNames': resourceNames,
    if (filter.isNotDefault) 'filter': filter,
    if (bufferWindow != null) 'bufferWindow': bufferWindow!.toJson(),
  };

  @override
  String toString() {
    final contents = ['filter=$filter'].join(',');
    return 'TailLogEntriesRequest($contents)';
  }
}

/// Result returned from `TailLogEntries`.
final class TailLogEntriesResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.TailLogEntriesResponse';

  /// A list of log entries. Each response in the stream will order entries with
  /// increasing values of `LogEntry.timestamp`. Ordering is not guaranteed
  /// between separate responses.
  final List<LogEntry> entries;

  /// If entries that otherwise would have been included in the session were not
  /// sent back to the client, counts of relevant entries omitted from the
  /// session with the reason that they were not included. There will be at most
  /// one of each reason per response. The counts represent the number of
  /// suppressed entries since the last streamed response.
  final List<TailLogEntriesResponse_SuppressionInfo> suppressionInfo;

  TailLogEntriesResponse({
    this.entries = const [],
    this.suppressionInfo = const [],
  }) : super(fullyQualifiedName);

  factory TailLogEntriesResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return TailLogEntriesResponse(
      entries: switch (json['entries']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) LogEntry.fromJson(i)],
        _ => throw const FormatException('"entries" is not a list'),
      },
      suppressionInfo: switch (json['suppressionInfo']) {
        null => [],
        List<Object?> $1 => [
          for (final i in $1)
            TailLogEntriesResponse_SuppressionInfo.fromJson(i),
        ],
        _ => throw const FormatException('"suppressionInfo" is not a list'),
      },
    );
  }

  @override
  Object toJson() => {
    if (entries.isNotDefault) 'entries': encodeList(entries),
    if (suppressionInfo.isNotDefault)
      'suppressionInfo': encodeList(suppressionInfo),
  };

  @override
  String toString() => 'TailLogEntriesResponse()';
}

/// Information about entries that were omitted from the session.
final class TailLogEntriesResponse_SuppressionInfo extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.TailLogEntriesResponse.SuppressionInfo';

  /// The reason that entries were omitted from the session.
  final TailLogEntriesResponse_SuppressionInfo_Reason reason;

  /// A lower bound on the count of entries omitted due to `reason`.
  final int suppressedCount;

  TailLogEntriesResponse_SuppressionInfo({
    this.reason = TailLogEntriesResponse_SuppressionInfo_Reason.$default,
    this.suppressedCount = 0,
  }) : super(fullyQualifiedName);

  factory TailLogEntriesResponse_SuppressionInfo.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return TailLogEntriesResponse_SuppressionInfo(
      reason: switch (json['reason']) {
        null => TailLogEntriesResponse_SuppressionInfo_Reason.$default,
        Object $1 => TailLogEntriesResponse_SuppressionInfo_Reason.fromJson($1),
      },
      suppressedCount: switch (json['suppressedCount']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (reason.isNotDefault) 'reason': reason.toJson(),
    if (suppressedCount.isNotDefault) 'suppressedCount': suppressedCount,
  };

  @override
  String toString() {
    final contents = [
      'reason=$reason',
      'suppressedCount=$suppressedCount',
    ].join(',');
    return 'SuppressionInfo($contents)';
  }
}

/// An indicator of why entries were omitted.
final class TailLogEntriesResponse_SuppressionInfo_Reason extends ProtoEnum {
  /// Unexpected default.
  static const reasonUnspecified =
      TailLogEntriesResponse_SuppressionInfo_Reason('REASON_UNSPECIFIED');

  /// Indicates suppression occurred due to relevant entries being
  /// received in excess of rate limits. For quotas and limits, see
  /// [Logging API quotas and
  /// limits](https://cloud.google.com/logging/quotas#api-limits).
  static const rateLimit = TailLogEntriesResponse_SuppressionInfo_Reason(
    'RATE_LIMIT',
  );

  /// Indicates suppression occurred due to the client not consuming
  /// responses quickly enough.
  static const notConsumed = TailLogEntriesResponse_SuppressionInfo_Reason(
    'NOT_CONSUMED',
  );

  /// The default value for [TailLogEntriesResponse_SuppressionInfo_Reason].
  static const $default = reasonUnspecified;

  const TailLogEntriesResponse_SuppressionInfo_Reason(super.value);

  factory TailLogEntriesResponse_SuppressionInfo_Reason.fromJson(
    Object? json,
  ) => TailLogEntriesResponse_SuppressionInfo_Reason(json as String);

  bool get isNotDefault => this != $default;

  @override
  String toString() => 'Reason.$value';
}

/// Configuration for an indexed field.
final class IndexConfig extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.IndexConfig';

  /// Required. The LogEntry field path to index.
  ///
  /// Note that some paths are automatically indexed, and other paths are not
  /// eligible for indexing. See [indexing documentation](
  /// https://cloud.google.com/logging/docs/view/advanced-queries#indexed-fields)
  /// for details.
  ///
  /// For example: `jsonPayload.request.status`
  final String fieldPath;

  /// Required. The type of data in this index.
  final IndexType type;

  /// Output only. The timestamp when the index was last modified.
  ///
  /// This is used to return the timestamp, and will be ignored if supplied
  /// during update.
  final Timestamp? createTime;

  IndexConfig({required this.fieldPath, required this.type, this.createTime})
    : super(fullyQualifiedName);

  factory IndexConfig.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return IndexConfig(
      fieldPath: switch (json['fieldPath']) {
        null => '',
        Object $1 => decodeString($1),
      },
      type: switch (json['type']) {
        null => IndexType.$default,
        Object $1 => IndexType.fromJson($1),
      },
      createTime: switch (json['createTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'fieldPath': fieldPath,
    'type': type.toJson(),
    if (createTime != null) 'createTime': createTime!.toJson(),
  };

  @override
  String toString() {
    final contents = ['fieldPath=$fieldPath', 'type=$type'].join(',');
    return 'IndexConfig($contents)';
  }
}

/// Describes a repository in which log entries are stored.
final class LogBucket extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.LogBucket';

  /// Output only. The resource name of the bucket.
  ///
  /// For example:
  ///
  ///   `projects/my-project/locations/global/buckets/my-bucket`
  ///
  /// For a list of supported locations, see [Supported
  /// Regions](https://cloud.google.com/logging/docs/region-support)
  ///
  /// For the location of `global` it is unspecified where log entries are
  /// actually stored.
  ///
  /// After a bucket has been created, the location cannot be changed.
  final String name;

  /// Describes this bucket.
  final String description;

  /// Output only. The creation timestamp of the bucket. This is not set for any
  /// of the default buckets.
  final Timestamp? createTime;

  /// Output only. The last update timestamp of the bucket.
  final Timestamp? updateTime;

  /// Logs will be retained by default for this amount of time, after which they
  /// will automatically be deleted. The minimum retention period is 1 day. If
  /// this value is set to zero at bucket creation time, the default time of 30
  /// days will be used.
  final int retentionDays;

  /// Whether the bucket is locked.
  ///
  /// The retention period on a locked bucket cannot be changed. Locked buckets
  /// may only be deleted if they are empty.
  final bool locked;

  /// Output only. The bucket lifecycle state.
  final LifecycleState lifecycleState;

  /// Whether log analytics is enabled for this bucket.
  ///
  /// Once enabled, log analytics features cannot be disabled.
  final bool analyticsEnabled;

  /// Log entry field paths that are denied access in this bucket.
  ///
  /// The following fields and their children are eligible: `textPayload`,
  /// `jsonPayload`, `protoPayload`, `httpRequest`, `labels`, `sourceLocation`.
  ///
  /// Restricting a repeated field will restrict all values. Adding a parent will
  /// block all child fields. (e.g. `foo.bar` will block `foo.bar.baz`)
  final List<String> restrictedFields;

  /// A list of indexed fields and related configuration data.
  final List<IndexConfig> indexConfigs;

  /// The CMEK settings of the log bucket. If present, new log entries written to
  /// this log bucket are encrypted using the CMEK key provided in this
  /// configuration. If a log bucket has CMEK settings, the CMEK settings cannot
  /// be disabled later by updating the log bucket. Changing the KMS key is
  /// allowed.
  final CmekSettings? cmekSettings;

  LogBucket({
    this.name = '',
    this.description = '',
    this.createTime,
    this.updateTime,
    this.retentionDays = 0,
    this.locked = false,
    this.lifecycleState = LifecycleState.$default,
    this.analyticsEnabled = false,
    this.restrictedFields = const [],
    this.indexConfigs = const [],
    this.cmekSettings,
  }) : super(fullyQualifiedName);

  factory LogBucket.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return LogBucket(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      description: switch (json['description']) {
        null => '',
        Object $1 => decodeString($1),
      },
      createTime: switch (json['createTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      updateTime: switch (json['updateTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      retentionDays: switch (json['retentionDays']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
      locked: switch (json['locked']) {
        null => false,
        Object $1 => decodeBool($1),
      },
      lifecycleState: switch (json['lifecycleState']) {
        null => LifecycleState.$default,
        Object $1 => LifecycleState.fromJson($1),
      },
      analyticsEnabled: switch (json['analyticsEnabled']) {
        null => false,
        Object $1 => decodeBool($1),
      },
      restrictedFields: switch (json['restrictedFields']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) decodeString(i)],
        _ => throw const FormatException('"restrictedFields" is not a list'),
      },
      indexConfigs: switch (json['indexConfigs']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) IndexConfig.fromJson(i)],
        _ => throw const FormatException('"indexConfigs" is not a list'),
      },
      cmekSettings: switch (json['cmekSettings']) {
        null => null,
        Object $1 => CmekSettings.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (name.isNotDefault) 'name': name,
    if (description.isNotDefault) 'description': description,
    if (createTime != null) 'createTime': createTime!.toJson(),
    if (updateTime != null) 'updateTime': updateTime!.toJson(),
    if (retentionDays.isNotDefault) 'retentionDays': retentionDays,
    if (locked.isNotDefault) 'locked': locked,
    if (lifecycleState.isNotDefault) 'lifecycleState': lifecycleState.toJson(),
    if (analyticsEnabled.isNotDefault) 'analyticsEnabled': analyticsEnabled,
    if (restrictedFields.isNotDefault) 'restrictedFields': restrictedFields,
    if (indexConfigs.isNotDefault) 'indexConfigs': encodeList(indexConfigs),
    if (cmekSettings != null) 'cmekSettings': cmekSettings!.toJson(),
  };

  @override
  String toString() {
    final contents = [
      'name=$name',
      'description=$description',
      'retentionDays=$retentionDays',
      'locked=$locked',
      'lifecycleState=$lifecycleState',
      'analyticsEnabled=$analyticsEnabled',
    ].join(',');
    return 'LogBucket($contents)';
  }
}

/// Describes a view over log entries in a bucket.
final class LogView extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.LogView';

  /// The resource name of the view.
  ///
  /// For example:
  ///
  ///   `projects/my-project/locations/global/buckets/my-bucket/views/my-view`
  final String name;

  /// Describes this view.
  final String description;

  /// Output only. The creation timestamp of the view.
  final Timestamp? createTime;

  /// Output only. The last update timestamp of the view.
  final Timestamp? updateTime;

  /// Filter that restricts which log entries in a bucket are visible in this
  /// view.
  ///
  /// Filters are restricted to be a logical AND of ==/!= of any of the
  /// following:
  ///
  ///   - originating project/folder/organization/billing account.
  ///   - resource type
  ///   - log id
  ///
  /// For example:
  ///
  ///   SOURCE("projects/myproject") AND resource.type = "gce_instance"
  ///                                AND LOG_ID("stdout")
  final String filter;

  LogView({
    this.name = '',
    this.description = '',
    this.createTime,
    this.updateTime,
    this.filter = '',
  }) : super(fullyQualifiedName);

  factory LogView.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return LogView(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      description: switch (json['description']) {
        null => '',
        Object $1 => decodeString($1),
      },
      createTime: switch (json['createTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      updateTime: switch (json['updateTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      filter: switch (json['filter']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (name.isNotDefault) 'name': name,
    if (description.isNotDefault) 'description': description,
    if (createTime != null) 'createTime': createTime!.toJson(),
    if (updateTime != null) 'updateTime': updateTime!.toJson(),
    if (filter.isNotDefault) 'filter': filter,
  };

  @override
  String toString() {
    final contents = [
      'name=$name',
      'description=$description',
      'filter=$filter',
    ].join(',');
    return 'LogView($contents)';
  }
}

/// Describes a sink used to export log entries to one of the following
/// destinations in any project: a Cloud Storage bucket, a BigQuery dataset, a
/// Pub/Sub topic or a Cloud Logging log bucket. A logs filter controls which log
/// entries are exported. The sink must be created within a project,
/// organization, billing account, or folder.
final class LogSink extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.LogSink';

  /// Required. The client-assigned sink identifier, unique within the project.
  ///
  /// For example: `"my-syslog-errors-to-pubsub"`. Sink identifiers are limited
  /// to 100 characters and can include only the following characters: upper and
  /// lower-case alphanumeric characters, underscores, hyphens, and periods.
  /// First character has to be alphanumeric.
  final String name;

  /// Required. The export destination:
  ///
  ///     "storage.googleapis.com/[GCS_BUCKET]"
  ///     "bigquery.googleapis.com/projects/[PROJECT_ID]/datasets/[DATASET]"
  ///     "pubsub.googleapis.com/projects/[PROJECT_ID]/topics/[TOPIC_ID]"
  ///
  /// The sink's `writer_identity`, set when the sink is created, must have
  /// permission to write to the destination or else the log entries are not
  /// exported. For more information, see
  /// [Exporting Logs with
  /// Sinks](https://cloud.google.com/logging/docs/api/tasks/exporting-logs).
  final String destination;

  /// Optional. An [advanced logs
  /// filter](https://cloud.google.com/logging/docs/view/advanced-queries). The
  /// only exported log entries are those that are in the resource owning the
  /// sink and that match the filter.
  ///
  /// For example:
  ///
  ///   `logName="projects/[PROJECT_ID]/logs/[LOG_ID]" AND severity>=ERROR`
  final String filter;

  /// Optional. A description of this sink.
  ///
  /// The maximum length of the description is 8000 characters.
  final String description;

  /// Optional. If set to true, then this sink is disabled and it does not export
  /// any log entries.
  final bool disabled;

  /// Optional. Log entries that match any of these exclusion filters will not be
  /// exported.
  ///
  /// If a log entry is matched by both `filter` and one of `exclusion_filters`
  /// it will not be exported.
  final List<LogExclusion> exclusions;

  /// Deprecated. This field is unused.
  final LogSink_VersionFormat outputVersionFormat;

  /// Output only. An IAM identity&mdash;a service account or group&mdash;under
  /// which Cloud Logging writes the exported log entries to the sink's
  /// destination. This field is either set by specifying
  /// `custom_writer_identity` or set automatically by
  /// `sinks.create` and
  /// `sinks.update` based on the
  /// value of `unique_writer_identity` in those methods.
  ///
  /// Until you grant this identity write-access to the destination, log entry
  /// exports from this sink will fail. For more information, see [Granting
  /// Access for a
  /// Resource](https://cloud.google.com/iam/docs/granting-roles-to-service-accounts#granting_access_to_a_service_account_for_a_resource).
  /// Consult the destination service's documentation to determine the
  /// appropriate IAM roles to assign to the identity.
  ///
  /// Sinks that have a destination that is a log bucket in the same project as
  /// the sink cannot have a writer_identity and no additional permissions are
  /// required.
  final String writerIdentity;

  /// Optional. This field applies only to sinks owned by organizations and
  /// folders. If the field is false, the default, only the logs owned by the
  /// sink's parent resource are available for export. If the field is true, then
  /// log entries from all the projects, folders, and billing accounts contained
  /// in the sink's parent resource are also available for export. Whether a
  /// particular log entry from the children is exported depends on the sink's
  /// filter expression.
  ///
  /// For example, if this field is true, then the filter
  /// `resource.type=gce_instance` would export all Compute Engine VM instance
  /// log entries from all projects in the sink's parent.
  ///
  /// To only export entries from certain child projects, filter on the project
  /// part of the log name:
  ///
  ///   logName:("projects/test-project1/" OR "projects/test-project2/") AND
  ///   resource.type=gce_instance
  final bool includeChildren;

  /// Optional. Options that affect sinks exporting data to BigQuery.
  final BigQueryOptions? bigqueryOptions;

  /// Output only. The creation timestamp of the sink.
  ///
  /// This field may not be present for older sinks.
  final Timestamp? createTime;

  /// Output only. The last update timestamp of the sink.
  ///
  /// This field may not be present for older sinks.
  final Timestamp? updateTime;

  LogSink({
    required this.name,
    required this.destination,
    this.filter = '',
    this.description = '',
    this.disabled = false,
    this.exclusions = const [],
    this.outputVersionFormat = LogSink_VersionFormat.$default,
    this.writerIdentity = '',
    this.includeChildren = false,
    this.bigqueryOptions,
    this.createTime,
    this.updateTime,
  }) : super(fullyQualifiedName);

  factory LogSink.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return LogSink(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      destination: switch (json['destination']) {
        null => '',
        Object $1 => decodeString($1),
      },
      filter: switch (json['filter']) {
        null => '',
        Object $1 => decodeString($1),
      },
      description: switch (json['description']) {
        null => '',
        Object $1 => decodeString($1),
      },
      disabled: switch (json['disabled']) {
        null => false,
        Object $1 => decodeBool($1),
      },
      exclusions: switch (json['exclusions']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) LogExclusion.fromJson(i)],
        _ => throw const FormatException('"exclusions" is not a list'),
      },
      outputVersionFormat: switch (json['outputVersionFormat']) {
        null => LogSink_VersionFormat.$default,
        Object $1 => LogSink_VersionFormat.fromJson($1),
      },
      writerIdentity: switch (json['writerIdentity']) {
        null => '',
        Object $1 => decodeString($1),
      },
      includeChildren: switch (json['includeChildren']) {
        null => false,
        Object $1 => decodeBool($1),
      },
      bigqueryOptions: switch (json['bigqueryOptions']) {
        null => null,
        Object $1 => BigQueryOptions.fromJson($1),
      },
      createTime: switch (json['createTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      updateTime: switch (json['updateTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'name': name,
    'destination': destination,
    if (filter.isNotDefault) 'filter': filter,
    if (description.isNotDefault) 'description': description,
    if (disabled.isNotDefault) 'disabled': disabled,
    if (exclusions.isNotDefault) 'exclusions': encodeList(exclusions),
    if (outputVersionFormat.isNotDefault)
      'outputVersionFormat': outputVersionFormat.toJson(),
    if (writerIdentity.isNotDefault) 'writerIdentity': writerIdentity,
    if (includeChildren.isNotDefault) 'includeChildren': includeChildren,
    if (bigqueryOptions != null) 'bigqueryOptions': bigqueryOptions!.toJson(),
    if (createTime != null) 'createTime': createTime!.toJson(),
    if (updateTime != null) 'updateTime': updateTime!.toJson(),
  };

  @override
  String toString() {
    final contents = [
      'name=$name',
      'destination=$destination',
      'filter=$filter',
      'description=$description',
      'disabled=$disabled',
      'outputVersionFormat=$outputVersionFormat',
      'writerIdentity=$writerIdentity',
      'includeChildren=$includeChildren',
    ].join(',');
    return 'LogSink($contents)';
  }
}

/// Deprecated. This is unused.
final class LogSink_VersionFormat extends ProtoEnum {
  /// An unspecified format version that will default to V2.
  static const versionFormatUnspecified = LogSink_VersionFormat(
    'VERSION_FORMAT_UNSPECIFIED',
  );

  /// `LogEntry` version 2 format.
  static const v2 = LogSink_VersionFormat('V2');

  /// `LogEntry` version 1 format.
  static const v1 = LogSink_VersionFormat('V1');

  /// The default value for [LogSink_VersionFormat].
  static const $default = versionFormatUnspecified;

  const LogSink_VersionFormat(super.value);

  factory LogSink_VersionFormat.fromJson(Object? json) =>
      LogSink_VersionFormat(json as String);

  bool get isNotDefault => this != $default;

  @override
  String toString() => 'VersionFormat.$value';
}

/// Describes a BigQuery dataset that was created by a link.
final class BigQueryDataset extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.BigQueryDataset';

  /// Output only. The full resource name of the BigQuery dataset. The DATASET_ID
  /// will match the ID of the link, so the link must match the naming
  /// restrictions of BigQuery datasets (alphanumeric characters and underscores
  /// only).
  ///
  /// The dataset will have a resource path of
  ///   "bigquery.googleapis.com/projects/[PROJECT_ID]/datasets/[DATASET_ID]"
  final String datasetId;

  BigQueryDataset({this.datasetId = ''}) : super(fullyQualifiedName);

  factory BigQueryDataset.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return BigQueryDataset(
      datasetId: switch (json['datasetId']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {if (datasetId.isNotDefault) 'datasetId': datasetId};

  @override
  String toString() {
    final contents = ['datasetId=$datasetId'].join(',');
    return 'BigQueryDataset($contents)';
  }
}

/// Describes a link connected to an analytics enabled bucket.
final class Link extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.Link';

  /// The resource name of the link. The name can have up to 100 characters.
  /// A valid link id (at the end of the link name) must only have alphanumeric
  /// characters and underscores within it.
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/[LINK_ID]"
  ///     "organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/[LINK_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/[LINK_ID]"
  ///     "folders/[FOLDER_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/[LINK_ID]"
  ///
  /// For example:
  ///
  ///   `projects/my-project/locations/global/buckets/my-bucket/links/my_link
  final String name;

  /// Describes this link.
  ///
  /// The maximum length of the description is 8000 characters.
  final String description;

  /// Output only. The creation timestamp of the link.
  final Timestamp? createTime;

  /// Output only. The resource lifecycle state.
  final LifecycleState lifecycleState;

  /// The information of a BigQuery Dataset. When a link is created, a BigQuery
  /// dataset is created along with it, in the same project as the LogBucket it's
  /// linked to. This dataset will also have BigQuery Views corresponding to the
  /// LogViews in the bucket.
  final BigQueryDataset? bigqueryDataset;

  Link({
    this.name = '',
    this.description = '',
    this.createTime,
    this.lifecycleState = LifecycleState.$default,
    this.bigqueryDataset,
  }) : super(fullyQualifiedName);

  factory Link.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return Link(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      description: switch (json['description']) {
        null => '',
        Object $1 => decodeString($1),
      },
      createTime: switch (json['createTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      lifecycleState: switch (json['lifecycleState']) {
        null => LifecycleState.$default,
        Object $1 => LifecycleState.fromJson($1),
      },
      bigqueryDataset: switch (json['bigqueryDataset']) {
        null => null,
        Object $1 => BigQueryDataset.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (name.isNotDefault) 'name': name,
    if (description.isNotDefault) 'description': description,
    if (createTime != null) 'createTime': createTime!.toJson(),
    if (lifecycleState.isNotDefault) 'lifecycleState': lifecycleState.toJson(),
    if (bigqueryDataset != null) 'bigqueryDataset': bigqueryDataset!.toJson(),
  };

  @override
  String toString() {
    final contents = [
      'name=$name',
      'description=$description',
      'lifecycleState=$lifecycleState',
    ].join(',');
    return 'Link($contents)';
  }
}

/// Options that change functionality of a sink exporting data to BigQuery.
final class BigQueryOptions extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.BigQueryOptions';

  /// Optional. Whether to use [BigQuery's partition
  /// tables](https://cloud.google.com/bigquery/docs/partitioned-tables). By
  /// default, Cloud Logging creates dated tables based on the log entries'
  /// timestamps, e.g. syslog_20170523. With partitioned tables the date suffix
  /// is no longer present and [special query
  /// syntax](https://cloud.google.com/bigquery/docs/querying-partitioned-tables)
  /// has to be used instead. In both cases, tables are sharded based on UTC
  /// timezone.
  final bool usePartitionedTables;

  /// Output only. True if new timestamp column based partitioning is in use,
  /// false if legacy ingestion-time partitioning is in use.
  ///
  /// All new sinks will have this field set true and will use timestamp column
  /// based partitioning. If use_partitioned_tables is false, this value has no
  /// meaning and will be false. Legacy sinks using partitioned tables will have
  /// this field set to false.
  final bool usesTimestampColumnPartitioning;

  BigQueryOptions({
    this.usePartitionedTables = false,
    this.usesTimestampColumnPartitioning = false,
  }) : super(fullyQualifiedName);

  factory BigQueryOptions.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return BigQueryOptions(
      usePartitionedTables: switch (json['usePartitionedTables']) {
        null => false,
        Object $1 => decodeBool($1),
      },
      usesTimestampColumnPartitioning:
          switch (json['usesTimestampColumnPartitioning']) {
            null => false,
            Object $1 => decodeBool($1),
          },
    );
  }

  @override
  Object toJson() => {
    if (usePartitionedTables.isNotDefault)
      'usePartitionedTables': usePartitionedTables,
    if (usesTimestampColumnPartitioning.isNotDefault)
      'usesTimestampColumnPartitioning': usesTimestampColumnPartitioning,
  };

  @override
  String toString() {
    final contents = [
      'usePartitionedTables=$usePartitionedTables',
      'usesTimestampColumnPartitioning=$usesTimestampColumnPartitioning',
    ].join(',');
    return 'BigQueryOptions($contents)';
  }
}

/// The parameters to `ListBuckets`.
final class ListBucketsRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListBucketsRequest';

  /// Required. The parent resource whose buckets are to be listed:
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION_ID]"
  ///     "organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]"
  ///     "folders/[FOLDER_ID]/locations/[LOCATION_ID]"
  ///
  /// Note: The locations portion of the resource must be specified, but
  /// supplying the character `-` in place of [LOCATION_ID] will return all
  /// buckets.
  final String parent;

  /// Optional. If present, then retrieve the next batch of results from the
  /// preceding call to this method. `pageToken` must be the value of
  /// `nextPageToken` from the previous response. The values of other method
  /// parameters should be identical to those in the previous call.
  final String pageToken;

  /// Optional. The maximum number of results to return from this request.
  /// Non-positive values are ignored. The presence of `nextPageToken` in the
  /// response indicates that more results might be available.
  final int pageSize;

  ListBucketsRequest({
    required this.parent,
    this.pageToken = '',
    this.pageSize = 0,
  }) : super(fullyQualifiedName);

  factory ListBucketsRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListBucketsRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageToken: switch (json['pageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageSize: switch (json['pageSize']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (pageToken.isNotDefault) 'pageToken': pageToken,
    if (pageSize.isNotDefault) 'pageSize': pageSize,
  };

  @override
  String toString() {
    final contents = [
      'parent=$parent',
      'pageToken=$pageToken',
      'pageSize=$pageSize',
    ].join(',');
    return 'ListBucketsRequest($contents)';
  }
}

/// The response from ListBuckets.
final class ListBucketsResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListBucketsResponse';

  /// A list of buckets.
  final List<LogBucket> buckets;

  /// If there might be more results than appear in this response, then
  /// `nextPageToken` is included. To get the next set of results, call the same
  /// method again using the value of `nextPageToken` as `pageToken`.
  final String nextPageToken;

  ListBucketsResponse({this.buckets = const [], this.nextPageToken = ''})
    : super(fullyQualifiedName);

  factory ListBucketsResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListBucketsResponse(
      buckets: switch (json['buckets']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) LogBucket.fromJson(i)],
        _ => throw const FormatException('"buckets" is not a list'),
      },
      nextPageToken: switch (json['nextPageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (buckets.isNotDefault) 'buckets': encodeList(buckets),
    if (nextPageToken.isNotDefault) 'nextPageToken': nextPageToken,
  };

  @override
  String toString() {
    final contents = ['nextPageToken=$nextPageToken'].join(',');
    return 'ListBucketsResponse($contents)';
  }
}

/// The parameters to `CreateBucket`.
final class CreateBucketRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.CreateBucketRequest';

  /// Required. The resource in which to create the log bucket:
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/locations/global"`
  final String parent;

  /// Required. A client-assigned identifier such as `"my-bucket"`. Identifiers
  /// are limited to 100 characters and can include only letters, digits,
  /// underscores, hyphens, and periods.
  final String bucketId;

  /// Required. The new bucket. The region specified in the new bucket must be
  /// compliant with any Location Restriction Org Policy. The name field in the
  /// bucket is ignored.
  final LogBucket? bucket;

  CreateBucketRequest({
    required this.parent,
    required this.bucketId,
    required this.bucket,
  }) : super(fullyQualifiedName);

  factory CreateBucketRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return CreateBucketRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      bucketId: switch (json['bucketId']) {
        null => '',
        Object $1 => decodeString($1),
      },
      bucket: switch (json['bucket']) {
        null => null,
        Object $1 => LogBucket.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    'bucketId': bucketId,
    if (bucket != null) 'bucket': bucket!.toJson(),
  };

  @override
  String toString() {
    final contents = ['parent=$parent', 'bucketId=$bucketId'].join(',');
    return 'CreateBucketRequest($contents)';
  }
}

/// The parameters to `UpdateBucket`.
final class UpdateBucketRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.UpdateBucketRequest';

  /// Required. The full resource name of the bucket to update.
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "folders/[FOLDER_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/locations/global/buckets/my-bucket"`
  final String name;

  /// Required. The updated bucket.
  final LogBucket? bucket;

  /// Required. Field mask that specifies the fields in `bucket` that need an
  /// update. A bucket field will be overwritten if, and only if, it is in the
  /// update mask. `name` and output only fields cannot be updated.
  ///
  /// For a detailed `FieldMask` definition, see:
  /// https://developers.google.com/protocol-buffers/docs/reference/google.protobuf#google.protobuf.FieldMask
  ///
  /// For example: `updateMask=retention_days`
  final FieldMask? updateMask;

  UpdateBucketRequest({
    required this.name,
    required this.bucket,
    required this.updateMask,
  }) : super(fullyQualifiedName);

  factory UpdateBucketRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return UpdateBucketRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      bucket: switch (json['bucket']) {
        null => null,
        Object $1 => LogBucket.fromJson($1),
      },
      updateMask: switch (json['updateMask']) {
        null => null,
        Object $1 => FieldMask.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'name': name,
    if (bucket != null) 'bucket': bucket!.toJson(),
    if (updateMask != null) 'updateMask': updateMask!.toJson(),
  };

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'UpdateBucketRequest($contents)';
  }
}

/// The parameters to `GetBucket`.
final class GetBucketRequest extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.GetBucketRequest';

  /// Required. The resource name of the bucket:
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "folders/[FOLDER_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/locations/global/buckets/my-bucket"`
  final String name;

  GetBucketRequest({required this.name}) : super(fullyQualifiedName);

  factory GetBucketRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return GetBucketRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'GetBucketRequest($contents)';
  }
}

/// The parameters to `DeleteBucket`.
final class DeleteBucketRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.DeleteBucketRequest';

  /// Required. The full resource name of the bucket to delete.
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "folders/[FOLDER_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/locations/global/buckets/my-bucket"`
  final String name;

  DeleteBucketRequest({required this.name}) : super(fullyQualifiedName);

  factory DeleteBucketRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return DeleteBucketRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'DeleteBucketRequest($contents)';
  }
}

/// The parameters to `UndeleteBucket`.
final class UndeleteBucketRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.UndeleteBucketRequest';

  /// Required. The full resource name of the bucket to undelete.
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "folders/[FOLDER_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/locations/global/buckets/my-bucket"`
  final String name;

  UndeleteBucketRequest({required this.name}) : super(fullyQualifiedName);

  factory UndeleteBucketRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return UndeleteBucketRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'UndeleteBucketRequest($contents)';
  }
}

/// The parameters to `ListViews`.
final class ListViewsRequest extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.ListViewsRequest';

  /// Required. The bucket whose views are to be listed:
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  final String parent;

  /// Optional. If present, then retrieve the next batch of results from the
  /// preceding call to this method. `pageToken` must be the value of
  /// `nextPageToken` from the previous response. The values of other method
  /// parameters should be identical to those in the previous call.
  final String pageToken;

  /// Optional. The maximum number of results to return from this request.
  ///
  /// Non-positive values are ignored. The presence of `nextPageToken` in the
  /// response indicates that more results might be available.
  final int pageSize;

  ListViewsRequest({
    required this.parent,
    this.pageToken = '',
    this.pageSize = 0,
  }) : super(fullyQualifiedName);

  factory ListViewsRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListViewsRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageToken: switch (json['pageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageSize: switch (json['pageSize']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (pageToken.isNotDefault) 'pageToken': pageToken,
    if (pageSize.isNotDefault) 'pageSize': pageSize,
  };

  @override
  String toString() {
    final contents = [
      'parent=$parent',
      'pageToken=$pageToken',
      'pageSize=$pageSize',
    ].join(',');
    return 'ListViewsRequest($contents)';
  }
}

/// The response from ListViews.
final class ListViewsResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListViewsResponse';

  /// A list of views.
  final List<LogView> views;

  /// If there might be more results than appear in this response, then
  /// `nextPageToken` is included. To get the next set of results, call the same
  /// method again using the value of `nextPageToken` as `pageToken`.
  final String nextPageToken;

  ListViewsResponse({this.views = const [], this.nextPageToken = ''})
    : super(fullyQualifiedName);

  factory ListViewsResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListViewsResponse(
      views: switch (json['views']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) LogView.fromJson(i)],
        _ => throw const FormatException('"views" is not a list'),
      },
      nextPageToken: switch (json['nextPageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (views.isNotDefault) 'views': encodeList(views),
    if (nextPageToken.isNotDefault) 'nextPageToken': nextPageToken,
  };

  @override
  String toString() {
    final contents = ['nextPageToken=$nextPageToken'].join(',');
    return 'ListViewsResponse($contents)';
  }
}

/// The parameters to `CreateView`.
final class CreateViewRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.CreateViewRequest';

  /// Required. The bucket in which to create the view
  ///
  ///     `"projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"`
  ///
  /// For example:
  ///
  ///   `"projects/my-project/locations/global/buckets/my-bucket"`
  final String parent;

  /// Required. A client-assigned identifier such as `"my-view"`. Identifiers are
  /// limited to 100 characters and can include only letters, digits,
  /// underscores, hyphens, and periods.
  final String viewId;

  /// Required. The new view.
  final LogView? view;

  CreateViewRequest({
    required this.parent,
    required this.viewId,
    required this.view,
  }) : super(fullyQualifiedName);

  factory CreateViewRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return CreateViewRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      viewId: switch (json['viewId']) {
        null => '',
        Object $1 => decodeString($1),
      },
      view: switch (json['view']) {
        null => null,
        Object $1 => LogView.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    'viewId': viewId,
    if (view != null) 'view': view!.toJson(),
  };

  @override
  String toString() {
    final contents = ['parent=$parent', 'viewId=$viewId'].join(',');
    return 'CreateViewRequest($contents)';
  }
}

/// The parameters to `UpdateView`.
final class UpdateViewRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.UpdateViewRequest';

  /// Required. The full resource name of the view to update
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/locations/global/buckets/my-bucket/views/my-view"`
  final String name;

  /// Required. The updated view.
  final LogView? view;

  /// Optional. Field mask that specifies the fields in `view` that need
  /// an update. A field will be overwritten if, and only if, it is
  /// in the update mask. `name` and output only fields cannot be updated.
  ///
  /// For a detailed `FieldMask` definition, see
  /// https://developers.google.com/protocol-buffers/docs/reference/google.protobuf#google.protobuf.FieldMask
  ///
  /// For example: `updateMask=filter`
  final FieldMask? updateMask;

  UpdateViewRequest({required this.name, required this.view, this.updateMask})
    : super(fullyQualifiedName);

  factory UpdateViewRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return UpdateViewRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      view: switch (json['view']) {
        null => null,
        Object $1 => LogView.fromJson($1),
      },
      updateMask: switch (json['updateMask']) {
        null => null,
        Object $1 => FieldMask.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'name': name,
    if (view != null) 'view': view!.toJson(),
    if (updateMask != null) 'updateMask': updateMask!.toJson(),
  };

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'UpdateViewRequest($contents)';
  }
}

/// The parameters to `GetView`.
final class GetViewRequest extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.GetViewRequest';

  /// Required. The resource name of the policy:
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/locations/global/buckets/my-bucket/views/my-view"`
  final String name;

  GetViewRequest({required this.name}) : super(fullyQualifiedName);

  factory GetViewRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return GetViewRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'GetViewRequest($contents)';
  }
}

/// The parameters to `DeleteView`.
final class DeleteViewRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.DeleteViewRequest';

  /// Required. The full resource name of the view to delete:
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/views/[VIEW_ID]"
  ///
  /// For example:
  ///
  ///    `"projects/my-project/locations/global/buckets/my-bucket/views/my-view"`
  final String name;

  DeleteViewRequest({required this.name}) : super(fullyQualifiedName);

  factory DeleteViewRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return DeleteViewRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'DeleteViewRequest($contents)';
  }
}

/// The parameters to `ListSinks`.
final class ListSinksRequest extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.ListSinksRequest';

  /// Required. The parent resource whose sinks are to be listed:
  ///
  ///     "projects/[PROJECT_ID]"
  ///     "organizations/[ORGANIZATION_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]"
  ///     "folders/[FOLDER_ID]"
  final String parent;

  /// Optional. If present, then retrieve the next batch of results from the
  /// preceding call to this method. `pageToken` must be the value of
  /// `nextPageToken` from the previous response. The values of other method
  /// parameters should be identical to those in the previous call.
  final String pageToken;

  /// Optional. The maximum number of results to return from this request.
  /// Non-positive values are ignored. The presence of `nextPageToken` in the
  /// response indicates that more results might be available.
  final int pageSize;

  ListSinksRequest({
    required this.parent,
    this.pageToken = '',
    this.pageSize = 0,
  }) : super(fullyQualifiedName);

  factory ListSinksRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListSinksRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageToken: switch (json['pageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageSize: switch (json['pageSize']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (pageToken.isNotDefault) 'pageToken': pageToken,
    if (pageSize.isNotDefault) 'pageSize': pageSize,
  };

  @override
  String toString() {
    final contents = [
      'parent=$parent',
      'pageToken=$pageToken',
      'pageSize=$pageSize',
    ].join(',');
    return 'ListSinksRequest($contents)';
  }
}

/// Result returned from `ListSinks`.
final class ListSinksResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListSinksResponse';

  /// A list of sinks.
  final List<LogSink> sinks;

  /// If there might be more results than appear in this response, then
  /// `nextPageToken` is included. To get the next set of results, call the same
  /// method again using the value of `nextPageToken` as `pageToken`.
  final String nextPageToken;

  ListSinksResponse({this.sinks = const [], this.nextPageToken = ''})
    : super(fullyQualifiedName);

  factory ListSinksResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListSinksResponse(
      sinks: switch (json['sinks']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) LogSink.fromJson(i)],
        _ => throw const FormatException('"sinks" is not a list'),
      },
      nextPageToken: switch (json['nextPageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (sinks.isNotDefault) 'sinks': encodeList(sinks),
    if (nextPageToken.isNotDefault) 'nextPageToken': nextPageToken,
  };

  @override
  String toString() {
    final contents = ['nextPageToken=$nextPageToken'].join(',');
    return 'ListSinksResponse($contents)';
  }
}

/// The parameters to `GetSink`.
final class GetSinkRequest extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.GetSinkRequest';

  /// Required. The resource name of the sink:
  ///
  ///     "projects/[PROJECT_ID]/sinks/[SINK_ID]"
  ///     "organizations/[ORGANIZATION_ID]/sinks/[SINK_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/sinks/[SINK_ID]"
  ///     "folders/[FOLDER_ID]/sinks/[SINK_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/sinks/my-sink"`
  final String sinkName;

  GetSinkRequest({required this.sinkName}) : super(fullyQualifiedName);

  factory GetSinkRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return GetSinkRequest(
      sinkName: switch (json['sinkName']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'sinkName': sinkName};

  @override
  String toString() {
    final contents = ['sinkName=$sinkName'].join(',');
    return 'GetSinkRequest($contents)';
  }
}

/// The parameters to `CreateSink`.
final class CreateSinkRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.CreateSinkRequest';

  /// Required. The resource in which to create the sink:
  ///
  ///     "projects/[PROJECT_ID]"
  ///     "organizations/[ORGANIZATION_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]"
  ///     "folders/[FOLDER_ID]"
  ///
  /// For examples:
  ///
  ///   `"projects/my-project"`
  ///   `"organizations/123456789"`
  final String parent;

  /// Required. The new sink, whose `name` parameter is a sink identifier that
  /// is not already in use.
  final LogSink? sink;

  /// Optional. Determines the kind of IAM identity returned as `writer_identity`
  /// in the new sink. If this value is omitted or set to false, and if the
  /// sink's parent is a project, then the value returned as `writer_identity` is
  /// the same group or service account used by Cloud Logging before the addition
  /// of writer identities to this API. The sink's destination must be in the
  /// same project as the sink itself.
  ///
  /// If this field is set to true, or if the sink is owned by a non-project
  /// resource such as an organization, then the value of `writer_identity` will
  /// be a unique service account used only for exports from the new sink. For
  /// more information, see `writer_identity` in
  /// `LogSink`.
  final bool uniqueWriterIdentity;

  CreateSinkRequest({
    required this.parent,
    required this.sink,
    this.uniqueWriterIdentity = false,
  }) : super(fullyQualifiedName);

  factory CreateSinkRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return CreateSinkRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      sink: switch (json['sink']) {
        null => null,
        Object $1 => LogSink.fromJson($1),
      },
      uniqueWriterIdentity: switch (json['uniqueWriterIdentity']) {
        null => false,
        Object $1 => decodeBool($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (sink != null) 'sink': sink!.toJson(),
    if (uniqueWriterIdentity.isNotDefault)
      'uniqueWriterIdentity': uniqueWriterIdentity,
  };

  @override
  String toString() {
    final contents = [
      'parent=$parent',
      'uniqueWriterIdentity=$uniqueWriterIdentity',
    ].join(',');
    return 'CreateSinkRequest($contents)';
  }
}

/// The parameters to `UpdateSink`.
final class UpdateSinkRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.UpdateSinkRequest';

  /// Required. The full resource name of the sink to update, including the
  /// parent resource and the sink identifier:
  ///
  ///     "projects/[PROJECT_ID]/sinks/[SINK_ID]"
  ///     "organizations/[ORGANIZATION_ID]/sinks/[SINK_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/sinks/[SINK_ID]"
  ///     "folders/[FOLDER_ID]/sinks/[SINK_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/sinks/my-sink"`
  final String sinkName;

  /// Required. The updated sink, whose name is the same identifier that appears
  /// as part of `sink_name`.
  final LogSink? sink;

  /// Optional. See `sinks.create`
  /// for a description of this field. When updating a sink, the effect of this
  /// field on the value of `writer_identity` in the updated sink depends on both
  /// the old and new values of this field:
  ///
  /// +   If the old and new values of this field are both false or both true,
  ///     then there is no change to the sink's `writer_identity`.
  /// +   If the old value is false and the new value is true, then
  ///     `writer_identity` is changed to a unique service account.
  /// +   It is an error if the old value is true and the new value is
  ///     set to false or defaulted to false.
  final bool uniqueWriterIdentity;

  /// Optional. Field mask that specifies the fields in `sink` that need
  /// an update. A sink field will be overwritten if, and only if, it is
  /// in the update mask. `name` and output only fields cannot be updated.
  ///
  /// An empty `updateMask` is temporarily treated as using the following mask
  /// for backwards compatibility purposes:
  ///
  ///   `destination,filter,includeChildren`
  ///
  /// At some point in the future, behavior will be removed and specifying an
  /// empty `updateMask` will be an error.
  ///
  /// For a detailed `FieldMask` definition, see
  /// https://developers.google.com/protocol-buffers/docs/reference/google.protobuf#google.protobuf.FieldMask
  ///
  /// For example: `updateMask=filter`
  final FieldMask? updateMask;

  UpdateSinkRequest({
    required this.sinkName,
    required this.sink,
    this.uniqueWriterIdentity = false,
    this.updateMask,
  }) : super(fullyQualifiedName);

  factory UpdateSinkRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return UpdateSinkRequest(
      sinkName: switch (json['sinkName']) {
        null => '',
        Object $1 => decodeString($1),
      },
      sink: switch (json['sink']) {
        null => null,
        Object $1 => LogSink.fromJson($1),
      },
      uniqueWriterIdentity: switch (json['uniqueWriterIdentity']) {
        null => false,
        Object $1 => decodeBool($1),
      },
      updateMask: switch (json['updateMask']) {
        null => null,
        Object $1 => FieldMask.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'sinkName': sinkName,
    if (sink != null) 'sink': sink!.toJson(),
    if (uniqueWriterIdentity.isNotDefault)
      'uniqueWriterIdentity': uniqueWriterIdentity,
    if (updateMask != null) 'updateMask': updateMask!.toJson(),
  };

  @override
  String toString() {
    final contents = [
      'sinkName=$sinkName',
      'uniqueWriterIdentity=$uniqueWriterIdentity',
    ].join(',');
    return 'UpdateSinkRequest($contents)';
  }
}

/// The parameters to `DeleteSink`.
final class DeleteSinkRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.DeleteSinkRequest';

  /// Required. The full resource name of the sink to delete, including the
  /// parent resource and the sink identifier:
  ///
  ///     "projects/[PROJECT_ID]/sinks/[SINK_ID]"
  ///     "organizations/[ORGANIZATION_ID]/sinks/[SINK_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/sinks/[SINK_ID]"
  ///     "folders/[FOLDER_ID]/sinks/[SINK_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/sinks/my-sink"`
  final String sinkName;

  DeleteSinkRequest({required this.sinkName}) : super(fullyQualifiedName);

  factory DeleteSinkRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return DeleteSinkRequest(
      sinkName: switch (json['sinkName']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'sinkName': sinkName};

  @override
  String toString() {
    final contents = ['sinkName=$sinkName'].join(',');
    return 'DeleteSinkRequest($contents)';
  }
}

/// The parameters to CreateLink.
final class CreateLinkRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.CreateLinkRequest';

  /// Required. The full resource name of the bucket to create a link for.
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  ///     "folders/[FOLDER_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]"
  final String parent;

  /// Required. The new link.
  final Link? link;

  /// Required. The ID to use for the link. The link_id can have up to 100
  /// characters. A valid link_id must only have alphanumeric characters and
  /// underscores within it.
  final String linkId;

  CreateLinkRequest({
    required this.parent,
    required this.link,
    required this.linkId,
  }) : super(fullyQualifiedName);

  factory CreateLinkRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return CreateLinkRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      link: switch (json['link']) {
        null => null,
        Object $1 => Link.fromJson($1),
      },
      linkId: switch (json['linkId']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (link != null) 'link': link!.toJson(),
    'linkId': linkId,
  };

  @override
  String toString() {
    final contents = ['parent=$parent', 'linkId=$linkId'].join(',');
    return 'CreateLinkRequest($contents)';
  }
}

/// The parameters to DeleteLink.
final class DeleteLinkRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.DeleteLinkRequest';

  /// Required. The full resource name of the link to delete.
  ///
  ///  "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/[LINK_ID]"
  ///   "organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/[LINK_ID]"
  ///   "billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/[LINK_ID]"
  ///   "folders/[FOLDER_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/[LINK_ID]"
  final String name;

  DeleteLinkRequest({required this.name}) : super(fullyQualifiedName);

  factory DeleteLinkRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return DeleteLinkRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'DeleteLinkRequest($contents)';
  }
}

/// The parameters to ListLinks.
final class ListLinksRequest extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.ListLinksRequest';

  /// Required. The parent resource whose links are to be listed:
  ///
  ///   "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/"
  ///   "organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/"
  ///   "billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/"
  ///   "folders/[FOLDER_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/
  final String parent;

  /// Optional. If present, then retrieve the next batch of results from the
  /// preceding call to this method. `pageToken` must be the value of
  /// `nextPageToken` from the previous response.
  final String pageToken;

  /// Optional. The maximum number of results to return from this request.
  final int pageSize;

  ListLinksRequest({
    required this.parent,
    this.pageToken = '',
    this.pageSize = 0,
  }) : super(fullyQualifiedName);

  factory ListLinksRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListLinksRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageToken: switch (json['pageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageSize: switch (json['pageSize']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (pageToken.isNotDefault) 'pageToken': pageToken,
    if (pageSize.isNotDefault) 'pageSize': pageSize,
  };

  @override
  String toString() {
    final contents = [
      'parent=$parent',
      'pageToken=$pageToken',
      'pageSize=$pageSize',
    ].join(',');
    return 'ListLinksRequest($contents)';
  }
}

/// The response from ListLinks.
final class ListLinksResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListLinksResponse';

  /// A list of links.
  final List<Link> links;

  /// If there might be more results than those appearing in this response, then
  /// `nextPageToken` is included. To get the next set of results, call the same
  /// method again using the value of `nextPageToken` as `pageToken`.
  final String nextPageToken;

  ListLinksResponse({this.links = const [], this.nextPageToken = ''})
    : super(fullyQualifiedName);

  factory ListLinksResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListLinksResponse(
      links: switch (json['links']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) Link.fromJson(i)],
        _ => throw const FormatException('"links" is not a list'),
      },
      nextPageToken: switch (json['nextPageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (links.isNotDefault) 'links': encodeList(links),
    if (nextPageToken.isNotDefault) 'nextPageToken': nextPageToken,
  };

  @override
  String toString() {
    final contents = ['nextPageToken=$nextPageToken'].join(',');
    return 'ListLinksResponse($contents)';
  }
}

/// The parameters to GetLink.
final class GetLinkRequest extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.GetLinkRequest';

  /// Required. The resource name of the link:
  ///
  ///   "projects/[PROJECT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/[LINK_ID]"
  ///   "organizations/[ORGANIZATION_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/[LINK_ID]"
  ///   "billingAccounts/[BILLING_ACCOUNT_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/[LINK_ID]"
  ///   "folders/[FOLDER_ID]/locations/[LOCATION_ID]/buckets/[BUCKET_ID]/links/[LINK_ID]
  final String name;

  GetLinkRequest({required this.name}) : super(fullyQualifiedName);

  factory GetLinkRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return GetLinkRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'GetLinkRequest($contents)';
  }
}

/// Specifies a set of log entries that are filtered out by a sink. If
/// your Google Cloud resource receives a large volume of log entries, you can
/// use exclusions to reduce your chargeable logs. Note that exclusions on
/// organization-level and folder-level sinks don't apply to child resources.
/// Note also that you cannot modify the _Required sink or exclude logs from it.
final class LogExclusion extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.LogExclusion';

  /// Required. A client-assigned identifier, such as
  /// `"load-balancer-exclusion"`. Identifiers are limited to 100 characters and
  /// can include only letters, digits, underscores, hyphens, and periods. First
  /// character has to be alphanumeric.
  final String name;

  /// Optional. A description of this exclusion.
  final String description;

  /// Required. An [advanced logs
  /// filter](https://cloud.google.com/logging/docs/view/advanced-queries) that
  /// matches the log entries to be excluded. By using the [sample
  /// function](https://cloud.google.com/logging/docs/view/advanced-queries#sample),
  /// you can exclude less than 100% of the matching log entries.
  ///
  /// For example, the following query matches 99% of low-severity log entries
  /// from Google Cloud Storage buckets:
  ///
  ///   `resource.type=gcs_bucket severity<ERROR sample(insertId, 0.99)`
  final String filter;

  /// Optional. If set to True, then this exclusion is disabled and it does not
  /// exclude any log entries. You can [update an
  /// exclusion][google.logging.v2.ConfigServiceV2.UpdateExclusion] to change the
  /// value of this field.
  final bool disabled;

  /// Output only. The creation timestamp of the exclusion.
  ///
  /// This field may not be present for older exclusions.
  final Timestamp? createTime;

  /// Output only. The last update timestamp of the exclusion.
  ///
  /// This field may not be present for older exclusions.
  final Timestamp? updateTime;

  LogExclusion({
    required this.name,
    this.description = '',
    required this.filter,
    this.disabled = false,
    this.createTime,
    this.updateTime,
  }) : super(fullyQualifiedName);

  factory LogExclusion.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return LogExclusion(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      description: switch (json['description']) {
        null => '',
        Object $1 => decodeString($1),
      },
      filter: switch (json['filter']) {
        null => '',
        Object $1 => decodeString($1),
      },
      disabled: switch (json['disabled']) {
        null => false,
        Object $1 => decodeBool($1),
      },
      createTime: switch (json['createTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      updateTime: switch (json['updateTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'name': name,
    if (description.isNotDefault) 'description': description,
    'filter': filter,
    if (disabled.isNotDefault) 'disabled': disabled,
    if (createTime != null) 'createTime': createTime!.toJson(),
    if (updateTime != null) 'updateTime': updateTime!.toJson(),
  };

  @override
  String toString() {
    final contents = [
      'name=$name',
      'description=$description',
      'filter=$filter',
      'disabled=$disabled',
    ].join(',');
    return 'LogExclusion($contents)';
  }
}

/// The parameters to `ListExclusions`.
final class ListExclusionsRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListExclusionsRequest';

  /// Required. The parent resource whose exclusions are to be listed.
  ///
  ///     "projects/[PROJECT_ID]"
  ///     "organizations/[ORGANIZATION_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]"
  ///     "folders/[FOLDER_ID]"
  final String parent;

  /// Optional. If present, then retrieve the next batch of results from the
  /// preceding call to this method. `pageToken` must be the value of
  /// `nextPageToken` from the previous response. The values of other method
  /// parameters should be identical to those in the previous call.
  final String pageToken;

  /// Optional. The maximum number of results to return from this request.
  /// Non-positive values are ignored. The presence of `nextPageToken` in the
  /// response indicates that more results might be available.
  final int pageSize;

  ListExclusionsRequest({
    required this.parent,
    this.pageToken = '',
    this.pageSize = 0,
  }) : super(fullyQualifiedName);

  factory ListExclusionsRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListExclusionsRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageToken: switch (json['pageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageSize: switch (json['pageSize']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (pageToken.isNotDefault) 'pageToken': pageToken,
    if (pageSize.isNotDefault) 'pageSize': pageSize,
  };

  @override
  String toString() {
    final contents = [
      'parent=$parent',
      'pageToken=$pageToken',
      'pageSize=$pageSize',
    ].join(',');
    return 'ListExclusionsRequest($contents)';
  }
}

/// Result returned from `ListExclusions`.
final class ListExclusionsResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListExclusionsResponse';

  /// A list of exclusions.
  final List<LogExclusion> exclusions;

  /// If there might be more results than appear in this response, then
  /// `nextPageToken` is included. To get the next set of results, call the same
  /// method again using the value of `nextPageToken` as `pageToken`.
  final String nextPageToken;

  ListExclusionsResponse({this.exclusions = const [], this.nextPageToken = ''})
    : super(fullyQualifiedName);

  factory ListExclusionsResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListExclusionsResponse(
      exclusions: switch (json['exclusions']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) LogExclusion.fromJson(i)],
        _ => throw const FormatException('"exclusions" is not a list'),
      },
      nextPageToken: switch (json['nextPageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (exclusions.isNotDefault) 'exclusions': encodeList(exclusions),
    if (nextPageToken.isNotDefault) 'nextPageToken': nextPageToken,
  };

  @override
  String toString() {
    final contents = ['nextPageToken=$nextPageToken'].join(',');
    return 'ListExclusionsResponse($contents)';
  }
}

/// The parameters to `GetExclusion`.
final class GetExclusionRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.GetExclusionRequest';

  /// Required. The resource name of an existing exclusion:
  ///
  ///     "projects/[PROJECT_ID]/exclusions/[EXCLUSION_ID]"
  ///     "organizations/[ORGANIZATION_ID]/exclusions/[EXCLUSION_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/exclusions/[EXCLUSION_ID]"
  ///     "folders/[FOLDER_ID]/exclusions/[EXCLUSION_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/exclusions/my-exclusion"`
  final String name;

  GetExclusionRequest({required this.name}) : super(fullyQualifiedName);

  factory GetExclusionRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return GetExclusionRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'GetExclusionRequest($contents)';
  }
}

/// The parameters to `CreateExclusion`.
final class CreateExclusionRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.CreateExclusionRequest';

  /// Required. The parent resource in which to create the exclusion:
  ///
  ///     "projects/[PROJECT_ID]"
  ///     "organizations/[ORGANIZATION_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]"
  ///     "folders/[FOLDER_ID]"
  ///
  /// For examples:
  ///
  ///   `"projects/my-logging-project"`
  ///   `"organizations/123456789"`
  final String parent;

  /// Required. The new exclusion, whose `name` parameter is an exclusion name
  /// that is not already used in the parent resource.
  final LogExclusion? exclusion;

  CreateExclusionRequest({required this.parent, required this.exclusion})
    : super(fullyQualifiedName);

  factory CreateExclusionRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return CreateExclusionRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      exclusion: switch (json['exclusion']) {
        null => null,
        Object $1 => LogExclusion.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (exclusion != null) 'exclusion': exclusion!.toJson(),
  };

  @override
  String toString() {
    final contents = ['parent=$parent'].join(',');
    return 'CreateExclusionRequest($contents)';
  }
}

/// The parameters to `UpdateExclusion`.
final class UpdateExclusionRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.UpdateExclusionRequest';

  /// Required. The resource name of the exclusion to update:
  ///
  ///     "projects/[PROJECT_ID]/exclusions/[EXCLUSION_ID]"
  ///     "organizations/[ORGANIZATION_ID]/exclusions/[EXCLUSION_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/exclusions/[EXCLUSION_ID]"
  ///     "folders/[FOLDER_ID]/exclusions/[EXCLUSION_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/exclusions/my-exclusion"`
  final String name;

  /// Required. New values for the existing exclusion. Only the fields specified
  /// in `update_mask` are relevant.
  final LogExclusion? exclusion;

  /// Required. A non-empty list of fields to change in the existing exclusion.
  /// New values for the fields are taken from the corresponding fields in the
  /// `LogExclusion` included in this request.
  /// Fields not mentioned in `update_mask` are not changed and are ignored in
  /// the request.
  ///
  /// For example, to change the filter and description of an exclusion,
  /// specify an `update_mask` of `"filter,description"`.
  final FieldMask? updateMask;

  UpdateExclusionRequest({
    required this.name,
    required this.exclusion,
    required this.updateMask,
  }) : super(fullyQualifiedName);

  factory UpdateExclusionRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return UpdateExclusionRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      exclusion: switch (json['exclusion']) {
        null => null,
        Object $1 => LogExclusion.fromJson($1),
      },
      updateMask: switch (json['updateMask']) {
        null => null,
        Object $1 => FieldMask.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'name': name,
    if (exclusion != null) 'exclusion': exclusion!.toJson(),
    if (updateMask != null) 'updateMask': updateMask!.toJson(),
  };

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'UpdateExclusionRequest($contents)';
  }
}

/// The parameters to `DeleteExclusion`.
final class DeleteExclusionRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.DeleteExclusionRequest';

  /// Required. The resource name of an existing exclusion to delete:
  ///
  ///     "projects/[PROJECT_ID]/exclusions/[EXCLUSION_ID]"
  ///     "organizations/[ORGANIZATION_ID]/exclusions/[EXCLUSION_ID]"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/exclusions/[EXCLUSION_ID]"
  ///     "folders/[FOLDER_ID]/exclusions/[EXCLUSION_ID]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/exclusions/my-exclusion"`
  final String name;

  DeleteExclusionRequest({required this.name}) : super(fullyQualifiedName);

  factory DeleteExclusionRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return DeleteExclusionRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'DeleteExclusionRequest($contents)';
  }
}

/// The parameters to
/// `GetCmekSettings`.
///
/// See [Enabling CMEK for Log
/// Router](https://cloud.google.com/logging/docs/routing/managed-encryption) for
/// more information.
final class GetCmekSettingsRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.GetCmekSettingsRequest';

  /// Required. The resource for which to retrieve CMEK settings.
  ///
  ///     "projects/[PROJECT_ID]/cmekSettings"
  ///     "organizations/[ORGANIZATION_ID]/cmekSettings"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/cmekSettings"
  ///     "folders/[FOLDER_ID]/cmekSettings"
  ///
  /// For example:
  ///
  ///   `"organizations/12345/cmekSettings"`
  ///
  /// Note: CMEK for the Log Router can be configured for Google Cloud projects,
  /// folders, organizations and billing accounts. Once configured for an
  /// organization, it applies to all projects and folders in the Google Cloud
  /// organization.
  final String name;

  GetCmekSettingsRequest({required this.name}) : super(fullyQualifiedName);

  factory GetCmekSettingsRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return GetCmekSettingsRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'GetCmekSettingsRequest($contents)';
  }
}

/// The parameters to
/// `UpdateCmekSettings`.
///
/// See [Enabling CMEK for Log
/// Router](https://cloud.google.com/logging/docs/routing/managed-encryption) for
/// more information.
final class UpdateCmekSettingsRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.UpdateCmekSettingsRequest';

  /// Required. The resource name for the CMEK settings to update.
  ///
  ///     "projects/[PROJECT_ID]/cmekSettings"
  ///     "organizations/[ORGANIZATION_ID]/cmekSettings"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/cmekSettings"
  ///     "folders/[FOLDER_ID]/cmekSettings"
  ///
  /// For example:
  ///
  ///   `"organizations/12345/cmekSettings"`
  ///
  /// Note: CMEK for the Log Router can currently only be configured for Google
  /// Cloud organizations. Once configured, it applies to all projects and
  /// folders in the Google Cloud organization.
  final String name;

  /// Required. The CMEK settings to update.
  ///
  /// See [Enabling CMEK for Log
  /// Router](https://cloud.google.com/logging/docs/routing/managed-encryption)
  /// for more information.
  final CmekSettings? cmekSettings;

  /// Optional. Field mask identifying which fields from `cmek_settings` should
  /// be updated. A field will be overwritten if and only if it is in the update
  /// mask. Output only fields cannot be updated.
  ///
  /// See `FieldMask` for more information.
  ///
  /// For example: `"updateMask=kmsKeyName"`
  final FieldMask? updateMask;

  UpdateCmekSettingsRequest({
    required this.name,
    required this.cmekSettings,
    this.updateMask,
  }) : super(fullyQualifiedName);

  factory UpdateCmekSettingsRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return UpdateCmekSettingsRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      cmekSettings: switch (json['cmekSettings']) {
        null => null,
        Object $1 => CmekSettings.fromJson($1),
      },
      updateMask: switch (json['updateMask']) {
        null => null,
        Object $1 => FieldMask.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'name': name,
    if (cmekSettings != null) 'cmekSettings': cmekSettings!.toJson(),
    if (updateMask != null) 'updateMask': updateMask!.toJson(),
  };

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'UpdateCmekSettingsRequest($contents)';
  }
}

/// Describes the customer-managed encryption key (CMEK) settings associated with
/// a project, folder, organization, billing account, or flexible resource.
///
/// Note: CMEK for the Log Router can currently only be configured for Google
/// Cloud organizations. Once configured, it applies to all projects and folders
/// in the Google Cloud organization.
///
/// See [Enabling CMEK for Log
/// Router](https://cloud.google.com/logging/docs/routing/managed-encryption) for
/// more information.
final class CmekSettings extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.CmekSettings';

  /// Output only. The resource name of the CMEK settings.
  final String name;

  /// The resource name for the configured Cloud KMS key.
  ///
  /// KMS key name format:
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION]/keyRings/[KEYRING]/cryptoKeys/[KEY]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/locations/us-central1/keyRings/my-ring/cryptoKeys/my-key"`
  ///
  ///
  ///
  /// To enable CMEK for the Log Router, set this field to a valid
  /// `kms_key_name` for which the associated service account has the required
  /// cloudkms.cryptoKeyEncrypterDecrypter roles assigned for the key.
  ///
  /// The Cloud KMS key used by the Log Router can be updated by changing the
  /// `kms_key_name` to a new valid key name or disabled by setting the key name
  /// to an empty string. Encryption operations that are in progress will be
  /// completed with the key that was in use when they started. Decryption
  /// operations will be completed using the key that was used at the time of
  /// encryption unless access to that key has been revoked.
  ///
  /// To disable CMEK for the Log Router, set this field to an empty string.
  ///
  /// See [Enabling CMEK for Log
  /// Router](https://cloud.google.com/logging/docs/routing/managed-encryption)
  /// for more information.
  final String kmsKeyName;

  /// The CryptoKeyVersion resource name for the configured Cloud KMS key.
  ///
  /// KMS key name format:
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION]/keyRings/[KEYRING]/cryptoKeys/[KEY]/cryptoKeyVersions/[VERSION]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/locations/us-central1/keyRings/my-ring/cryptoKeys/my-key/cryptoKeyVersions/1"`
  ///
  /// This is a read-only field used to convey the specific configured
  /// CryptoKeyVersion of `kms_key` that has been configured. It will be
  /// populated in cases where the CMEK settings are bound to a single key
  /// version.
  ///
  /// If this field is populated, the `kms_key` is tied to a specific
  /// CryptoKeyVersion.
  final String kmsKeyVersionName;

  /// Output only. The service account that will be used by the Log Router to
  /// access your Cloud KMS key.
  ///
  /// Before enabling CMEK for Log Router, you must first assign the
  /// cloudkms.cryptoKeyEncrypterDecrypter role to the service account that
  /// the Log Router will use to access your Cloud KMS key. Use
  /// `GetCmekSettings` to
  /// obtain the service account ID.
  ///
  /// See [Enabling CMEK for Log
  /// Router](https://cloud.google.com/logging/docs/routing/managed-encryption)
  /// for more information.
  final String serviceAccountId;

  CmekSettings({
    this.name = '',
    this.kmsKeyName = '',
    this.kmsKeyVersionName = '',
    this.serviceAccountId = '',
  }) : super(fullyQualifiedName);

  factory CmekSettings.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return CmekSettings(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      kmsKeyName: switch (json['kmsKeyName']) {
        null => '',
        Object $1 => decodeString($1),
      },
      kmsKeyVersionName: switch (json['kmsKeyVersionName']) {
        null => '',
        Object $1 => decodeString($1),
      },
      serviceAccountId: switch (json['serviceAccountId']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (name.isNotDefault) 'name': name,
    if (kmsKeyName.isNotDefault) 'kmsKeyName': kmsKeyName,
    if (kmsKeyVersionName.isNotDefault) 'kmsKeyVersionName': kmsKeyVersionName,
    if (serviceAccountId.isNotDefault) 'serviceAccountId': serviceAccountId,
  };

  @override
  String toString() {
    final contents = [
      'name=$name',
      'kmsKeyName=$kmsKeyName',
      'kmsKeyVersionName=$kmsKeyVersionName',
      'serviceAccountId=$serviceAccountId',
    ].join(',');
    return 'CmekSettings($contents)';
  }
}

/// The parameters to
/// `GetSettings`.
///
/// See [Enabling CMEK for Log
/// Router](https://cloud.google.com/logging/docs/routing/managed-encryption) for
/// more information.
final class GetSettingsRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.GetSettingsRequest';

  /// Required. The resource for which to retrieve settings.
  ///
  ///     "projects/[PROJECT_ID]/settings"
  ///     "organizations/[ORGANIZATION_ID]/settings"
  ///     "billingAccounts/[BILLING_ACCOUNT_ID]/settings"
  ///     "folders/[FOLDER_ID]/settings"
  ///
  /// For example:
  ///
  ///   `"organizations/12345/settings"`
  ///
  /// Note: Settings for the Log Router can be get for Google Cloud projects,
  /// folders, organizations and billing accounts. Currently it can only be
  /// configured for organizations. Once configured for an organization, it
  /// applies to all projects and folders in the Google Cloud organization.
  final String name;

  GetSettingsRequest({required this.name}) : super(fullyQualifiedName);

  factory GetSettingsRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return GetSettingsRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'GetSettingsRequest($contents)';
  }
}

/// The parameters to
/// `UpdateSettings`.
///
/// See [Enabling CMEK for Log
/// Router](https://cloud.google.com/logging/docs/routing/managed-encryption) for
/// more information.
final class UpdateSettingsRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.UpdateSettingsRequest';

  /// Required. The resource name for the settings to update.
  ///
  ///     "organizations/[ORGANIZATION_ID]/settings"
  ///
  /// For example:
  ///
  ///   `"organizations/12345/settings"`
  ///
  /// Note: Settings for the Log Router can currently only be configured for
  /// Google Cloud organizations. Once configured, it applies to all projects and
  /// folders in the Google Cloud organization.
  final String name;

  /// Required. The settings to update.
  ///
  /// See [Enabling CMEK for Log
  /// Router](https://cloud.google.com/logging/docs/routing/managed-encryption)
  /// for more information.
  final Settings? settings;

  /// Optional. Field mask identifying which fields from `settings` should
  /// be updated. A field will be overwritten if and only if it is in the update
  /// mask. Output only fields cannot be updated.
  ///
  /// See `FieldMask` for more information.
  ///
  /// For example: `"updateMask=kmsKeyName"`
  final FieldMask? updateMask;

  UpdateSettingsRequest({
    required this.name,
    required this.settings,
    this.updateMask,
  }) : super(fullyQualifiedName);

  factory UpdateSettingsRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return UpdateSettingsRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      settings: switch (json['settings']) {
        null => null,
        Object $1 => Settings.fromJson($1),
      },
      updateMask: switch (json['updateMask']) {
        null => null,
        Object $1 => FieldMask.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'name': name,
    if (settings != null) 'settings': settings!.toJson(),
    if (updateMask != null) 'updateMask': updateMask!.toJson(),
  };

  @override
  String toString() {
    final contents = ['name=$name'].join(',');
    return 'UpdateSettingsRequest($contents)';
  }
}

/// Describes the settings associated with a project, folder, organization,
/// billing account, or flexible resource.
final class Settings extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.Settings';

  /// Output only. The resource name of the settings.
  final String name;

  /// Optional. The resource name for the configured Cloud KMS key.
  ///
  /// KMS key name format:
  ///
  ///     "projects/[PROJECT_ID]/locations/[LOCATION]/keyRings/[KEYRING]/cryptoKeys/[KEY]"
  ///
  /// For example:
  ///
  ///   `"projects/my-project/locations/us-central1/keyRings/my-ring/cryptoKeys/my-key"`
  ///
  ///
  ///
  /// To enable CMEK for the Log Router, set this field to a valid
  /// `kms_key_name` for which the associated service account has the required
  /// `roles/cloudkms.cryptoKeyEncrypterDecrypter` role assigned for the key.
  ///
  /// The Cloud KMS key used by the Log Router can be updated by changing the
  /// `kms_key_name` to a new valid key name. Encryption operations that are in
  /// progress will be completed with the key that was in use when they started.
  /// Decryption operations will be completed using the key that was used at the
  /// time of encryption unless access to that key has been revoked.
  ///
  /// To disable CMEK for the Log Router, set this field to an empty string.
  ///
  /// See [Enabling CMEK for Log
  /// Router](https://cloud.google.com/logging/docs/routing/managed-encryption)
  /// for more information.
  final String kmsKeyName;

  /// Output only. The service account that will be used by the Log Router to
  /// access your Cloud KMS key.
  ///
  /// Before enabling CMEK for Log Router, you must first assign the role
  /// `roles/cloudkms.cryptoKeyEncrypterDecrypter` to the service account that
  /// the Log Router will use to access your Cloud KMS key. Use
  /// `GetSettings` to
  /// obtain the service account ID.
  ///
  /// See [Enabling CMEK for Log
  /// Router](https://cloud.google.com/logging/docs/routing/managed-encryption)
  /// for more information.
  final String kmsServiceAccountId;

  /// Optional. The Cloud region that will be used for _Default and _Required log
  /// buckets for newly created projects and folders. For example `europe-west1`.
  /// This setting does not affect the location of custom log buckets.
  final String storageLocation;

  /// Optional. If set to true, the _Default sink in newly created projects and
  /// folders will created in a disabled state. This can be used to automatically
  /// disable log ingestion if there is already an aggregated sink configured in
  /// the hierarchy. The _Default sink can be re-enabled manually if needed.
  final bool disableDefaultSink;

  Settings({
    this.name = '',
    this.kmsKeyName = '',
    this.kmsServiceAccountId = '',
    this.storageLocation = '',
    this.disableDefaultSink = false,
  }) : super(fullyQualifiedName);

  factory Settings.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return Settings(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      kmsKeyName: switch (json['kmsKeyName']) {
        null => '',
        Object $1 => decodeString($1),
      },
      kmsServiceAccountId: switch (json['kmsServiceAccountId']) {
        null => '',
        Object $1 => decodeString($1),
      },
      storageLocation: switch (json['storageLocation']) {
        null => '',
        Object $1 => decodeString($1),
      },
      disableDefaultSink: switch (json['disableDefaultSink']) {
        null => false,
        Object $1 => decodeBool($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (name.isNotDefault) 'name': name,
    if (kmsKeyName.isNotDefault) 'kmsKeyName': kmsKeyName,
    if (kmsServiceAccountId.isNotDefault)
      'kmsServiceAccountId': kmsServiceAccountId,
    if (storageLocation.isNotDefault) 'storageLocation': storageLocation,
    if (disableDefaultSink.isNotDefault)
      'disableDefaultSink': disableDefaultSink,
  };

  @override
  String toString() {
    final contents = [
      'name=$name',
      'kmsKeyName=$kmsKeyName',
      'kmsServiceAccountId=$kmsServiceAccountId',
      'storageLocation=$storageLocation',
      'disableDefaultSink=$disableDefaultSink',
    ].join(',');
    return 'Settings($contents)';
  }
}

/// The parameters to CopyLogEntries.
final class CopyLogEntriesRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.CopyLogEntriesRequest';

  /// Required. Log bucket from which to copy log entries.
  ///
  /// For example:
  ///
  ///   `"projects/my-project/locations/global/buckets/my-source-bucket"`
  final String name;

  /// Optional. A filter specifying which log entries to copy. The filter must be
  /// no more than 20k characters. An empty filter matches all log entries.
  final String filter;

  /// Required. Destination to which to copy log entries.
  final String destination;

  CopyLogEntriesRequest({
    required this.name,
    this.filter = '',
    required this.destination,
  }) : super(fullyQualifiedName);

  factory CopyLogEntriesRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return CopyLogEntriesRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      filter: switch (json['filter']) {
        null => '',
        Object $1 => decodeString($1),
      },
      destination: switch (json['destination']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    'name': name,
    if (filter.isNotDefault) 'filter': filter,
    'destination': destination,
  };

  @override
  String toString() {
    final contents = [
      'name=$name',
      'filter=$filter',
      'destination=$destination',
    ].join(',');
    return 'CopyLogEntriesRequest($contents)';
  }
}

/// Metadata for CopyLogEntries long running operations.
final class CopyLogEntriesMetadata extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.CopyLogEntriesMetadata';

  /// The create time of an operation.
  final Timestamp? startTime;

  /// The end time of an operation.
  final Timestamp? endTime;

  /// State of an operation.
  final OperationState state;

  /// Identifies whether the user has requested cancellation of the operation.
  final bool cancellationRequested;

  /// CopyLogEntries RPC request.
  final CopyLogEntriesRequest? request;

  /// Estimated progress of the operation (0 - 100%).
  final int progress;

  /// The IAM identity of a service account that must be granted access to the
  /// destination.
  ///
  /// If the service account is not granted permission to the destination within
  /// an hour, the operation will be cancelled.
  ///
  /// For example: `"serviceAccount:foo@bar.com"`
  final String writerIdentity;

  CopyLogEntriesMetadata({
    this.startTime,
    this.endTime,
    this.state = OperationState.$default,
    this.cancellationRequested = false,
    this.request,
    this.progress = 0,
    this.writerIdentity = '',
  }) : super(fullyQualifiedName);

  factory CopyLogEntriesMetadata.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return CopyLogEntriesMetadata(
      startTime: switch (json['startTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      endTime: switch (json['endTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      state: switch (json['state']) {
        null => OperationState.$default,
        Object $1 => OperationState.fromJson($1),
      },
      cancellationRequested: switch (json['cancellationRequested']) {
        null => false,
        Object $1 => decodeBool($1),
      },
      request: switch (json['request']) {
        null => null,
        Object $1 => CopyLogEntriesRequest.fromJson($1),
      },
      progress: switch (json['progress']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
      writerIdentity: switch (json['writerIdentity']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (startTime != null) 'startTime': startTime!.toJson(),
    if (endTime != null) 'endTime': endTime!.toJson(),
    if (state.isNotDefault) 'state': state.toJson(),
    if (cancellationRequested.isNotDefault)
      'cancellationRequested': cancellationRequested,
    if (request != null) 'request': request!.toJson(),
    if (progress.isNotDefault) 'progress': progress,
    if (writerIdentity.isNotDefault) 'writerIdentity': writerIdentity,
  };

  @override
  String toString() {
    final contents = [
      'state=$state',
      'cancellationRequested=$cancellationRequested',
      'progress=$progress',
      'writerIdentity=$writerIdentity',
    ].join(',');
    return 'CopyLogEntriesMetadata($contents)';
  }
}

/// Response type for CopyLogEntries long running operations.
final class CopyLogEntriesResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.CopyLogEntriesResponse';

  /// Number of log entries copied.
  final int logEntriesCopiedCount;

  CopyLogEntriesResponse({this.logEntriesCopiedCount = 0})
    : super(fullyQualifiedName);

  factory CopyLogEntriesResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return CopyLogEntriesResponse(
      logEntriesCopiedCount: switch (json['logEntriesCopiedCount']) {
        null => 0,
        Object $1 => decodeInt64($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (logEntriesCopiedCount.isNotDefault)
      'logEntriesCopiedCount': encodeInt64(logEntriesCopiedCount),
  };

  @override
  String toString() {
    final contents = ['logEntriesCopiedCount=$logEntriesCopiedCount'].join(',');
    return 'CopyLogEntriesResponse($contents)';
  }
}

/// Metadata for LongRunningUpdateBucket Operations.
final class BucketMetadata extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.BucketMetadata';

  /// The create time of an operation.
  final Timestamp? startTime;

  /// The end time of an operation.
  final Timestamp? endTime;

  /// State of an operation.
  final OperationState state;

  /// LongRunningCreateBucket RPC request.
  final CreateBucketRequest? createBucketRequest;

  /// LongRunningUpdateBucket RPC request.
  final UpdateBucketRequest? updateBucketRequest;

  BucketMetadata({
    this.startTime,
    this.endTime,
    this.state = OperationState.$default,
    this.createBucketRequest,
    this.updateBucketRequest,
  }) : super(fullyQualifiedName);

  factory BucketMetadata.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return BucketMetadata(
      startTime: switch (json['startTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      endTime: switch (json['endTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      state: switch (json['state']) {
        null => OperationState.$default,
        Object $1 => OperationState.fromJson($1),
      },
      createBucketRequest: switch (json['createBucketRequest']) {
        null => null,
        Object $1 => CreateBucketRequest.fromJson($1),
      },
      updateBucketRequest: switch (json['updateBucketRequest']) {
        null => null,
        Object $1 => UpdateBucketRequest.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (startTime != null) 'startTime': startTime!.toJson(),
    if (endTime != null) 'endTime': endTime!.toJson(),
    if (state.isNotDefault) 'state': state.toJson(),
    if (createBucketRequest != null)
      'createBucketRequest': createBucketRequest!.toJson(),
    if (updateBucketRequest != null)
      'updateBucketRequest': updateBucketRequest!.toJson(),
  };

  @override
  String toString() {
    final contents = ['state=$state'].join(',');
    return 'BucketMetadata($contents)';
  }
}

/// Metadata for long running Link operations.
final class LinkMetadata extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.LinkMetadata';

  /// The start time of an operation.
  final Timestamp? startTime;

  /// The end time of an operation.
  final Timestamp? endTime;

  /// State of an operation.
  final OperationState state;

  /// CreateLink RPC request.
  final CreateLinkRequest? createLinkRequest;

  /// DeleteLink RPC request.
  final DeleteLinkRequest? deleteLinkRequest;

  LinkMetadata({
    this.startTime,
    this.endTime,
    this.state = OperationState.$default,
    this.createLinkRequest,
    this.deleteLinkRequest,
  }) : super(fullyQualifiedName);

  factory LinkMetadata.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return LinkMetadata(
      startTime: switch (json['startTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      endTime: switch (json['endTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      state: switch (json['state']) {
        null => OperationState.$default,
        Object $1 => OperationState.fromJson($1),
      },
      createLinkRequest: switch (json['createLinkRequest']) {
        null => null,
        Object $1 => CreateLinkRequest.fromJson($1),
      },
      deleteLinkRequest: switch (json['deleteLinkRequest']) {
        null => null,
        Object $1 => DeleteLinkRequest.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (startTime != null) 'startTime': startTime!.toJson(),
    if (endTime != null) 'endTime': endTime!.toJson(),
    if (state.isNotDefault) 'state': state.toJson(),
    if (createLinkRequest != null)
      'createLinkRequest': createLinkRequest!.toJson(),
    if (deleteLinkRequest != null)
      'deleteLinkRequest': deleteLinkRequest!.toJson(),
  };

  @override
  String toString() {
    final contents = ['state=$state'].join(',');
    return 'LinkMetadata($contents)';
  }
}

/// Cloud Logging specific location metadata.
final class LocationMetadata extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.LocationMetadata';

  /// Indicates whether or not Log Analytics features are supported in the given
  /// location.
  final bool logAnalyticsEnabled;

  LocationMetadata({this.logAnalyticsEnabled = false})
    : super(fullyQualifiedName);

  factory LocationMetadata.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return LocationMetadata(
      logAnalyticsEnabled: switch (json['logAnalyticsEnabled']) {
        null => false,
        Object $1 => decodeBool($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (logAnalyticsEnabled.isNotDefault)
      'logAnalyticsEnabled': logAnalyticsEnabled,
  };

  @override
  String toString() {
    final contents = ['logAnalyticsEnabled=$logAnalyticsEnabled'].join(',');
    return 'LocationMetadata($contents)';
  }
}

/// Describes a logs-based metric. The value of the metric is the number of log
/// entries that match a logs filter in a given time interval.
///
/// Logs-based metrics can also be used to extract values from logs and create a
/// distribution of the values. The distribution records the statistics of the
/// extracted values along with an optional histogram of the values as specified
/// by the bucket options.
final class LogMetric extends ProtoMessage {
  static const String fullyQualifiedName = 'google.logging.v2.LogMetric';

  /// Required. The client-assigned metric identifier.
  /// Examples: `"error_count"`, `"nginx/requests"`.
  ///
  /// Metric identifiers are limited to 100 characters and can include only the
  /// following characters: `A-Z`, `a-z`, `0-9`, and the special characters
  /// `_-.,+!*',()%/`. The forward-slash character (`/`) denotes a hierarchy of
  /// name pieces, and it cannot be the first character of the name.
  ///
  /// This field is the `[METRIC_ID]` part of a metric resource name in the
  /// format "projects/[PROJECT_ID]/metrics/[METRIC_ID]". Example: If the
  /// resource name of a metric is
  /// `"projects/my-project/metrics/nginx%2Frequests"`, this field's value is
  /// `"nginx/requests"`.
  final String name;

  /// Optional. A description of this metric, which is used in documentation.
  /// The maximum length of the description is 8000 characters.
  final String description;

  /// Required. An [advanced logs
  /// filter](https://cloud.google.com/logging/docs/view/advanced_filters) which
  /// is used to match log entries. Example:
  ///
  ///     "resource.type=gae_app AND severity>=ERROR"
  ///
  /// The maximum length of the filter is 20000 characters.
  final String filter;

  /// Optional. The resource name of the Log Bucket that owns the Log Metric.
  /// Only Log Buckets in projects are supported. The bucket has to be in the
  /// same project as the metric.
  ///
  /// For example:
  ///
  ///   `projects/my-project/locations/global/buckets/my-bucket`
  ///
  /// If empty, then the Log Metric is considered a non-Bucket Log Metric.
  final String bucketName;

  /// Optional. If set to True, then this metric is disabled and it does not
  /// generate any points.
  final bool disabled;

  /// Optional. The metric descriptor associated with the logs-based metric.
  /// If unspecified, it uses a default metric descriptor with a DELTA metric
  /// kind, INT64 value type, with no labels and a unit of "1". Such a metric
  /// counts the number of log entries matching the `filter` expression.
  ///
  /// The `name`, `type`, and `description` fields in the `metric_descriptor`
  /// are output only, and is constructed using the `name` and `description`
  /// field in the LogMetric.
  ///
  /// To create a logs-based metric that records a distribution of log values, a
  /// DELTA metric kind with a DISTRIBUTION value type must be used along with
  /// a `value_extractor` expression in the LogMetric.
  ///
  /// Each label in the metric descriptor must have a matching label
  /// name as the key and an extractor expression as the value in the
  /// `label_extractors` map.
  ///
  /// The `metric_kind` and `value_type` fields in the `metric_descriptor` cannot
  /// be updated once initially configured. New labels can be added in the
  /// `metric_descriptor`, but existing labels cannot be modified except for
  /// their description.
  final MetricDescriptor? metricDescriptor;

  /// Optional. A `value_extractor` is required when using a distribution
  /// logs-based metric to extract the values to record from a log entry.
  /// Two functions are supported for value extraction: `EXTRACT(field)` or
  /// `REGEXP_EXTRACT(field, regex)`. The arguments are:
  ///
  ///   1. field: The name of the log entry field from which the value is to be
  ///      extracted.
  ///   2. regex: A regular expression using the Google RE2 syntax
  ///      (https://github.com/google/re2/wiki/Syntax) with a single capture
  ///      group to extract data from the specified log entry field. The value
  ///      of the field is converted to a string before applying the regex.
  ///      It is an error to specify a regex that does not include exactly one
  ///      capture group.
  ///
  /// The result of the extraction must be convertible to a double type, as the
  /// distribution always records double values. If either the extraction or
  /// the conversion to double fails, then those values are not recorded in the
  /// distribution.
  ///
  /// Example: `REGEXP_EXTRACT(jsonPayload.request, ".*quantity=(\d+).*")`
  final String valueExtractor;

  /// Optional. A map from a label key string to an extractor expression which is
  /// used to extract data from a log entry field and assign as the label value.
  /// Each label key specified in the LabelDescriptor must have an associated
  /// extractor expression in this map. The syntax of the extractor expression
  /// is the same as for the `value_extractor` field.
  ///
  /// The extracted value is converted to the type defined in the label
  /// descriptor. If either the extraction or the type conversion fails,
  /// the label will have a default value. The default value for a string
  /// label is an empty string, for an integer label its 0, and for a boolean
  /// label its `false`.
  ///
  /// Note that there are upper bounds on the maximum number of labels and the
  /// number of active time series that are allowed in a project.
  final Map<String, String> labelExtractors;

  /// Optional. The `bucket_options` are required when the logs-based metric is
  /// using a DISTRIBUTION value type and it describes the bucket boundaries
  /// used to create a histogram of the extracted values.
  final Distribution_BucketOptions? bucketOptions;

  /// Output only. The creation timestamp of the metric.
  ///
  /// This field may not be present for older metrics.
  final Timestamp? createTime;

  /// Output only. The last update timestamp of the metric.
  ///
  /// This field may not be present for older metrics.
  final Timestamp? updateTime;

  /// Deprecated. The API version that created or updated this metric.
  /// The v2 format is used by default and cannot be changed.
  final LogMetric_ApiVersion version;

  LogMetric({
    required this.name,
    this.description = '',
    required this.filter,
    this.bucketName = '',
    this.disabled = false,
    this.metricDescriptor,
    this.valueExtractor = '',
    this.labelExtractors = const {},
    this.bucketOptions,
    this.createTime,
    this.updateTime,
    this.version = LogMetric_ApiVersion.$default,
  }) : super(fullyQualifiedName);

  factory LogMetric.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return LogMetric(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      description: switch (json['description']) {
        null => '',
        Object $1 => decodeString($1),
      },
      filter: switch (json['filter']) {
        null => '',
        Object $1 => decodeString($1),
      },
      bucketName: switch (json['bucketName']) {
        null => '',
        Object $1 => decodeString($1),
      },
      disabled: switch (json['disabled']) {
        null => false,
        Object $1 => decodeBool($1),
      },
      metricDescriptor: switch (json['metricDescriptor']) {
        null => null,
        Object $1 => MetricDescriptor.fromJson($1),
      },
      valueExtractor: switch (json['valueExtractor']) {
        null => '',
        Object $1 => decodeString($1),
      },
      labelExtractors: switch (json['labelExtractors']) {
        null => {},
        Map<String, Object?> $1 => {
          for (final e in $1.entries)
            decodeString(e.key): decodeString(e.value),
        },
        _ => throw const FormatException('"labelExtractors" is not an object'),
      },
      bucketOptions: switch (json['bucketOptions']) {
        null => null,
        Object $1 => Distribution_BucketOptions.fromJson($1),
      },
      createTime: switch (json['createTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      updateTime: switch (json['updateTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      version: switch (json['version']) {
        null => LogMetric_ApiVersion.$default,
        Object $1 => LogMetric_ApiVersion.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'name': name,
    if (description.isNotDefault) 'description': description,
    'filter': filter,
    if (bucketName.isNotDefault) 'bucketName': bucketName,
    if (disabled.isNotDefault) 'disabled': disabled,
    if (metricDescriptor != null)
      'metricDescriptor': metricDescriptor!.toJson(),
    if (valueExtractor.isNotDefault) 'valueExtractor': valueExtractor,
    if (labelExtractors.isNotDefault) 'labelExtractors': labelExtractors,
    if (bucketOptions != null) 'bucketOptions': bucketOptions!.toJson(),
    if (createTime != null) 'createTime': createTime!.toJson(),
    if (updateTime != null) 'updateTime': updateTime!.toJson(),
    if (version.isNotDefault) 'version': version.toJson(),
  };

  @override
  String toString() {
    final contents = [
      'name=$name',
      'description=$description',
      'filter=$filter',
      'bucketName=$bucketName',
      'disabled=$disabled',
      'valueExtractor=$valueExtractor',
      'version=$version',
    ].join(',');
    return 'LogMetric($contents)';
  }
}

/// Logging API version.
final class LogMetric_ApiVersion extends ProtoEnum {
  /// Logging API v2.
  static const v2 = LogMetric_ApiVersion('V2');

  /// Logging API v1.
  static const v1 = LogMetric_ApiVersion('V1');

  /// The default value for [LogMetric_ApiVersion].
  static const $default = v2;

  const LogMetric_ApiVersion(super.value);

  factory LogMetric_ApiVersion.fromJson(Object? json) =>
      LogMetric_ApiVersion(json as String);

  bool get isNotDefault => this != $default;

  @override
  String toString() => 'ApiVersion.$value';
}

/// The parameters to ListLogMetrics.
final class ListLogMetricsRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListLogMetricsRequest';

  /// Required. The name of the project containing the metrics:
  ///
  ///     "projects/[PROJECT_ID]"
  final String parent;

  /// Optional. If present, then retrieve the next batch of results from the
  /// preceding call to this method. `pageToken` must be the value of
  /// `nextPageToken` from the previous response. The values of other method
  /// parameters should be identical to those in the previous call.
  final String pageToken;

  /// Optional. The maximum number of results to return from this request.
  /// Non-positive values are ignored. The presence of `nextPageToken` in the
  /// response indicates that more results might be available.
  final int pageSize;

  ListLogMetricsRequest({
    required this.parent,
    this.pageToken = '',
    this.pageSize = 0,
  }) : super(fullyQualifiedName);

  factory ListLogMetricsRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListLogMetricsRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageToken: switch (json['pageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageSize: switch (json['pageSize']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (pageToken.isNotDefault) 'pageToken': pageToken,
    if (pageSize.isNotDefault) 'pageSize': pageSize,
  };

  @override
  String toString() {
    final contents = [
      'parent=$parent',
      'pageToken=$pageToken',
      'pageSize=$pageSize',
    ].join(',');
    return 'ListLogMetricsRequest($contents)';
  }
}

/// Result returned from ListLogMetrics.
final class ListLogMetricsResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.ListLogMetricsResponse';

  /// A list of logs-based metrics.
  final List<LogMetric> metrics;

  /// If there might be more results than appear in this response, then
  /// `nextPageToken` is included. To get the next set of results, call this
  /// method again using the value of `nextPageToken` as `pageToken`.
  final String nextPageToken;

  ListLogMetricsResponse({this.metrics = const [], this.nextPageToken = ''})
    : super(fullyQualifiedName);

  factory ListLogMetricsResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListLogMetricsResponse(
      metrics: switch (json['metrics']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) LogMetric.fromJson(i)],
        _ => throw const FormatException('"metrics" is not a list'),
      },
      nextPageToken: switch (json['nextPageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (metrics.isNotDefault) 'metrics': encodeList(metrics),
    if (nextPageToken.isNotDefault) 'nextPageToken': nextPageToken,
  };

  @override
  String toString() {
    final contents = ['nextPageToken=$nextPageToken'].join(',');
    return 'ListLogMetricsResponse($contents)';
  }
}

/// The parameters to GetLogMetric.
final class GetLogMetricRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.GetLogMetricRequest';

  /// Required. The resource name of the desired metric:
  ///
  ///     "projects/[PROJECT_ID]/metrics/[METRIC_ID]"
  final String metricName;

  GetLogMetricRequest({required this.metricName}) : super(fullyQualifiedName);

  factory GetLogMetricRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return GetLogMetricRequest(
      metricName: switch (json['metricName']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'metricName': metricName};

  @override
  String toString() {
    final contents = ['metricName=$metricName'].join(',');
    return 'GetLogMetricRequest($contents)';
  }
}

/// The parameters to CreateLogMetric.
final class CreateLogMetricRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.CreateLogMetricRequest';

  /// Required. The resource name of the project in which to create the metric:
  ///
  ///     "projects/[PROJECT_ID]"
  ///
  /// The new metric must be provided in the request.
  final String parent;

  /// Required. The new logs-based metric, which must not have an identifier that
  /// already exists.
  final LogMetric? metric;

  CreateLogMetricRequest({required this.parent, required this.metric})
    : super(fullyQualifiedName);

  factory CreateLogMetricRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return CreateLogMetricRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      metric: switch (json['metric']) {
        null => null,
        Object $1 => LogMetric.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (metric != null) 'metric': metric!.toJson(),
  };

  @override
  String toString() {
    final contents = ['parent=$parent'].join(',');
    return 'CreateLogMetricRequest($contents)';
  }
}

/// The parameters to UpdateLogMetric.
final class UpdateLogMetricRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.UpdateLogMetricRequest';

  /// Required. The resource name of the metric to update:
  ///
  ///     "projects/[PROJECT_ID]/metrics/[METRIC_ID]"
  ///
  /// The updated metric must be provided in the request and it's
  /// `name` field must be the same as `[METRIC_ID]` If the metric
  /// does not exist in `[PROJECT_ID]`, then a new metric is created.
  final String metricName;

  /// Required. The updated metric.
  final LogMetric? metric;

  UpdateLogMetricRequest({required this.metricName, required this.metric})
    : super(fullyQualifiedName);

  factory UpdateLogMetricRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return UpdateLogMetricRequest(
      metricName: switch (json['metricName']) {
        null => '',
        Object $1 => decodeString($1),
      },
      metric: switch (json['metric']) {
        null => null,
        Object $1 => LogMetric.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'metricName': metricName,
    if (metric != null) 'metric': metric!.toJson(),
  };

  @override
  String toString() {
    final contents = ['metricName=$metricName'].join(',');
    return 'UpdateLogMetricRequest($contents)';
  }
}

/// The parameters to DeleteLogMetric.
final class DeleteLogMetricRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.logging.v2.DeleteLogMetricRequest';

  /// Required. The resource name of the metric to delete:
  ///
  ///     "projects/[PROJECT_ID]/metrics/[METRIC_ID]"
  final String metricName;

  DeleteLogMetricRequest({required this.metricName})
    : super(fullyQualifiedName);

  factory DeleteLogMetricRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return DeleteLogMetricRequest(
      metricName: switch (json['metricName']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'metricName': metricName};

  @override
  String toString() {
    final contents = ['metricName=$metricName'].join(',');
    return 'DeleteLogMetricRequest($contents)';
  }
}

/// List of different operation states.
/// High level state of the operation. This is used to report the job's
/// current state to the user. Once a long running operation is created,
/// the current state of the operation can be queried even before the
/// operation is finished and the final result is available.
final class OperationState extends ProtoEnum {
  /// Should not be used.
  static const operationStateUnspecified = OperationState(
    'OPERATION_STATE_UNSPECIFIED',
  );

  /// The operation is scheduled.
  static const operationStateScheduled = OperationState(
    'OPERATION_STATE_SCHEDULED',
  );

  /// Waiting for necessary permissions.
  static const operationStateWaitingForPermissions = OperationState(
    'OPERATION_STATE_WAITING_FOR_PERMISSIONS',
  );

  /// The operation is running.
  static const operationStateRunning = OperationState(
    'OPERATION_STATE_RUNNING',
  );

  /// The operation was completed successfully.
  static const operationStateSucceeded = OperationState(
    'OPERATION_STATE_SUCCEEDED',
  );

  /// The operation failed.
  static const operationStateFailed = OperationState('OPERATION_STATE_FAILED');

  /// The operation was cancelled by the user.
  static const operationStateCancelled = OperationState(
    'OPERATION_STATE_CANCELLED',
  );

  /// The default value for [OperationState].
  static const $default = operationStateUnspecified;

  const OperationState(super.value);

  factory OperationState.fromJson(Object? json) =>
      OperationState(json as String);

  bool get isNotDefault => this != $default;

  @override
  String toString() => 'OperationState.$value';
}

/// LogBucket lifecycle states.
final class LifecycleState extends ProtoEnum {
  /// Unspecified state. This is only used/useful for distinguishing unset
  /// values.
  static const lifecycleStateUnspecified = LifecycleState(
    'LIFECYCLE_STATE_UNSPECIFIED',
  );

  /// The normal and active state.
  static const active = LifecycleState('ACTIVE');

  /// The resource has been marked for deletion by the user. For some resources
  /// (e.g. buckets), this can be reversed by an un-delete operation.
  static const deleteRequested = LifecycleState('DELETE_REQUESTED');

  /// The resource has been marked for an update by the user. It will remain in
  /// this state until the update is complete.
  static const updating = LifecycleState('UPDATING');

  /// The resource has been marked for creation by the user. It will remain in
  /// this state until the creation is complete.
  static const creating = LifecycleState('CREATING');

  /// The resource is in an INTERNAL error state.
  static const failed = LifecycleState('FAILED');

  /// The default value for [LifecycleState].
  static const $default = lifecycleStateUnspecified;

  const LifecycleState(super.value);

  factory LifecycleState.fromJson(Object? json) =>
      LifecycleState(json as String);

  bool get isNotDefault => this != $default;

  @override
  String toString() => 'LifecycleState.$value';
}

/// IndexType is used for custom indexing. It describes the type of an indexed
/// field.
final class IndexType extends ProtoEnum {
  /// The index's type is unspecified.
  static const indexTypeUnspecified = IndexType('INDEX_TYPE_UNSPECIFIED');

  /// The index is a string-type index.
  static const indexTypeString = IndexType('INDEX_TYPE_STRING');

  /// The index is a integer-type index.
  static const indexTypeInteger = IndexType('INDEX_TYPE_INTEGER');

  /// The default value for [IndexType].
  static const $default = indexTypeUnspecified;

  const IndexType(super.value);

  factory IndexType.fromJson(Object? json) => IndexType(json as String);

  bool get isNotDefault => this != $default;

  @override
  String toString() => 'IndexType.$value';
}
