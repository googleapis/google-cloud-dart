// Copyright 2026 Google LLC
//
// Licensed under the Apache License, Version 2.0 (the "License");
// you may not use this file except in compliance with the License.
// You may obtain a copy of the License at
//
//     https://www.apache.org/licenses/LICENSE-2.0
//
// Unless required by applicable law or agreed to in writing, software
// distributed under the License is distributed on an "AS IS" BASIS,
// WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
// See the License for the specific language governing permissions and
// limitations under the License.
//
// Code generated by sidekick. DO NOT EDIT.

/// The Google Cloud client for the Cloud Tasks API.
///
/// Manages the execution of large numbers of distributed requests.
///
/// @docImport 'package:google_cloud_rpc/exceptions.dart';
library;

// ignore_for_file: camel_case_types
// ignore_for_file: comment_references
// ignore_for_file: constant_identifier_names
// ignore_for_file: implementation_imports
// ignore_for_file: lines_longer_than_80_chars
// ignore_for_file: non_constant_identifier_names
// ignore_for_file: unintended_html_in_doc_comment

import 'package:google_cloud_iam_v1/iam.dart';
import 'package:google_cloud_location/location.dart';
import 'package:google_cloud_protobuf/protobuf.dart';
import 'package:google_cloud_protobuf/src/encoding.dart';
import 'package:google_cloud_rpc/rpc.dart';
import 'package:google_cloud_rpc/service_client.dart';
import 'package:http/http.dart' as http;

import 'version.dart';

const _apiKeys = ['GOOGLE_API_KEY'];

/// Cloud Tasks allows developers to manage the execution of background
/// work in their applications.
final class CloudTasks {
  static const _defaultHost = 'cloudtasks.googleapis.com';
  final Uri _endPoint;

  final ServiceClient _client;

  /// Creates a `CloudTasks` using [client] for transport.
  ///
  /// The provided [http.Client] must be configured to provide whatever
  /// authentication is required by `CloudTasks`. You can do that using
  /// [`package:googleapis_auth`](https://pub.dev/packages/googleapis_auth).
  ///
  /// If [endPoint] is provided then its `scheme`, `host`, and `port` are
  /// used for all API requests. For example, `Uri.http('127.0.0.1:8080')`
  /// could be used to force the `Firestore` service to communicate with the
  /// local emulator.
  ///
  /// If [gcclVersion] is set then `gccl/<version>` will be included in the
  /// `x-google-api-client` header. This argument is only meant for use by
  /// hand-written official Google Cloud API clients.
  CloudTasks({required http.Client client, Uri? endPoint, String? gcclVersion})
    : _client = ServiceClient(
        client: client,
        gapicVersion: packageVersion,
        gcclVersion: gcclVersion,
      ),
      _endPoint = endPoint == null
          ? Uri.https(_defaultHost, '')
          : Uri(
              scheme: endPoint.scheme,
              host: endPoint.host,
              port: endPoint.port,
            );

  /// Creates a `CloudTasks` that does authentication through an API key.
  ///
  /// If called without arguments, the API key is taken from these environment
  /// variables:
  ///
  /// - `GOOGLE_API_KEY`
  ///
  /// Throws [ConfigurationException] if called without arguments and none of
  /// the above environment variables are set. On the web,
  /// always throws [ConfigurationException] if called without arguments.
  ///
  /// See [API Keys Overview](https://cloud.google.com/api-keys/docs/overview).
  factory CloudTasks.fromApiKey([String? apiKey]) =>
      CloudTasks(client: httpClientFromApiKey(apiKey, _apiKeys));

  /// Lists queues.
  ///
  /// Queues are returned in lexicographical order.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<ListQueuesResponse> listQueues(ListQueuesRequest request) async {
    final url = _endPoint.replace(
      path: '/v2/${request.parent}/queues',
      queryParameters: {
        if (request.filter case final $1 when $1.isNotDefault) 'filter': $1,
        if (request.pageSize case final $1 when $1.isNotDefault)
          'pageSize': '${$1}',
        if (request.pageToken case final $1 when $1.isNotDefault)
          'pageToken': $1,
      },
    );
    final response = await _client.get(url);
    return ListQueuesResponse.fromJson(response);
  }

  /// Gets a queue.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<Queue> getQueue(GetQueueRequest request) async {
    final url = _endPoint.replace(path: '/v2/${request.name}');
    final response = await _client.get(url);
    return Queue.fromJson(response);
  }

  /// Creates a queue.
  ///
  /// Queues created with this method allow tasks to live for a maximum of 31
  /// days. After a task is 31 days old, the task will be deleted regardless of
  /// whether it was dispatched or not.
  ///
  /// WARNING: Using this method may have unintended side effects if you are
  /// using an App Engine `queue.yaml` or `queue.xml` file to manage your queues.
  /// Read
  /// [Overview of Queue Management and
  /// queue.yaml](https://cloud.google.com/tasks/docs/queue-yaml) before using
  /// this method.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<Queue> createQueue(CreateQueueRequest request) async {
    final url = _endPoint.replace(path: '/v2/${request.parent}/queues');
    final response = await _client.post(url, body: request.queue);
    return Queue.fromJson(response);
  }

  /// Updates a queue.
  ///
  /// This method creates the queue if it does not exist and updates
  /// the queue if it does exist.
  ///
  /// Queues created with this method allow tasks to live for a maximum of 31
  /// days. After a task is 31 days old, the task will be deleted regardless of
  /// whether it was dispatched or not.
  ///
  /// WARNING: Using this method may have unintended side effects if you are
  /// using an App Engine `queue.yaml` or `queue.xml` file to manage your queues.
  /// Read
  /// [Overview of Queue Management and
  /// queue.yaml](https://cloud.google.com/tasks/docs/queue-yaml) before using
  /// this method.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<Queue> updateQueue(UpdateQueueRequest request) async {
    final url = _endPoint.replace(
      path: '/v2/${request.queue!.name}',
      queryParameters: {
        if (request.updateMask case final $1?) 'updateMask': $1.toJson(),
      },
    );
    final response = await _client.patch(url, body: request.queue);
    return Queue.fromJson(response);
  }

  /// Deletes a queue.
  ///
  /// This command will delete the queue even if it has tasks in it.
  ///
  /// Note: If you delete a queue, a queue with the same name can't be created
  /// for 7 days.
  ///
  /// WARNING: Using this method may have unintended side effects if you are
  /// using an App Engine `queue.yaml` or `queue.xml` file to manage your queues.
  /// Read
  /// [Overview of Queue Management and
  /// queue.yaml](https://cloud.google.com/tasks/docs/queue-yaml) before using
  /// this method.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<void> deleteQueue(DeleteQueueRequest request) async {
    final url = _endPoint.replace(path: '/v2/${request.name}');
    await _client.delete(url);
  }

  /// Purges a queue by deleting all of its tasks.
  ///
  /// All tasks created before this method is called are permanently deleted.
  ///
  /// Purge operations can take up to one minute to take effect. Tasks
  /// might be dispatched before the purge takes effect. A purge is irreversible.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<Queue> purgeQueue(PurgeQueueRequest request) async {
    final url = _endPoint.replace(path: '/v2/${request.name}:purge');
    final response = await _client.post(url, body: request);
    return Queue.fromJson(response);
  }

  /// Pauses the queue.
  ///
  /// If a queue is paused then the system will stop dispatching tasks
  /// until the queue is resumed via
  /// `ResumeQueue`. Tasks can
  /// still be added when the queue is paused. A queue is paused if its
  /// `state` is
  /// `PAUSED`.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<Queue> pauseQueue(PauseQueueRequest request) async {
    final url = _endPoint.replace(path: '/v2/${request.name}:pause');
    final response = await _client.post(url, body: request);
    return Queue.fromJson(response);
  }

  /// Resume a queue.
  ///
  /// This method resumes a queue after it has been
  /// `PAUSED` or
  /// `DISABLED`. The state of a
  /// queue is stored in the queue's `state`;
  /// after calling this method it will be set to
  /// `RUNNING`.
  ///
  /// WARNING: Resuming many high-QPS queues at the same time can
  /// lead to target overloading. If you are resuming high-QPS
  /// queues, follow the 500/50/5 pattern described in
  /// [Managing Cloud Tasks Scaling
  /// Risks](https://cloud.google.com/tasks/docs/manage-cloud-task-scaling).
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<Queue> resumeQueue(ResumeQueueRequest request) async {
    final url = _endPoint.replace(path: '/v2/${request.name}:resume');
    final response = await _client.post(url, body: request);
    return Queue.fromJson(response);
  }

  /// Gets the access control policy for a `Queue`.
  /// Returns an empty policy if the resource exists and does not have a policy
  /// set.
  ///
  /// Authorization requires the following
  /// [Google IAM](https://cloud.google.com/iam) permission on the specified
  /// resource parent:
  ///
  /// * `cloudtasks.queues.getIamPolicy`
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<Policy> getIamPolicy(GetIamPolicyRequest request) async {
    final url = _endPoint.replace(path: '/v2/${request.resource}:getIamPolicy');
    final response = await _client.post(url, body: request);
    return Policy.fromJson(response);
  }

  /// Sets the access control policy for a `Queue`.
  /// Replaces any existing policy.
  ///
  /// Note: The Cloud Console does not check queue-level IAM permissions yet.
  /// Project-level permissions are required to use the Cloud Console.
  ///
  /// Authorization requires the following
  /// [Google IAM](https://cloud.google.com/iam) permission on the specified
  /// resource parent:
  ///
  /// * `cloudtasks.queues.setIamPolicy`
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<Policy> setIamPolicy(SetIamPolicyRequest request) async {
    final url = _endPoint.replace(path: '/v2/${request.resource}:setIamPolicy');
    final response = await _client.post(url, body: request);
    return Policy.fromJson(response);
  }

  /// Returns permissions that a caller has on a
  /// `Queue`. If the resource does not exist, this
  /// will return an empty set of permissions, not a
  /// `NOT_FOUND` error.
  ///
  /// Note: This operation is designed to be used for building permission-aware
  /// UIs and command-line tools, not for authorization checking. This operation
  /// may "fail open" without warning.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<TestIamPermissionsResponse> testIamPermissions(
    TestIamPermissionsRequest request,
  ) async {
    final url = _endPoint.replace(
      path: '/v2/${request.resource}:testIamPermissions',
    );
    final response = await _client.post(url, body: request);
    return TestIamPermissionsResponse.fromJson(response);
  }

  /// Lists the tasks in a queue.
  ///
  /// By default, only the `BASIC` view is
  /// retrieved due to performance considerations;
  /// `response_view`
  /// controls the subset of information which is returned.
  ///
  /// The tasks may be returned in any order. The ordering may change at any
  /// time.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<ListTasksResponse> listTasks(ListTasksRequest request) async {
    final url = _endPoint.replace(
      path: '/v2/${request.parent}/tasks',
      queryParameters: {
        if (request.responseView case final $1 when $1.isNotDefault)
          'responseView': $1.value,
        if (request.pageSize case final $1 when $1.isNotDefault)
          'pageSize': '${$1}',
        if (request.pageToken case final $1 when $1.isNotDefault)
          'pageToken': $1,
      },
    );
    final response = await _client.get(url);
    return ListTasksResponse.fromJson(response);
  }

  /// Gets a task.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<Task> getTask(GetTaskRequest request) async {
    final url = _endPoint.replace(
      path: '/v2/${request.name}',
      queryParameters: {
        if (request.responseView case final $1 when $1.isNotDefault)
          'responseView': $1.value,
      },
    );
    final response = await _client.get(url);
    return Task.fromJson(response);
  }

  /// Creates a task and adds it to a queue.
  ///
  /// Tasks cannot be updated after creation; there is no UpdateTask command.
  ///
  /// * The maximum task size is 100KB.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<Task> createTask(CreateTaskRequest request) async {
    final url = _endPoint.replace(path: '/v2/${request.parent}/tasks');
    final response = await _client.post(url, body: request);
    return Task.fromJson(response);
  }

  /// Deletes a task.
  ///
  /// A task can be deleted if it is scheduled or dispatched. A task
  /// cannot be deleted if it has executed successfully or permanently
  /// failed.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<void> deleteTask(DeleteTaskRequest request) async {
    final url = _endPoint.replace(path: '/v2/${request.name}');
    await _client.delete(url);
  }

  /// Forces a task to run now.
  ///
  /// When this method is called, Cloud Tasks will dispatch the task, even if
  /// the task is already running, the queue has reached its
  /// `RateLimits` or is
  /// `PAUSED`.
  ///
  /// This command is meant to be used for manual debugging. For
  /// example, `RunTask` can be used to
  /// retry a failed task after a fix has been made or to manually force a task
  /// to be dispatched now.
  ///
  /// The dispatched task is returned. That is, the task that is returned
  /// contains the `status` after the task is dispatched but
  /// before the task is received by its target.
  ///
  /// If Cloud Tasks receives a successful response from the task's
  /// target, then the task will be deleted; otherwise the task's
  /// `schedule_time` will be reset to
  /// the time that `RunTask` was
  /// called plus the retry delay specified in the queue's
  /// `RetryConfig`.
  ///
  /// `RunTask` returns
  /// `NOT_FOUND` when it is called on a
  /// task that has already succeeded or permanently failed.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<Task> runTask(RunTaskRequest request) async {
    final url = _endPoint.replace(path: '/v2/${request.name}:run');
    final response = await _client.post(url, body: request);
    return Task.fromJson(response);
  }

  /// Lists information about the supported locations for this service.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<ListLocationsResponse> listLocations(
    ListLocationsRequest request,
  ) async {
    final url = _endPoint.replace(
      path: '/v2/${request.name}/locations',
      queryParameters: {
        if (request.filter case final $1 when $1.isNotDefault) 'filter': $1,
        if (request.pageSize case final $1 when $1.isNotDefault)
          'pageSize': '${$1}',
        if (request.pageToken case final $1 when $1.isNotDefault)
          'pageToken': $1,
      },
    );
    final response = await _client.get(url);
    return ListLocationsResponse.fromJson(response);
  }

  /// Gets information about a location.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  Future<Location> getLocation(GetLocationRequest request) async {
    final url = _endPoint.replace(path: '/v2/${request.name}');
    final response = await _client.get(url);
    return Location.fromJson(response);
  }

  /// Closes the client and cleans up any resources associated with it.
  ///
  /// Once [close] is called, no other methods should be called.
  void close() => _client.close();
}

/// Testing fake for [CloudTasks].
base class FakeCloudTasks implements CloudTasks {
  final Future<ListQueuesResponse> Function(ListQueuesRequest request)?
  _listQueues;
  final Future<Queue> Function(GetQueueRequest request)? _getQueue;
  final Future<Queue> Function(CreateQueueRequest request)? _createQueue;
  final Future<Queue> Function(UpdateQueueRequest request)? _updateQueue;
  final Future<void> Function(DeleteQueueRequest request)? _deleteQueue;
  final Future<Queue> Function(PurgeQueueRequest request)? _purgeQueue;
  final Future<Queue> Function(PauseQueueRequest request)? _pauseQueue;
  final Future<Queue> Function(ResumeQueueRequest request)? _resumeQueue;
  final Future<Policy> Function(GetIamPolicyRequest request)? _getIamPolicy;
  final Future<Policy> Function(SetIamPolicyRequest request)? _setIamPolicy;
  final Future<TestIamPermissionsResponse> Function(
    TestIamPermissionsRequest request,
  )?
  _testIamPermissions;
  final Future<ListTasksResponse> Function(ListTasksRequest request)?
  _listTasks;
  final Future<Task> Function(GetTaskRequest request)? _getTask;
  final Future<Task> Function(CreateTaskRequest request)? _createTask;
  final Future<void> Function(DeleteTaskRequest request)? _deleteTask;
  final Future<Task> Function(RunTaskRequest request)? _runTask;
  final Future<ListLocationsResponse> Function(ListLocationsRequest request)?
  _listLocations;
  final Future<Location> Function(GetLocationRequest request)? _getLocation;

  @override
  Uri get _endPoint => throw UnsupportedError('_endPoint');
  @override
  ServiceClient get _client => throw UnsupportedError('_client');

  bool isClosed = false;

  FakeCloudTasks({
    Future<ListQueuesResponse> Function(ListQueuesRequest request)? listQueues,
    Future<Queue> Function(GetQueueRequest request)? getQueue,
    Future<Queue> Function(CreateQueueRequest request)? createQueue,
    Future<Queue> Function(UpdateQueueRequest request)? updateQueue,
    Future<void> Function(DeleteQueueRequest request)? deleteQueue,
    Future<Queue> Function(PurgeQueueRequest request)? purgeQueue,
    Future<Queue> Function(PauseQueueRequest request)? pauseQueue,
    Future<Queue> Function(ResumeQueueRequest request)? resumeQueue,
    Future<Policy> Function(GetIamPolicyRequest request)? getIamPolicy,
    Future<Policy> Function(SetIamPolicyRequest request)? setIamPolicy,
    Future<TestIamPermissionsResponse> Function(
      TestIamPermissionsRequest request,
    )?
    testIamPermissions,
    Future<ListTasksResponse> Function(ListTasksRequest request)? listTasks,
    Future<Task> Function(GetTaskRequest request)? getTask,
    Future<Task> Function(CreateTaskRequest request)? createTask,
    Future<void> Function(DeleteTaskRequest request)? deleteTask,
    Future<Task> Function(RunTaskRequest request)? runTask,
    Future<ListLocationsResponse> Function(ListLocationsRequest request)?
    listLocations,
    Future<Location> Function(GetLocationRequest request)? getLocation,
  }) : _listQueues = listQueues,
       _getQueue = getQueue,
       _createQueue = createQueue,
       _updateQueue = updateQueue,
       _deleteQueue = deleteQueue,
       _purgeQueue = purgeQueue,
       _pauseQueue = pauseQueue,
       _resumeQueue = resumeQueue,
       _getIamPolicy = getIamPolicy,
       _setIamPolicy = setIamPolicy,
       _testIamPermissions = testIamPermissions,
       _listTasks = listTasks,
       _getTask = getTask,
       _createTask = createTask,
       _deleteTask = deleteTask,
       _runTask = runTask,
       _listLocations = listLocations,
       _getLocation = getLocation;

  /// Lists queues.
  ///
  /// Queues are returned in lexicographical order.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<ListQueuesResponse> listQueues(ListQueuesRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_listQueues case final listQueues?) {
      return listQueues(request);
    }
    throw UnsupportedError('listQueues');
  }

  /// Gets a queue.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<Queue> getQueue(GetQueueRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_getQueue case final getQueue?) {
      return getQueue(request);
    }
    throw UnsupportedError('getQueue');
  }

  /// Creates a queue.
  ///
  /// Queues created with this method allow tasks to live for a maximum of 31
  /// days. After a task is 31 days old, the task will be deleted regardless of
  /// whether it was dispatched or not.
  ///
  /// WARNING: Using this method may have unintended side effects if you are
  /// using an App Engine `queue.yaml` or `queue.xml` file to manage your queues.
  /// Read
  /// [Overview of Queue Management and
  /// queue.yaml](https://cloud.google.com/tasks/docs/queue-yaml) before using
  /// this method.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<Queue> createQueue(CreateQueueRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_createQueue case final createQueue?) {
      return createQueue(request);
    }
    throw UnsupportedError('createQueue');
  }

  /// Updates a queue.
  ///
  /// This method creates the queue if it does not exist and updates
  /// the queue if it does exist.
  ///
  /// Queues created with this method allow tasks to live for a maximum of 31
  /// days. After a task is 31 days old, the task will be deleted regardless of
  /// whether it was dispatched or not.
  ///
  /// WARNING: Using this method may have unintended side effects if you are
  /// using an App Engine `queue.yaml` or `queue.xml` file to manage your queues.
  /// Read
  /// [Overview of Queue Management and
  /// queue.yaml](https://cloud.google.com/tasks/docs/queue-yaml) before using
  /// this method.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<Queue> updateQueue(UpdateQueueRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_updateQueue case final updateQueue?) {
      return updateQueue(request);
    }
    throw UnsupportedError('updateQueue');
  }

  /// Deletes a queue.
  ///
  /// This command will delete the queue even if it has tasks in it.
  ///
  /// Note: If you delete a queue, a queue with the same name can't be created
  /// for 7 days.
  ///
  /// WARNING: Using this method may have unintended side effects if you are
  /// using an App Engine `queue.yaml` or `queue.xml` file to manage your queues.
  /// Read
  /// [Overview of Queue Management and
  /// queue.yaml](https://cloud.google.com/tasks/docs/queue-yaml) before using
  /// this method.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<void> deleteQueue(DeleteQueueRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_deleteQueue case final deleteQueue?) {
      return deleteQueue(request);
    }
    throw UnsupportedError('deleteQueue');
  }

  /// Purges a queue by deleting all of its tasks.
  ///
  /// All tasks created before this method is called are permanently deleted.
  ///
  /// Purge operations can take up to one minute to take effect. Tasks
  /// might be dispatched before the purge takes effect. A purge is irreversible.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<Queue> purgeQueue(PurgeQueueRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_purgeQueue case final purgeQueue?) {
      return purgeQueue(request);
    }
    throw UnsupportedError('purgeQueue');
  }

  /// Pauses the queue.
  ///
  /// If a queue is paused then the system will stop dispatching tasks
  /// until the queue is resumed via
  /// `ResumeQueue`. Tasks can
  /// still be added when the queue is paused. A queue is paused if its
  /// `state` is
  /// `PAUSED`.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<Queue> pauseQueue(PauseQueueRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_pauseQueue case final pauseQueue?) {
      return pauseQueue(request);
    }
    throw UnsupportedError('pauseQueue');
  }

  /// Resume a queue.
  ///
  /// This method resumes a queue after it has been
  /// `PAUSED` or
  /// `DISABLED`. The state of a
  /// queue is stored in the queue's `state`;
  /// after calling this method it will be set to
  /// `RUNNING`.
  ///
  /// WARNING: Resuming many high-QPS queues at the same time can
  /// lead to target overloading. If you are resuming high-QPS
  /// queues, follow the 500/50/5 pattern described in
  /// [Managing Cloud Tasks Scaling
  /// Risks](https://cloud.google.com/tasks/docs/manage-cloud-task-scaling).
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<Queue> resumeQueue(ResumeQueueRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_resumeQueue case final resumeQueue?) {
      return resumeQueue(request);
    }
    throw UnsupportedError('resumeQueue');
  }

  /// Gets the access control policy for a `Queue`.
  /// Returns an empty policy if the resource exists and does not have a policy
  /// set.
  ///
  /// Authorization requires the following
  /// [Google IAM](https://cloud.google.com/iam) permission on the specified
  /// resource parent:
  ///
  /// * `cloudtasks.queues.getIamPolicy`
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<Policy> getIamPolicy(GetIamPolicyRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_getIamPolicy case final getIamPolicy?) {
      return getIamPolicy(request);
    }
    throw UnsupportedError('getIamPolicy');
  }

  /// Sets the access control policy for a `Queue`.
  /// Replaces any existing policy.
  ///
  /// Note: The Cloud Console does not check queue-level IAM permissions yet.
  /// Project-level permissions are required to use the Cloud Console.
  ///
  /// Authorization requires the following
  /// [Google IAM](https://cloud.google.com/iam) permission on the specified
  /// resource parent:
  ///
  /// * `cloudtasks.queues.setIamPolicy`
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<Policy> setIamPolicy(SetIamPolicyRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_setIamPolicy case final setIamPolicy?) {
      return setIamPolicy(request);
    }
    throw UnsupportedError('setIamPolicy');
  }

  /// Returns permissions that a caller has on a
  /// `Queue`. If the resource does not exist, this
  /// will return an empty set of permissions, not a
  /// `NOT_FOUND` error.
  ///
  /// Note: This operation is designed to be used for building permission-aware
  /// UIs and command-line tools, not for authorization checking. This operation
  /// may "fail open" without warning.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<TestIamPermissionsResponse> testIamPermissions(
    TestIamPermissionsRequest request,
  ) async {
    if (isClosed) throw StateError('Service is closed');

    if (_testIamPermissions case final testIamPermissions?) {
      return testIamPermissions(request);
    }
    throw UnsupportedError('testIamPermissions');
  }

  /// Lists the tasks in a queue.
  ///
  /// By default, only the `BASIC` view is
  /// retrieved due to performance considerations;
  /// `response_view`
  /// controls the subset of information which is returned.
  ///
  /// The tasks may be returned in any order. The ordering may change at any
  /// time.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<ListTasksResponse> listTasks(ListTasksRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_listTasks case final listTasks?) {
      return listTasks(request);
    }
    throw UnsupportedError('listTasks');
  }

  /// Gets a task.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<Task> getTask(GetTaskRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_getTask case final getTask?) {
      return getTask(request);
    }
    throw UnsupportedError('getTask');
  }

  /// Creates a task and adds it to a queue.
  ///
  /// Tasks cannot be updated after creation; there is no UpdateTask command.
  ///
  /// * The maximum task size is 100KB.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<Task> createTask(CreateTaskRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_createTask case final createTask?) {
      return createTask(request);
    }
    throw UnsupportedError('createTask');
  }

  /// Deletes a task.
  ///
  /// A task can be deleted if it is scheduled or dispatched. A task
  /// cannot be deleted if it has executed successfully or permanently
  /// failed.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<void> deleteTask(DeleteTaskRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_deleteTask case final deleteTask?) {
      return deleteTask(request);
    }
    throw UnsupportedError('deleteTask');
  }

  /// Forces a task to run now.
  ///
  /// When this method is called, Cloud Tasks will dispatch the task, even if
  /// the task is already running, the queue has reached its
  /// `RateLimits` or is
  /// `PAUSED`.
  ///
  /// This command is meant to be used for manual debugging. For
  /// example, `RunTask` can be used to
  /// retry a failed task after a fix has been made or to manually force a task
  /// to be dispatched now.
  ///
  /// The dispatched task is returned. That is, the task that is returned
  /// contains the `status` after the task is dispatched but
  /// before the task is received by its target.
  ///
  /// If Cloud Tasks receives a successful response from the task's
  /// target, then the task will be deleted; otherwise the task's
  /// `schedule_time` will be reset to
  /// the time that `RunTask` was
  /// called plus the retry delay specified in the queue's
  /// `RetryConfig`.
  ///
  /// `RunTask` returns
  /// `NOT_FOUND` when it is called on a
  /// task that has already succeeded or permanently failed.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<Task> runTask(RunTaskRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_runTask case final runTask?) {
      return runTask(request);
    }
    throw UnsupportedError('runTask');
  }

  /// Lists information about the supported locations for this service.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<ListLocationsResponse> listLocations(
    ListLocationsRequest request,
  ) async {
    if (isClosed) throw StateError('Service is closed');

    if (_listLocations case final listLocations?) {
      return listLocations(request);
    }
    throw UnsupportedError('listLocations');
  }

  /// Gets information about a location.
  ///
  /// Throws a [http.ClientException] if there were problems communicating with
  /// the API service. Throws a [ServiceException] if the API method failed for
  /// any reason.
  @override
  Future<Location> getLocation(GetLocationRequest request) async {
    if (isClosed) throw StateError('Service is closed');

    if (_getLocation case final getLocation?) {
      return getLocation(request);
    }
    throw UnsupportedError('getLocation');
  }

  @override
  void close() {
    isClosed = true;
  }
}

/// Request message for
/// `ListQueues`.
final class ListQueuesRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.ListQueuesRequest';

  /// Required. The location name.
  /// For example: `projects/PROJECT_ID/locations/LOCATION_ID`
  final String parent;

  /// `filter` can be used to specify a subset of queues. Any
  /// `Queue` field can be used as a filter and
  /// several operators as supported. For example: `<=, <, >=, >, !=, =, :`. The
  /// filter syntax is the same as described in [Stackdriver's Advanced Logs
  /// Filters](https://cloud.google.com/logging/docs/view/advanced_filters).
  ///
  /// Sample filter "state: PAUSED".
  ///
  /// Note that using filters might cause fewer queues than the
  /// requested page_size to be returned.
  final String filter;

  /// Requested page size.
  ///
  /// The maximum page size is 9800. If unspecified, the page size will
  /// be the maximum. Fewer queues than requested might be returned,
  /// even if more queues exist; use the
  /// `next_page_token`
  /// in the response to determine if more queues exist.
  final int pageSize;

  /// A token identifying the page of results to return.
  ///
  /// To request the first page results, page_token must be empty. To
  /// request the next page of results, page_token must be the value of
  /// `next_page_token`
  /// returned from the previous call to
  /// `ListQueues` method. It is an
  /// error to switch the value of the
  /// `filter` while iterating
  /// through pages.
  final String pageToken;

  ListQueuesRequest({
    required this.parent,
    this.filter = '',
    this.pageSize = 0,
    this.pageToken = '',
  }) : super(fullyQualifiedName);

  factory ListQueuesRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListQueuesRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      filter: switch (json['filter']) {
        null => '',
        Object $1 => decodeString($1),
      },
      pageSize: switch (json['pageSize']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
      pageToken: switch (json['pageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (filter.isNotDefault) 'filter': filter,
    if (pageSize.isNotDefault) 'pageSize': pageSize,
    if (pageToken.isNotDefault) 'pageToken': pageToken,
  };

  @override
  String toString() {
    final $contents = [
      'parent=$parent',
      'filter=$filter',
      'pageSize=$pageSize',
      'pageToken=$pageToken',
    ].join(',');
    return 'ListQueuesRequest(${$contents})';
  }
}

/// Response message for
/// `ListQueues`.
final class ListQueuesResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.ListQueuesResponse';

  /// The list of queues.
  final List<Queue> queues;

  /// A token to retrieve next page of results.
  ///
  /// To return the next page of results, call
  /// `ListQueues` with this value
  /// as the `page_token`.
  ///
  /// If the next_page_token is empty, there are no more results.
  ///
  /// The page token is valid for only 2 hours.
  final String nextPageToken;

  ListQueuesResponse({this.queues = const [], this.nextPageToken = ''})
    : super(fullyQualifiedName);

  factory ListQueuesResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListQueuesResponse(
      queues: switch (json['queues']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) Queue.fromJson(i)],
        _ => throw const FormatException('"queues" is not a list'),
      },
      nextPageToken: switch (json['nextPageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (queues.isNotDefault) 'queues': [for (final i in queues) i.toJson()],
    if (nextPageToken.isNotDefault) 'nextPageToken': nextPageToken,
  };

  @override
  String toString() {
    final $contents = ['nextPageToken=$nextPageToken'].join(',');
    return 'ListQueuesResponse(${$contents})';
  }
}

/// Request message for `GetQueue`.
final class GetQueueRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.GetQueueRequest';

  /// Required. The resource name of the queue. For example:
  /// `projects/PROJECT_ID/locations/LOCATION_ID/queues/QUEUE_ID`
  final String name;

  GetQueueRequest({required this.name}) : super(fullyQualifiedName);

  factory GetQueueRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return GetQueueRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final $contents = ['name=$name'].join(',');
    return 'GetQueueRequest(${$contents})';
  }
}

/// Request message for
/// `CreateQueue`.
final class CreateQueueRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.CreateQueueRequest';

  /// Required. The location name in which the queue will be created.
  /// For example: `projects/PROJECT_ID/locations/LOCATION_ID`
  ///
  /// The list of allowed locations can be obtained by calling Cloud
  /// Tasks' implementation of
  /// `ListLocations`.
  final String parent;

  /// Required. The queue to create.
  ///
  /// [Queue's name][google.cloud.tasks.v2.Queue.name] cannot be the same as an
  /// existing queue.
  final Queue? queue;

  CreateQueueRequest({required this.parent, required this.queue})
    : super(fullyQualifiedName);

  factory CreateQueueRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return CreateQueueRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      queue: switch (json['queue']) {
        null => null,
        Object $1 => Queue.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {'parent': parent, 'queue': ?queue?.toJson()};

  @override
  String toString() {
    final $contents = ['parent=$parent'].join(',');
    return 'CreateQueueRequest(${$contents})';
  }
}

/// Request message for
/// `UpdateQueue`.
final class UpdateQueueRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.UpdateQueueRequest';

  /// Required. The queue to create or update.
  ///
  /// The queue's `name` must be specified.
  ///
  /// Output only fields cannot be modified using UpdateQueue.
  /// Any value specified for an output only field will be ignored.
  /// The queue's `name` cannot be changed.
  final Queue? queue;

  /// A mask used to specify which fields of the queue are being updated.
  ///
  /// If empty, then all fields will be updated.
  final FieldMask? updateMask;

  UpdateQueueRequest({required this.queue, this.updateMask})
    : super(fullyQualifiedName);

  factory UpdateQueueRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return UpdateQueueRequest(
      queue: switch (json['queue']) {
        null => null,
        Object $1 => Queue.fromJson($1),
      },
      updateMask: switch (json['updateMask']) {
        null => null,
        Object $1 => FieldMask.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'queue': ?queue?.toJson(),
    'updateMask': ?updateMask?.toJson(),
  };

  @override
  String toString() => 'UpdateQueueRequest()';
}

/// Request message for
/// `DeleteQueue`.
final class DeleteQueueRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.DeleteQueueRequest';

  /// Required. The queue name. For example:
  /// `projects/PROJECT_ID/locations/LOCATION_ID/queues/QUEUE_ID`
  final String name;

  DeleteQueueRequest({required this.name}) : super(fullyQualifiedName);

  factory DeleteQueueRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return DeleteQueueRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final $contents = ['name=$name'].join(',');
    return 'DeleteQueueRequest(${$contents})';
  }
}

/// Request message for
/// `PurgeQueue`.
final class PurgeQueueRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.PurgeQueueRequest';

  /// Required. The queue name. For example:
  /// `projects/PROJECT_ID/location/LOCATION_ID/queues/QUEUE_ID`
  final String name;

  PurgeQueueRequest({required this.name}) : super(fullyQualifiedName);

  factory PurgeQueueRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return PurgeQueueRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final $contents = ['name=$name'].join(',');
    return 'PurgeQueueRequest(${$contents})';
  }
}

/// Request message for
/// `PauseQueue`.
final class PauseQueueRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.PauseQueueRequest';

  /// Required. The queue name. For example:
  /// `projects/PROJECT_ID/location/LOCATION_ID/queues/QUEUE_ID`
  final String name;

  PauseQueueRequest({required this.name}) : super(fullyQualifiedName);

  factory PauseQueueRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return PauseQueueRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final $contents = ['name=$name'].join(',');
    return 'PauseQueueRequest(${$contents})';
  }
}

/// Request message for
/// `ResumeQueue`.
final class ResumeQueueRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.ResumeQueueRequest';

  /// Required. The queue name. For example:
  /// `projects/PROJECT_ID/location/LOCATION_ID/queues/QUEUE_ID`
  final String name;

  ResumeQueueRequest({required this.name}) : super(fullyQualifiedName);

  factory ResumeQueueRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ResumeQueueRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final $contents = ['name=$name'].join(',');
    return 'ResumeQueueRequest(${$contents})';
  }
}

/// Request message for listing tasks using
/// `ListTasks`.
final class ListTasksRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.ListTasksRequest';

  /// Required. The queue name. For example:
  /// `projects/PROJECT_ID/locations/LOCATION_ID/queues/QUEUE_ID`
  final String parent;

  /// The response_view specifies which subset of the
  /// `Task` will be returned.
  ///
  /// By default response_view is `BASIC`;
  /// not all information is retrieved by default because some data, such as
  /// payloads, might be desirable to return only when needed because
  /// of its large size or because of the sensitivity of data that it
  /// contains.
  ///
  /// Authorization for `FULL` requires
  /// `cloudtasks.tasks.fullView` [Google IAM](https://cloud.google.com/iam/)
  /// permission on the `Task` resource.
  final Task_View responseView;

  /// Maximum page size.
  ///
  /// Fewer tasks than requested might be returned, even if more tasks exist; use
  /// `next_page_token`
  /// in the response to determine if more tasks exist.
  ///
  /// The maximum page size is 1000. If unspecified, the page size will be the
  /// maximum.
  final int pageSize;

  /// A token identifying the page of results to return.
  ///
  /// To request the first page results, page_token must be empty. To
  /// request the next page of results, page_token must be the value of
  /// `next_page_token`
  /// returned from the previous call to
  /// `ListTasks` method.
  ///
  /// The page token is valid for only 2 hours.
  final String pageToken;

  ListTasksRequest({
    required this.parent,
    this.responseView = Task_View.$default,
    this.pageSize = 0,
    this.pageToken = '',
  }) : super(fullyQualifiedName);

  factory ListTasksRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListTasksRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      responseView: switch (json['responseView']) {
        null => Task_View.$default,
        Object $1 => Task_View.fromJson($1),
      },
      pageSize: switch (json['pageSize']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
      pageToken: switch (json['pageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    if (responseView.isNotDefault) 'responseView': responseView.toJson(),
    if (pageSize.isNotDefault) 'pageSize': pageSize,
    if (pageToken.isNotDefault) 'pageToken': pageToken,
  };

  @override
  String toString() {
    final $contents = [
      'parent=$parent',
      'responseView=$responseView',
      'pageSize=$pageSize',
      'pageToken=$pageToken',
    ].join(',');
    return 'ListTasksRequest(${$contents})';
  }
}

/// Response message for listing tasks using
/// `ListTasks`.
final class ListTasksResponse extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.ListTasksResponse';

  /// The list of tasks.
  final List<Task> tasks;

  /// A token to retrieve next page of results.
  ///
  /// To return the next page of results, call
  /// `ListTasks` with this value as
  /// the `page_token`.
  ///
  /// If the next_page_token is empty, there are no more results.
  final String nextPageToken;

  ListTasksResponse({this.tasks = const [], this.nextPageToken = ''})
    : super(fullyQualifiedName);

  factory ListTasksResponse.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return ListTasksResponse(
      tasks: switch (json['tasks']) {
        null => [],
        List<Object?> $1 => [for (final i in $1) Task.fromJson(i)],
        _ => throw const FormatException('"tasks" is not a list'),
      },
      nextPageToken: switch (json['nextPageToken']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (tasks.isNotDefault) 'tasks': [for (final i in tasks) i.toJson()],
    if (nextPageToken.isNotDefault) 'nextPageToken': nextPageToken,
  };

  @override
  String toString() {
    final $contents = ['nextPageToken=$nextPageToken'].join(',');
    return 'ListTasksResponse(${$contents})';
  }
}

/// Request message for getting a task using
/// `GetTask`.
final class GetTaskRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.GetTaskRequest';

  /// Required. The task name. For example:
  /// `projects/PROJECT_ID/locations/LOCATION_ID/queues/QUEUE_ID/tasks/TASK_ID`
  final String name;

  /// The response_view specifies which subset of the
  /// `Task` will be returned.
  ///
  /// By default response_view is `BASIC`;
  /// not all information is retrieved by default because some data, such as
  /// payloads, might be desirable to return only when needed because
  /// of its large size or because of the sensitivity of data that it
  /// contains.
  ///
  /// Authorization for `FULL` requires
  /// `cloudtasks.tasks.fullView` [Google IAM](https://cloud.google.com/iam/)
  /// permission on the `Task` resource.
  final Task_View responseView;

  GetTaskRequest({required this.name, this.responseView = Task_View.$default})
    : super(fullyQualifiedName);

  factory GetTaskRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return GetTaskRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      responseView: switch (json['responseView']) {
        null => Task_View.$default,
        Object $1 => Task_View.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'name': name,
    if (responseView.isNotDefault) 'responseView': responseView.toJson(),
  };

  @override
  String toString() {
    final $contents = ['name=$name', 'responseView=$responseView'].join(',');
    return 'GetTaskRequest(${$contents})';
  }
}

/// Request message for
/// `CreateTask`.
final class CreateTaskRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.CreateTaskRequest';

  /// Required. The queue name. For example:
  /// `projects/PROJECT_ID/locations/LOCATION_ID/queues/QUEUE_ID`
  ///
  /// The queue must already exist.
  final String parent;

  /// Required. The task to add.
  ///
  /// Task names have the following format:
  /// `projects/PROJECT_ID/locations/LOCATION_ID/queues/QUEUE_ID/tasks/TASK_ID`.
  /// The user can optionally specify a task
  /// `name`. If a name is not specified then
  /// the system will generate a random unique task id, which will be set in the
  /// task returned in the `response`.
  ///
  /// If `schedule_time` is not set or
  /// is in the past then Cloud Tasks will set it to the current time.
  ///
  /// Task De-duplication:
  ///
  /// Explicitly specifying a task ID enables task de-duplication.  If
  /// a task's ID is identical to that of an existing task or a task
  /// that was deleted or executed recently then the call will fail
  /// with `ALREADY_EXISTS`.
  /// If the task's queue was created using Cloud Tasks, then another task with
  /// the same name can't be created for ~1hour after the original task was
  /// deleted or executed. If the task's queue was created using queue.yaml or
  /// queue.xml, then another task with the same name can't be created
  /// for ~9days after the original task was deleted or executed.
  ///
  /// Because there is an extra lookup cost to identify duplicate task
  /// names, these `CreateTask`
  /// calls have significantly increased latency. Using hashed strings for the
  /// task id or for the prefix of the task id is recommended. Choosing task ids
  /// that are sequential or have sequential prefixes, for example using a
  /// timestamp, causes an increase in latency and error rates in all
  /// task commands. The infrastructure relies on an approximately
  /// uniform distribution of task ids to store and serve tasks
  /// efficiently.
  final Task? task;

  /// The response_view specifies which subset of the
  /// `Task` will be returned.
  ///
  /// By default response_view is `BASIC`;
  /// not all information is retrieved by default because some data, such as
  /// payloads, might be desirable to return only when needed because
  /// of its large size or because of the sensitivity of data that it
  /// contains.
  ///
  /// Authorization for `FULL` requires
  /// `cloudtasks.tasks.fullView` [Google IAM](https://cloud.google.com/iam/)
  /// permission on the `Task` resource.
  final Task_View responseView;

  CreateTaskRequest({
    required this.parent,
    required this.task,
    this.responseView = Task_View.$default,
  }) : super(fullyQualifiedName);

  factory CreateTaskRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return CreateTaskRequest(
      parent: switch (json['parent']) {
        null => '',
        Object $1 => decodeString($1),
      },
      task: switch (json['task']) {
        null => null,
        Object $1 => Task.fromJson($1),
      },
      responseView: switch (json['responseView']) {
        null => Task_View.$default,
        Object $1 => Task_View.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'parent': parent,
    'task': ?task?.toJson(),
    if (responseView.isNotDefault) 'responseView': responseView.toJson(),
  };

  @override
  String toString() {
    final $contents = [
      'parent=$parent',
      'responseView=$responseView',
    ].join(',');
    return 'CreateTaskRequest(${$contents})';
  }
}

/// Request message for deleting a task using
/// `DeleteTask`.
final class DeleteTaskRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.DeleteTaskRequest';

  /// Required. The task name. For example:
  /// `projects/PROJECT_ID/locations/LOCATION_ID/queues/QUEUE_ID/tasks/TASK_ID`
  final String name;

  DeleteTaskRequest({required this.name}) : super(fullyQualifiedName);

  factory DeleteTaskRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return DeleteTaskRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {'name': name};

  @override
  String toString() {
    final $contents = ['name=$name'].join(',');
    return 'DeleteTaskRequest(${$contents})';
  }
}

/// Request message for forcing a task to run now using
/// `RunTask`.
final class RunTaskRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.RunTaskRequest';

  /// Required. The task name. For example:
  /// `projects/PROJECT_ID/locations/LOCATION_ID/queues/QUEUE_ID/tasks/TASK_ID`
  final String name;

  /// The response_view specifies which subset of the
  /// `Task` will be returned.
  ///
  /// By default response_view is `BASIC`;
  /// not all information is retrieved by default because some data, such as
  /// payloads, might be desirable to return only when needed because
  /// of its large size or because of the sensitivity of data that it
  /// contains.
  ///
  /// Authorization for `FULL` requires
  /// `cloudtasks.tasks.fullView` [Google IAM](https://cloud.google.com/iam/)
  /// permission on the `Task` resource.
  final Task_View responseView;

  RunTaskRequest({required this.name, this.responseView = Task_View.$default})
    : super(fullyQualifiedName);

  factory RunTaskRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return RunTaskRequest(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      responseView: switch (json['responseView']) {
        null => Task_View.$default,
        Object $1 => Task_View.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'name': name,
    if (responseView.isNotDefault) 'responseView': responseView.toJson(),
  };

  @override
  String toString() {
    final $contents = ['name=$name', 'responseView=$responseView'].join(',');
    return 'RunTaskRequest(${$contents})';
  }
}

/// A queue is a container of related tasks. Queues are configured to manage
/// how those tasks are dispatched. Configurable properties include rate limits,
/// retry options, queue types, and others.
final class Queue extends ProtoMessage {
  static const String fullyQualifiedName = 'google.cloud.tasks.v2.Queue';

  /// Caller-specified and required in
  /// `CreateQueue`, after which it
  /// becomes output only.
  ///
  /// The queue name.
  ///
  /// The queue name must have the following format:
  /// `projects/PROJECT_ID/locations/LOCATION_ID/queues/QUEUE_ID`
  ///
  /// * `PROJECT_ID` can contain letters ([A-Za-z]), numbers ([0-9]),
  ///    hyphens (-), colons (:), or periods (.).
  ///    For more information, see
  ///    [Identifying
  ///    projects](https://cloud.google.com/resource-manager/docs/creating-managing-projects#identifying_projects)
  /// * `LOCATION_ID` is the canonical ID for the queue's location.
  ///    The list of available locations can be obtained by calling
  ///    `ListLocations`.
  ///    For more information, see https://cloud.google.com/about/locations/.
  /// * `QUEUE_ID` can contain letters ([A-Za-z]), numbers ([0-9]), or
  ///   hyphens (-). The maximum length is 100 characters.
  final String name;

  /// Overrides for
  /// [task-level
  /// app_engine_routing][google.cloud.tasks.v2.AppEngineHttpRequest.app_engine_routing].
  /// These settings apply only to
  /// [App Engine tasks][google.cloud.tasks.v2.AppEngineHttpRequest] in this
  /// queue. [Http tasks][google.cloud.tasks.v2.HttpRequest] are not affected.
  ///
  /// If set, `app_engine_routing_override` is used for all
  /// [App Engine tasks][google.cloud.tasks.v2.AppEngineHttpRequest] in the
  /// queue, no matter what the setting is for the [task-level
  /// app_engine_routing][google.cloud.tasks.v2.AppEngineHttpRequest.app_engine_routing].
  final AppEngineRouting? appEngineRoutingOverride;

  /// Rate limits for task dispatches.
  ///
  /// `rate_limits` and
  /// `retry_config` are related
  /// because they both control task attempts. However they control task attempts
  /// in different ways:
  ///
  /// * `rate_limits` controls the total
  /// rate of
  ///   dispatches from a queue (i.e. all traffic dispatched from the
  ///   queue, regardless of whether the dispatch is from a first
  ///   attempt or a retry).
  /// * `retry_config` controls what
  /// happens to
  ///   particular a task after its first attempt fails. That is,
  ///   `retry_config` controls task
  ///   retries (the second attempt, third attempt, etc).
  ///
  /// The queue's actual dispatch rate is the result of:
  ///
  /// * Number of tasks in the queue
  /// * User-specified throttling:
  /// `rate_limits`,
  ///   `retry_config`, and the
  ///   [queue's state][google.cloud.tasks.v2.Queue.state].
  /// * System throttling due to `429` (Too Many Requests) or `503` (Service
  ///   Unavailable) responses from the worker, high error rates, or to smooth
  ///   sudden large traffic spikes.
  final RateLimits? rateLimits;

  /// Settings that determine the retry behavior.
  ///
  /// * For tasks created using Cloud Tasks: the queue-level retry settings
  ///   apply to all tasks in the queue that were created using Cloud Tasks.
  ///   Retry settings cannot be set on individual tasks.
  /// * For tasks created using the App Engine SDK: the queue-level retry
  ///   settings apply to all tasks in the queue which do not have retry settings
  ///   explicitly set on the task and were created by the App Engine SDK. See
  ///   [App Engine
  ///   documentation](https://cloud.google.com/appengine/docs/standard/python/taskqueue/push/retrying-tasks).
  final RetryConfig? retryConfig;

  /// Output only. The state of the queue.
  ///
  /// `state` can only be changed by calling
  /// `PauseQueue`,
  /// `ResumeQueue`, or uploading
  /// [queue.yaml/xml](https://cloud.google.com/appengine/docs/python/config/queueref).
  /// `UpdateQueue` cannot be used
  /// to change `state`.
  final Queue_State state;

  /// Output only. The last time this queue was purged.
  ///
  /// All tasks that were `created`
  /// before this time were purged.
  ///
  /// A queue can be purged using
  /// `PurgeQueue`, the [App Engine
  /// Task Queue SDK, or the Cloud
  /// Console](https://cloud.google.com/appengine/docs/standard/python/taskqueue/push/deleting-tasks-and-queues#purging_all_tasks_from_a_queue).
  ///
  /// Purge time will be truncated to the nearest microsecond. Purge
  /// time will be unset if the queue has never been purged.
  final Timestamp? purgeTime;

  /// Configuration options for writing logs to
  /// [Stackdriver Logging](https://cloud.google.com/logging/docs/). If this
  /// field is unset, then no logs are written.
  final StackdriverLoggingConfig? stackdriverLoggingConfig;

  Queue({
    this.name = '',
    this.appEngineRoutingOverride,
    this.rateLimits,
    this.retryConfig,
    this.state = Queue_State.$default,
    this.purgeTime,
    this.stackdriverLoggingConfig,
  }) : super(fullyQualifiedName);

  factory Queue.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return Queue(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      appEngineRoutingOverride: switch (json['appEngineRoutingOverride']) {
        null => null,
        Object $1 => AppEngineRouting.fromJson($1),
      },
      rateLimits: switch (json['rateLimits']) {
        null => null,
        Object $1 => RateLimits.fromJson($1),
      },
      retryConfig: switch (json['retryConfig']) {
        null => null,
        Object $1 => RetryConfig.fromJson($1),
      },
      state: switch (json['state']) {
        null => Queue_State.$default,
        Object $1 => Queue_State.fromJson($1),
      },
      purgeTime: switch (json['purgeTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      stackdriverLoggingConfig: switch (json['stackdriverLoggingConfig']) {
        null => null,
        Object $1 => StackdriverLoggingConfig.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (name.isNotDefault) 'name': name,
    'appEngineRoutingOverride': ?appEngineRoutingOverride?.toJson(),
    'rateLimits': ?rateLimits?.toJson(),
    'retryConfig': ?retryConfig?.toJson(),
    if (state.isNotDefault) 'state': state.toJson(),
    'purgeTime': ?purgeTime?.toJson(),
    'stackdriverLoggingConfig': ?stackdriverLoggingConfig?.toJson(),
  };

  @override
  String toString() {
    final $contents = ['name=$name', 'state=$state'].join(',');
    return 'Queue(${$contents})';
  }
}

/// State of the queue.
final class Queue_State extends ProtoEnum {
  /// Unspecified state.
  static const stateUnspecified = Queue_State('STATE_UNSPECIFIED');

  /// The queue is running. Tasks can be dispatched.
  ///
  /// If the queue was created using Cloud Tasks and the queue has
  /// had no activity (method calls or task dispatches) for 30 days,
  /// the queue may take a few minutes to re-activate. Some method
  /// calls may return `NOT_FOUND` and
  /// tasks may not be dispatched for a few minutes until the queue
  /// has been re-activated.
  static const running = Queue_State('RUNNING');

  /// Tasks are paused by the user. If the queue is paused then Cloud
  /// Tasks will stop delivering tasks from it, but more tasks can
  /// still be added to it by the user.
  static const paused = Queue_State('PAUSED');

  /// The queue is disabled.
  ///
  /// A queue becomes `DISABLED` when
  /// [queue.yaml](https://cloud.google.com/appengine/docs/python/config/queueref)
  /// or
  /// [queue.xml](https://cloud.google.com/appengine/docs/standard/java/config/queueref)
  /// is uploaded which does not contain the queue. You cannot directly disable
  /// a queue.
  ///
  /// When a queue is disabled, tasks can still be added to a queue
  /// but the tasks are not dispatched.
  ///
  /// To permanently delete this queue and all of its tasks, call
  /// `DeleteQueue`.
  static const disabled = Queue_State('DISABLED');

  /// The default value for [Queue_State].
  static const $default = stateUnspecified;

  const Queue_State(super.value);

  factory Queue_State.fromJson(Object? json) => Queue_State(json as String);

  bool get isNotDefault => this != $default;

  @override
  String toString() => 'State.$value';
}

/// Rate limits.
///
/// This message determines the maximum rate that tasks can be dispatched by a
/// queue, regardless of whether the dispatch is a first task attempt or a retry.
///
/// Note: The debugging command,
/// `RunTask`, will run a task even if
/// the queue has reached its `RateLimits`.
final class RateLimits extends ProtoMessage {
  static const String fullyQualifiedName = 'google.cloud.tasks.v2.RateLimits';

  /// The maximum rate at which tasks are dispatched from this queue.
  ///
  /// If unspecified when the queue is created, Cloud Tasks will pick the
  /// default.
  ///
  /// * The maximum allowed value is 500.
  ///
  ///
  /// This field has the same meaning as
  /// [rate in
  /// queue.yaml/xml](https://cloud.google.com/appengine/docs/standard/python/config/queueref#rate).
  final double maxDispatchesPerSecond;

  /// Output only. The max burst size.
  ///
  /// Max burst size limits how fast tasks in queue are processed when
  /// many tasks are in the queue and the rate is high. This field
  /// allows the queue to have a high rate so processing starts shortly
  /// after a task is enqueued, but still limits resource usage when
  /// many tasks are enqueued in a short period of time.
  ///
  /// The [token bucket](https://wikipedia.org/wiki/Token_Bucket)
  /// algorithm is used to control the rate of task dispatches. Each
  /// queue has a token bucket that holds tokens, up to the maximum
  /// specified by `max_burst_size`. Each time a task is dispatched, a
  /// token is removed from the bucket. Tasks will be dispatched until
  /// the queue's bucket runs out of tokens. The bucket will be
  /// continuously refilled with new tokens based on
  /// `max_dispatches_per_second`.
  ///
  /// Cloud Tasks will pick the value of `max_burst_size` based on the
  /// value of
  /// `max_dispatches_per_second`.
  ///
  /// For queues that were created or updated using
  /// `queue.yaml/xml`, `max_burst_size` is equal to
  /// [bucket_size](https://cloud.google.com/appengine/docs/standard/python/config/queueref#bucket_size).
  /// Since `max_burst_size` is output only, if
  /// `UpdateQueue` is called on a
  /// queue created by `queue.yaml/xml`, `max_burst_size` will be reset based on
  /// the value of
  /// `max_dispatches_per_second`,
  /// regardless of whether
  /// `max_dispatches_per_second`
  /// is updated.
  final int maxBurstSize;

  /// The maximum number of concurrent tasks that Cloud Tasks allows
  /// to be dispatched for this queue. After this threshold has been
  /// reached, Cloud Tasks stops dispatching tasks until the number of
  /// concurrent requests decreases.
  ///
  /// If unspecified when the queue is created, Cloud Tasks will pick the
  /// default.
  ///
  ///
  /// The maximum allowed value is 5,000.
  ///
  ///
  /// This field has the same meaning as
  /// [max_concurrent_requests in
  /// queue.yaml/xml](https://cloud.google.com/appengine/docs/standard/python/config/queueref#max_concurrent_requests).
  final int maxConcurrentDispatches;

  RateLimits({
    this.maxDispatchesPerSecond = 0,
    this.maxBurstSize = 0,
    this.maxConcurrentDispatches = 0,
  }) : super(fullyQualifiedName);

  factory RateLimits.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return RateLimits(
      maxDispatchesPerSecond: switch (json['maxDispatchesPerSecond']) {
        null => 0,
        Object $1 => decodeDouble($1),
      },
      maxBurstSize: switch (json['maxBurstSize']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
      maxConcurrentDispatches: switch (json['maxConcurrentDispatches']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (maxDispatchesPerSecond.isNotDefault)
      'maxDispatchesPerSecond': encodeDouble(maxDispatchesPerSecond),
    if (maxBurstSize.isNotDefault) 'maxBurstSize': maxBurstSize,
    if (maxConcurrentDispatches.isNotDefault)
      'maxConcurrentDispatches': maxConcurrentDispatches,
  };

  @override
  String toString() {
    final $contents = [
      'maxDispatchesPerSecond=$maxDispatchesPerSecond',
      'maxBurstSize=$maxBurstSize',
      'maxConcurrentDispatches=$maxConcurrentDispatches',
    ].join(',');
    return 'RateLimits(${$contents})';
  }
}

/// Retry config.
///
/// These settings determine when a failed task attempt is retried.
final class RetryConfig extends ProtoMessage {
  static const String fullyQualifiedName = 'google.cloud.tasks.v2.RetryConfig';

  /// Number of attempts per task.
  ///
  /// Cloud Tasks will attempt the task `max_attempts` times (that is, if the
  /// first attempt fails, then there will be `max_attempts - 1` retries). Must
  /// be >= -1.
  ///
  /// If unspecified when the queue is created, Cloud Tasks will pick the
  /// default.
  ///
  /// -1 indicates unlimited attempts.
  ///
  /// This field has the same meaning as
  /// [task_retry_limit in
  /// queue.yaml/xml](https://cloud.google.com/appengine/docs/standard/python/config/queueref#retry_parameters).
  final int maxAttempts;

  /// If positive, `max_retry_duration` specifies the time limit for
  /// retrying a failed task, measured from when the task was first
  /// attempted. Once `max_retry_duration` time has passed *and* the
  /// task has been attempted
  /// `max_attempts` times, no
  /// further attempts will be made and the task will be deleted.
  ///
  /// If zero, then the task age is unlimited.
  ///
  /// If unspecified when the queue is created, Cloud Tasks will pick the
  /// default.
  ///
  ///
  /// `max_retry_duration` will be truncated to the nearest second.
  ///
  /// This field has the same meaning as
  /// [task_age_limit in
  /// queue.yaml/xml](https://cloud.google.com/appengine/docs/standard/python/config/queueref#retry_parameters).
  final Duration? maxRetryDuration;

  /// A task will be `scheduled` for
  /// retry between `min_backoff`
  /// and `max_backoff` duration
  /// after it fails, if the queue's
  /// `RetryConfig` specifies that the task
  /// should be retried.
  ///
  /// If unspecified when the queue is created, Cloud Tasks will pick the
  /// default.
  ///
  ///
  /// `min_backoff` will be truncated to the nearest second.
  ///
  /// This field has the same meaning as
  /// [min_backoff_seconds in
  /// queue.yaml/xml](https://cloud.google.com/appengine/docs/standard/python/config/queueref#retry_parameters).
  final Duration? minBackoff;

  /// A task will be `scheduled` for
  /// retry between `min_backoff`
  /// and `max_backoff` duration
  /// after it fails, if the queue's
  /// `RetryConfig` specifies that the task
  /// should be retried.
  ///
  /// If unspecified when the queue is created, Cloud Tasks will pick the
  /// default.
  ///
  ///
  /// `max_backoff` will be truncated to the nearest second.
  ///
  /// This field has the same meaning as
  /// [max_backoff_seconds in
  /// queue.yaml/xml](https://cloud.google.com/appengine/docs/standard/python/config/queueref#retry_parameters).
  final Duration? maxBackoff;

  /// The time between retries will double `max_doublings` times.
  ///
  /// A task's retry interval starts at
  /// `min_backoff`, then doubles
  /// `max_doublings` times, then increases linearly, and finally
  /// retries at intervals of
  /// `max_backoff` up to
  /// `max_attempts` times.
  ///
  /// For example, if
  /// `min_backoff` is 10s,
  /// `max_backoff` is 300s, and
  /// `max_doublings` is 3, then the a task will first be retried in
  /// 10s. The retry interval will double three times, and then
  /// increase linearly by 2^3 * 10s.  Finally, the task will retry at
  /// intervals of `max_backoff`
  /// until the task has been attempted
  /// `max_attempts` times. Thus,
  /// the requests will retry at 10s, 20s, 40s, 80s, 160s, 240s, 300s, 300s, ....
  ///
  /// If unspecified when the queue is created, Cloud Tasks will pick the
  /// default.
  ///
  ///
  /// This field has the same meaning as
  /// [max_doublings in
  /// queue.yaml/xml](https://cloud.google.com/appengine/docs/standard/python/config/queueref#retry_parameters).
  final int maxDoublings;

  RetryConfig({
    this.maxAttempts = 0,
    this.maxRetryDuration,
    this.minBackoff,
    this.maxBackoff,
    this.maxDoublings = 0,
  }) : super(fullyQualifiedName);

  factory RetryConfig.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return RetryConfig(
      maxAttempts: switch (json['maxAttempts']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
      maxRetryDuration: switch (json['maxRetryDuration']) {
        null => null,
        Object $1 => Duration.fromJson($1),
      },
      minBackoff: switch (json['minBackoff']) {
        null => null,
        Object $1 => Duration.fromJson($1),
      },
      maxBackoff: switch (json['maxBackoff']) {
        null => null,
        Object $1 => Duration.fromJson($1),
      },
      maxDoublings: switch (json['maxDoublings']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (maxAttempts.isNotDefault) 'maxAttempts': maxAttempts,
    'maxRetryDuration': ?maxRetryDuration?.toJson(),
    'minBackoff': ?minBackoff?.toJson(),
    'maxBackoff': ?maxBackoff?.toJson(),
    if (maxDoublings.isNotDefault) 'maxDoublings': maxDoublings,
  };

  @override
  String toString() {
    final $contents = [
      'maxAttempts=$maxAttempts',
      'maxDoublings=$maxDoublings',
    ].join(',');
    return 'RetryConfig(${$contents})';
  }
}

/// Configuration options for writing logs to
/// [Stackdriver Logging](https://cloud.google.com/logging/docs/).
final class StackdriverLoggingConfig extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.StackdriverLoggingConfig';

  /// Specifies the fraction of operations to write to
  /// [Stackdriver Logging](https://cloud.google.com/logging/docs/).
  /// This field may contain any value between 0.0 and 1.0, inclusive.
  /// 0.0 is the default and means that no operations are logged.
  final double samplingRatio;

  StackdriverLoggingConfig({this.samplingRatio = 0})
    : super(fullyQualifiedName);

  factory StackdriverLoggingConfig.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return StackdriverLoggingConfig(
      samplingRatio: switch (json['samplingRatio']) {
        null => 0,
        Object $1 => decodeDouble($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (samplingRatio.isNotDefault)
      'samplingRatio': encodeDouble(samplingRatio),
  };

  @override
  String toString() {
    final $contents = ['samplingRatio=$samplingRatio'].join(',');
    return 'StackdriverLoggingConfig(${$contents})';
  }
}

/// HTTP request.
///
/// The task will be pushed to the worker as an HTTP request. If the worker
/// or the redirected worker acknowledges the task by returning a successful HTTP
/// response code ([`200` - `299`]), the task will be removed from the queue. If
/// any other HTTP response code is returned or no response is received, the
/// task will be retried according to the following:
///
/// * User-specified throttling: [retry
/// configuration][google.cloud.tasks.v2.Queue.retry_config],
///   [rate limits][google.cloud.tasks.v2.Queue.rate_limits], and the [queue's
///   state][google.cloud.tasks.v2.Queue.state].
///
/// * System throttling: To prevent the worker from overloading, Cloud Tasks may
///   temporarily reduce the queue's effective rate. User-specified settings
///   will not be changed.
///
///  System throttling happens because:
///
///   * Cloud Tasks backs off on all errors. Normally the backoff specified in
///     [rate limits][google.cloud.tasks.v2.Queue.rate_limits] will be used. But
///     if the worker returns `429` (Too Many Requests), `503` (Service
///     Unavailable), or the rate of errors is high, Cloud Tasks will use a
///     higher backoff rate. The retry specified in the `Retry-After` HTTP
///     response header is considered.
///
///   * To prevent traffic spikes and to smooth sudden increases in traffic,
///     dispatches ramp up slowly when the queue is newly created or idle and
///     if large numbers of tasks suddenly become available to dispatch (due to
///     spikes in create task rates, the queue being unpaused, or many tasks
///     that are scheduled at the same time).
final class HttpRequest extends ProtoMessage {
  static const String fullyQualifiedName = 'google.cloud.tasks.v2.HttpRequest';

  /// Required. The full url path that the request will be sent to.
  ///
  /// This string must begin with either "http://" or "https://". Some examples
  /// are: `http://acme.com` and `https://acme.com/sales:8080`. Cloud Tasks will
  /// encode some characters for safety and compatibility. The maximum allowed
  /// URL length is 2083 characters after encoding.
  ///
  /// The `Location` header response from a redirect response [`300` - `399`]
  /// may be followed. The redirect is not counted as a separate attempt.
  final String url;

  /// The HTTP method to use for the request. The default is POST.
  final HttpMethod httpMethod;

  /// HTTP request headers.
  ///
  /// This map contains the header field names and values.
  /// Headers can be set when the
  /// [task is created][google.cloud.tasks.v2beta3.CloudTasks.CreateTask].
  ///
  /// These headers represent a subset of the headers that will accompany the
  /// task's HTTP request. Some HTTP request headers will be ignored or replaced.
  ///
  /// A partial list of headers that will be ignored or replaced is:
  ///
  /// * Host: This will be computed by Cloud Tasks and derived from
  ///   `HttpRequest.url`.
  /// * Content-Length: This will be computed by Cloud Tasks.
  /// * User-Agent: This will be set to `"Google-Cloud-Tasks"`.
  /// * `X-Google-*`: Google use only.
  /// * `X-AppEngine-*`: Google use only.
  ///
  /// `Content-Type` won't be set by Cloud Tasks. You can explicitly set
  /// `Content-Type` to a media type when the
  ///  [task is created][google.cloud.tasks.v2beta3.CloudTasks.CreateTask].
  ///  For example, `Content-Type` can be set to `"application/octet-stream"` or
  ///  `"application/json"`.
  ///
  /// Headers which can have multiple values (according to RFC2616) can be
  /// specified using comma-separated values.
  ///
  /// The size of the headers must be less than 80KB.
  final Map<String, String> headers;

  /// HTTP request body.
  ///
  /// A request body is allowed only if the
  /// [HTTP method][google.cloud.tasks.v2.HttpRequest.http_method] is POST, PUT,
  /// or PATCH. It is an error to set body on a task with an incompatible
  /// `HttpMethod`.
  final Uint8List body;

  /// If specified, an
  /// [OAuth token](https://developers.google.com/identity/protocols/OAuth2)
  /// will be generated and attached as an `Authorization` header in the HTTP
  /// request.
  ///
  /// This type of authorization should generally only be used when calling
  /// Google APIs hosted on *.googleapis.com.
  final OauthToken? oauthToken;

  /// If specified, an
  /// [OIDC](https://developers.google.com/identity/protocols/OpenIDConnect)
  /// token will be generated and attached as an `Authorization` header in the
  /// HTTP request.
  ///
  /// This type of authorization can be used for many scenarios, including
  /// calling Cloud Run, or endpoints where you intend to validate the token
  /// yourself.
  final OidcToken? oidcToken;

  HttpRequest({
    required this.url,
    this.httpMethod = HttpMethod.$default,
    this.headers = const {},
    Uint8List? body,
    this.oauthToken,
    this.oidcToken,
  }) : body = body ?? Uint8List(0),
       super(fullyQualifiedName);

  factory HttpRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return HttpRequest(
      url: switch (json['url']) {
        null => '',
        Object $1 => decodeString($1),
      },
      httpMethod: switch (json['httpMethod']) {
        null => HttpMethod.$default,
        Object $1 => HttpMethod.fromJson($1),
      },
      headers: switch (json['headers']) {
        null => {},
        Map<String, Object?> $1 => {
          for (final e in $1.entries)
            decodeString(e.key): decodeString(e.value),
        },
        _ => throw const FormatException('"headers" is not an object'),
      },
      body: switch (json['body']) {
        null => Uint8List(0),
        Object $1 => decodeBytes($1),
      },
      oauthToken: switch (json['oauthToken']) {
        null => null,
        Object $1 => OauthToken.fromJson($1),
      },
      oidcToken: switch (json['oidcToken']) {
        null => null,
        Object $1 => OidcToken.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'url': url,
    if (httpMethod.isNotDefault) 'httpMethod': httpMethod.toJson(),
    if (headers.isNotDefault) 'headers': headers,
    if (body.isNotDefault) 'body': encodeBytes(body),
    'oauthToken': ?oauthToken?.toJson(),
    'oidcToken': ?oidcToken?.toJson(),
  };

  @override
  String toString() {
    final $contents = [
      'url=$url',
      'httpMethod=$httpMethod',
      'body=$body',
    ].join(',');
    return 'HttpRequest(${$contents})';
  }
}

/// App Engine HTTP request.
///
/// The message defines the HTTP request that is sent to an App Engine app when
/// the task is dispatched.
///
/// Using `AppEngineHttpRequest`
/// requires
/// [`appengine.applications.get`](https://cloud.google.com/appengine/docs/admin-api/access-control)
/// Google IAM permission for the project
/// and the following scope:
///
/// `https://www.googleapis.com/auth/cloud-platform`
///
/// The task will be delivered to the App Engine app which belongs to the same
/// project as the queue. For more information, see
/// [How Requests are
/// Routed](https://cloud.google.com/appengine/docs/standard/python/how-requests-are-routed)
/// and how routing is affected by
/// [dispatch
/// files](https://cloud.google.com/appengine/docs/python/config/dispatchref).
/// Traffic is encrypted during transport and never leaves Google datacenters.
/// Because this traffic is carried over a communication mechanism internal to
/// Google, you cannot explicitly set the protocol (for example, HTTP or HTTPS).
/// The request to the handler, however, will appear to have used the HTTP
/// protocol.
///
/// The `AppEngineRouting` used to
/// construct the URL that the task is delivered to can be set at the queue-level
/// or task-level:
///
/// * If [app_engine_routing_override is set on the
///   queue][google.cloud.tasks.v2.Queue.app_engine_routing_override], this value
///   is used for all tasks in the queue, no matter what the setting is for the
///   [task-level
///   app_engine_routing][google.cloud.tasks.v2.AppEngineHttpRequest.app_engine_routing].
///
///
/// The `url` that the task will be sent to is:
///
/// * `url =` `host` `+`
///   `relative_uri`
///
/// Tasks can be dispatched to secure app handlers, unsecure app handlers, and
/// URIs restricted with
/// [`login:
/// admin`](https://cloud.google.com/appengine/docs/standard/python/config/appref).
/// Because tasks are not run as any user, they cannot be dispatched to URIs
/// restricted with
/// [`login:
/// required`](https://cloud.google.com/appengine/docs/standard/python/config/appref)
/// Task dispatches also do not follow redirects.
///
/// The task attempt has succeeded if the app's request handler returns an HTTP
/// response code in the range [`200` - `299`]. The task attempt has failed if
/// the app's handler returns a non-2xx response code or Cloud Tasks does
/// not receive response before the
/// `deadline`. Failed tasks will
/// be retried according to the [retry
/// configuration][google.cloud.tasks.v2.Queue.retry_config]. `503` (Service
/// Unavailable) is considered an App Engine system error instead of an
/// application error and will cause Cloud Tasks' traffic congestion control to
/// temporarily throttle the queue's dispatches. Unlike other types of task
/// targets, a `429` (Too Many Requests) response from an app handler does not
/// cause traffic congestion control to throttle the queue.
final class AppEngineHttpRequest extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.AppEngineHttpRequest';

  /// The HTTP method to use for the request. The default is POST.
  ///
  /// The app's request handler for the task's target URL must be able to handle
  /// HTTP requests with this http_method, otherwise the task attempt fails with
  /// error code 405 (Method Not Allowed). See [Writing a push task request
  /// handler](https://cloud.google.com/appengine/docs/java/taskqueue/push/creating-handlers#writing_a_push_task_request_handler)
  /// and the App Engine documentation for your runtime on [How Requests are
  /// Handled](https://cloud.google.com/appengine/docs/standard/python3/how-requests-are-handled).
  final HttpMethod httpMethod;

  /// Task-level setting for App Engine routing.
  ///
  /// * If [app_engine_routing_override is set on the
  ///   queue][google.cloud.tasks.v2.Queue.app_engine_routing_override], this
  ///   value is used for all tasks in the queue, no matter what the setting is
  ///   for the [task-level
  ///   app_engine_routing][google.cloud.tasks.v2.AppEngineHttpRequest.app_engine_routing].
  final AppEngineRouting? appEngineRouting;

  /// The relative URI.
  ///
  /// The relative URI must begin with "/" and must be a valid HTTP relative URI.
  /// It can contain a path and query string arguments.
  /// If the relative URI is empty, then the root path "/" will be used.
  /// No spaces are allowed, and the maximum length allowed is 2083 characters.
  final String relativeUri;

  /// HTTP request headers.
  ///
  /// This map contains the header field names and values.
  /// Headers can be set when the
  /// [task is created][google.cloud.tasks.v2.CloudTasks.CreateTask].
  /// Repeated headers are not supported but a header value can contain commas.
  ///
  /// Cloud Tasks sets some headers to default values:
  ///
  /// * `User-Agent`: By default, this header is
  ///   `"AppEngine-Google; (+http://code.google.com/appengine)"`.
  ///   This header can be modified, but Cloud Tasks will append
  ///   `"AppEngine-Google; (+http://code.google.com/appengine)"` to the
  ///   modified `User-Agent`.
  ///
  /// If the task has a `body`,
  /// Cloud Tasks sets the following headers:
  ///
  /// * `Content-Type`: By default, the `Content-Type` header is set to
  ///   `"application/octet-stream"`. The default can be overridden by explicitly
  ///   setting `Content-Type` to a particular media type when the
  ///   [task is created][google.cloud.tasks.v2.CloudTasks.CreateTask].
  ///   For example, `Content-Type` can be set to `"application/json"`.
  /// * `Content-Length`: This is computed by Cloud Tasks. This value is
  ///   output only.   It cannot be changed.
  ///
  /// The headers below cannot be set or overridden:
  ///
  /// * `Host`
  /// * `X-Google-*`
  /// * `X-AppEngine-*`
  ///
  /// In addition, Cloud Tasks sets some headers when the task is dispatched,
  /// such as headers containing information about the task; see
  /// [request
  /// headers](https://cloud.google.com/tasks/docs/creating-appengine-handlers#reading_request_headers).
  /// These headers are set only when the task is dispatched, so they are not
  /// visible when the task is returned in a Cloud Tasks response.
  ///
  /// Although there is no specific limit for the maximum number of headers or
  /// the size, there is a limit on the maximum size of the
  /// `Task`. For more information, see the
  /// `CreateTask` documentation.
  final Map<String, String> headers;

  /// HTTP request body.
  ///
  /// A request body is allowed only if the HTTP method is POST or PUT. It is
  /// an error to set a body on a task with an incompatible
  /// `HttpMethod`.
  final Uint8List body;

  AppEngineHttpRequest({
    this.httpMethod = HttpMethod.$default,
    this.appEngineRouting,
    this.relativeUri = '',
    this.headers = const {},
    Uint8List? body,
  }) : body = body ?? Uint8List(0),
       super(fullyQualifiedName);

  factory AppEngineHttpRequest.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return AppEngineHttpRequest(
      httpMethod: switch (json['httpMethod']) {
        null => HttpMethod.$default,
        Object $1 => HttpMethod.fromJson($1),
      },
      appEngineRouting: switch (json['appEngineRouting']) {
        null => null,
        Object $1 => AppEngineRouting.fromJson($1),
      },
      relativeUri: switch (json['relativeUri']) {
        null => '',
        Object $1 => decodeString($1),
      },
      headers: switch (json['headers']) {
        null => {},
        Map<String, Object?> $1 => {
          for (final e in $1.entries)
            decodeString(e.key): decodeString(e.value),
        },
        _ => throw const FormatException('"headers" is not an object'),
      },
      body: switch (json['body']) {
        null => Uint8List(0),
        Object $1 => decodeBytes($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (httpMethod.isNotDefault) 'httpMethod': httpMethod.toJson(),
    'appEngineRouting': ?appEngineRouting?.toJson(),
    if (relativeUri.isNotDefault) 'relativeUri': relativeUri,
    if (headers.isNotDefault) 'headers': headers,
    if (body.isNotDefault) 'body': encodeBytes(body),
  };

  @override
  String toString() {
    final $contents = [
      'httpMethod=$httpMethod',
      'relativeUri=$relativeUri',
      'body=$body',
    ].join(',');
    return 'AppEngineHttpRequest(${$contents})';
  }
}

/// App Engine Routing.
///
/// Defines routing characteristics specific to App Engine - service, version,
/// and instance.
///
/// For more information about services, versions, and instances see
/// [An Overview of App
/// Engine](https://cloud.google.com/appengine/docs/python/an-overview-of-app-engine),
/// [Microservices Architecture on Google App
/// Engine](https://cloud.google.com/appengine/docs/python/microservices-on-app-engine),
/// [App Engine Standard request
/// routing](https://cloud.google.com/appengine/docs/standard/python/how-requests-are-routed),
/// and [App Engine Flex request
/// routing](https://cloud.google.com/appengine/docs/flexible/python/how-requests-are-routed).
///
/// Using `AppEngineRouting` requires
/// [`appengine.applications.get`](https://cloud.google.com/appengine/docs/admin-api/access-control)
/// Google IAM permission for the project
/// and the following scope:
///
/// `https://www.googleapis.com/auth/cloud-platform`
final class AppEngineRouting extends ProtoMessage {
  static const String fullyQualifiedName =
      'google.cloud.tasks.v2.AppEngineRouting';

  /// App service.
  ///
  /// By default, the task is sent to the service which is the default
  /// service when the task is attempted.
  ///
  /// For some queues or tasks which were created using the App Engine
  /// Task Queue API, `host` is not
  /// parsable into `service`,
  /// `version`, and
  /// `instance`. For example,
  /// some tasks which were created using the App Engine SDK use a custom domain
  /// name; custom domains are not parsed by Cloud Tasks. If
  /// `host` is not parsable, then
  /// `service`,
  /// `version`, and
  /// `instance` are the empty
  /// string.
  final String service;

  /// App version.
  ///
  /// By default, the task is sent to the version which is the default
  /// version when the task is attempted.
  ///
  /// For some queues or tasks which were created using the App Engine
  /// Task Queue API, `host` is not
  /// parsable into `service`,
  /// `version`, and
  /// `instance`. For example,
  /// some tasks which were created using the App Engine SDK use a custom domain
  /// name; custom domains are not parsed by Cloud Tasks. If
  /// `host` is not parsable, then
  /// `service`,
  /// `version`, and
  /// `instance` are the empty
  /// string.
  final String version;

  /// App instance.
  ///
  /// By default, the task is sent to an instance which is available when
  /// the task is attempted.
  ///
  /// Requests can only be sent to a specific instance if
  /// [manual scaling is used in App Engine
  /// Standard](https://cloud.google.com/appengine/docs/python/an-overview-of-app-engine?hl=en_US#scaling_types_and_instance_classes).
  /// App Engine Flex does not support instances. For more information, see
  /// [App Engine Standard request
  /// routing](https://cloud.google.com/appengine/docs/standard/python/how-requests-are-routed)
  /// and [App Engine Flex request
  /// routing](https://cloud.google.com/appengine/docs/flexible/python/how-requests-are-routed).
  final String instance;

  /// Output only. The host that the task is sent to.
  ///
  /// The host is constructed from the domain name of the app associated with
  /// the queue's project ID (for example <app-id>.appspot.com), and the
  /// `service`,
  /// `version`, and
  /// `instance`. Tasks which
  /// were created using the App Engine SDK might have a custom domain name.
  ///
  /// For more information, see
  /// [How Requests are
  /// Routed](https://cloud.google.com/appengine/docs/standard/python/how-requests-are-routed).
  final String host;

  AppEngineRouting({
    this.service = '',
    this.version = '',
    this.instance = '',
    this.host = '',
  }) : super(fullyQualifiedName);

  factory AppEngineRouting.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return AppEngineRouting(
      service: switch (json['service']) {
        null => '',
        Object $1 => decodeString($1),
      },
      version: switch (json['version']) {
        null => '',
        Object $1 => decodeString($1),
      },
      instance: switch (json['instance']) {
        null => '',
        Object $1 => decodeString($1),
      },
      host: switch (json['host']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (service.isNotDefault) 'service': service,
    if (version.isNotDefault) 'version': version,
    if (instance.isNotDefault) 'instance': instance,
    if (host.isNotDefault) 'host': host,
  };

  @override
  String toString() {
    final $contents = [
      'service=$service',
      'version=$version',
      'instance=$instance',
      'host=$host',
    ].join(',');
    return 'AppEngineRouting(${$contents})';
  }
}

/// Contains information needed for generating an
/// [OAuth token](https://developers.google.com/identity/protocols/OAuth2).
/// This type of authorization should generally only be used when calling Google
/// APIs hosted on *.googleapis.com.
final class OauthToken extends ProtoMessage {
  static const String fullyQualifiedName = 'google.cloud.tasks.v2.OAuthToken';

  /// [Service account email](https://cloud.google.com/iam/docs/service-accounts)
  /// to be used for generating OAuth token.
  /// The service account must be within the same project as the queue. The
  /// caller must have iam.serviceAccounts.actAs permission for the service
  /// account.
  final String serviceAccountEmail;

  /// OAuth scope to be used for generating OAuth access token.
  /// If not specified, "https://www.googleapis.com/auth/cloud-platform"
  /// will be used.
  final String scope;

  OauthToken({this.serviceAccountEmail = '', this.scope = ''})
    : super(fullyQualifiedName);

  factory OauthToken.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return OauthToken(
      serviceAccountEmail: switch (json['serviceAccountEmail']) {
        null => '',
        Object $1 => decodeString($1),
      },
      scope: switch (json['scope']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (serviceAccountEmail.isNotDefault)
      'serviceAccountEmail': serviceAccountEmail,
    if (scope.isNotDefault) 'scope': scope,
  };

  @override
  String toString() {
    final $contents = [
      'serviceAccountEmail=$serviceAccountEmail',
      'scope=$scope',
    ].join(',');
    return 'OAuthToken(${$contents})';
  }
}

/// Contains information needed for generating an
/// [OpenID Connect
/// token](https://developers.google.com/identity/protocols/OpenIDConnect).
/// This type of authorization can be used for many scenarios, including
/// calling Cloud Run, or endpoints where you intend to validate the token
/// yourself.
final class OidcToken extends ProtoMessage {
  static const String fullyQualifiedName = 'google.cloud.tasks.v2.OidcToken';

  /// [Service account email](https://cloud.google.com/iam/docs/service-accounts)
  /// to be used for generating OIDC token.
  /// The service account must be within the same project as the queue. The
  /// caller must have iam.serviceAccounts.actAs permission for the service
  /// account.
  final String serviceAccountEmail;

  /// Audience to be used when generating OIDC token. If not specified, the URI
  /// specified in target will be used.
  final String audience;

  OidcToken({this.serviceAccountEmail = '', this.audience = ''})
    : super(fullyQualifiedName);

  factory OidcToken.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return OidcToken(
      serviceAccountEmail: switch (json['serviceAccountEmail']) {
        null => '',
        Object $1 => decodeString($1),
      },
      audience: switch (json['audience']) {
        null => '',
        Object $1 => decodeString($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (serviceAccountEmail.isNotDefault)
      'serviceAccountEmail': serviceAccountEmail,
    if (audience.isNotDefault) 'audience': audience,
  };

  @override
  String toString() {
    final $contents = [
      'serviceAccountEmail=$serviceAccountEmail',
      'audience=$audience',
    ].join(',');
    return 'OidcToken(${$contents})';
  }
}

/// A unit of scheduled work.
final class Task extends ProtoMessage {
  static const String fullyQualifiedName = 'google.cloud.tasks.v2.Task';

  /// Optionally caller-specified in
  /// `CreateTask`.
  ///
  /// The task name.
  ///
  /// The task name must have the following format:
  /// `projects/PROJECT_ID/locations/LOCATION_ID/queues/QUEUE_ID/tasks/TASK_ID`
  ///
  /// * `PROJECT_ID` can contain letters ([A-Za-z]), numbers ([0-9]),
  ///    hyphens (-), colons (:), or periods (.).
  ///    For more information, see
  ///    [Identifying
  ///    projects](https://cloud.google.com/resource-manager/docs/creating-managing-projects#identifying_projects)
  /// * `LOCATION_ID` is the canonical ID for the task's location.
  ///    The list of available locations can be obtained by calling
  ///    `ListLocations`.
  ///    For more information, see https://cloud.google.com/about/locations/.
  /// * `QUEUE_ID` can contain letters ([A-Za-z]), numbers ([0-9]), or
  ///   hyphens (-). The maximum length is 100 characters.
  /// * `TASK_ID` can contain only letters ([A-Za-z]), numbers ([0-9]),
  ///   hyphens (-), or underscores (_). The maximum length is 500 characters.
  final String name;

  /// HTTP request that is sent to the App Engine app handler.
  ///
  /// An App Engine task is a task that has
  /// `AppEngineHttpRequest` set.
  final AppEngineHttpRequest? appEngineHttpRequest;

  /// HTTP request that is sent to the worker.
  ///
  /// An HTTP task is a task that has
  /// `HttpRequest` set.
  final HttpRequest? httpRequest;

  /// The time when the task is scheduled to be attempted or retried.
  ///
  /// `schedule_time` will be truncated to the nearest microsecond.
  final Timestamp? scheduleTime;

  /// Output only. The time that the task was created.
  ///
  /// `create_time` will be truncated to the nearest second.
  final Timestamp? createTime;

  /// The deadline for requests sent to the worker. If the worker does not
  /// respond by this deadline then the request is cancelled and the attempt
  /// is marked as a `DEADLINE_EXCEEDED` failure. Cloud Tasks will retry the
  /// task according to the `RetryConfig`.
  ///
  /// Note that when the request is cancelled, Cloud Tasks will stop listening
  /// for the response, but whether the worker stops processing depends on the
  /// worker. For example, if the worker is stuck, it may not react to cancelled
  /// requests.
  ///
  /// The default and maximum values depend on the type of request:
  ///
  /// * For [HTTP tasks][google.cloud.tasks.v2.HttpRequest], the default is 10
  /// minutes. The deadline
  ///   must be in the interval [15 seconds, 30 minutes].
  ///
  /// * For [App Engine tasks][google.cloud.tasks.v2.AppEngineHttpRequest], 0
  /// indicates that the
  ///   request has the default deadline. The default deadline depends on the
  ///   [scaling
  ///   type](https://cloud.google.com/appengine/docs/standard/go/how-instances-are-managed#instance_scaling)
  ///   of the service: 10 minutes for standard apps with automatic scaling, 24
  ///   hours for standard apps with manual and basic scaling, and 60 minutes for
  ///   flex apps. If the request deadline is set, it must be in the interval [15
  ///   seconds, 24 hours 15 seconds]. Regardless of the task's
  ///   `dispatch_deadline`, the app handler will not run for longer than than
  ///   the service's timeout. We recommend setting the `dispatch_deadline` to
  ///   at most a few seconds more than the app handler's timeout. For more
  ///   information see
  ///   [Timeouts](https://cloud.google.com/tasks/docs/creating-appengine-handlers#timeouts).
  ///
  /// `dispatch_deadline` will be truncated to the nearest millisecond. The
  /// deadline is an approximate deadline.
  final Duration? dispatchDeadline;

  /// Output only. The number of attempts dispatched.
  ///
  /// This count includes attempts which have been dispatched but haven't
  /// received a response.
  final int dispatchCount;

  /// Output only. The number of attempts which have received a response.
  final int responseCount;

  /// Output only. The status of the task's first attempt.
  ///
  /// Only `dispatch_time` will be
  /// set. The other `Attempt` information is not
  /// retained by Cloud Tasks.
  final Attempt? firstAttempt;

  /// Output only. The status of the task's last attempt.
  final Attempt? lastAttempt;

  /// Output only. The view specifies which subset of the
  /// `Task` has been returned.
  final Task_View view;

  Task({
    this.name = '',
    this.appEngineHttpRequest,
    this.httpRequest,
    this.scheduleTime,
    this.createTime,
    this.dispatchDeadline,
    this.dispatchCount = 0,
    this.responseCount = 0,
    this.firstAttempt,
    this.lastAttempt,
    this.view = Task_View.$default,
  }) : super(fullyQualifiedName);

  factory Task.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return Task(
      name: switch (json['name']) {
        null => '',
        Object $1 => decodeString($1),
      },
      appEngineHttpRequest: switch (json['appEngineHttpRequest']) {
        null => null,
        Object $1 => AppEngineHttpRequest.fromJson($1),
      },
      httpRequest: switch (json['httpRequest']) {
        null => null,
        Object $1 => HttpRequest.fromJson($1),
      },
      scheduleTime: switch (json['scheduleTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      createTime: switch (json['createTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      dispatchDeadline: switch (json['dispatchDeadline']) {
        null => null,
        Object $1 => Duration.fromJson($1),
      },
      dispatchCount: switch (json['dispatchCount']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
      responseCount: switch (json['responseCount']) {
        null => 0,
        Object $1 => decodeInt($1),
      },
      firstAttempt: switch (json['firstAttempt']) {
        null => null,
        Object $1 => Attempt.fromJson($1),
      },
      lastAttempt: switch (json['lastAttempt']) {
        null => null,
        Object $1 => Attempt.fromJson($1),
      },
      view: switch (json['view']) {
        null => Task_View.$default,
        Object $1 => Task_View.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    if (name.isNotDefault) 'name': name,
    'appEngineHttpRequest': ?appEngineHttpRequest?.toJson(),
    'httpRequest': ?httpRequest?.toJson(),
    'scheduleTime': ?scheduleTime?.toJson(),
    'createTime': ?createTime?.toJson(),
    'dispatchDeadline': ?dispatchDeadline?.toJson(),
    if (dispatchCount.isNotDefault) 'dispatchCount': dispatchCount,
    if (responseCount.isNotDefault) 'responseCount': responseCount,
    'firstAttempt': ?firstAttempt?.toJson(),
    'lastAttempt': ?lastAttempt?.toJson(),
    if (view.isNotDefault) 'view': view.toJson(),
  };

  @override
  String toString() {
    final $contents = [
      'name=$name',
      'dispatchCount=$dispatchCount',
      'responseCount=$responseCount',
      'view=$view',
    ].join(',');
    return 'Task(${$contents})';
  }
}

/// The view specifies a subset of `Task` data.
///
/// When a task is returned in a response, not all
/// information is retrieved by default because some data, such as
/// payloads, might be desirable to return only when needed because
/// of its large size or because of the sensitivity of data that it
/// contains.
final class Task_View extends ProtoEnum {
  /// Unspecified. Defaults to BASIC.
  static const viewUnspecified = Task_View('VIEW_UNSPECIFIED');

  /// The basic view omits fields which can be large or can contain
  /// sensitive data.
  ///
  /// This view does not include the
  /// [body in
  /// AppEngineHttpRequest][google.cloud.tasks.v2.AppEngineHttpRequest.body].
  /// Bodies are desirable to return only when needed, because they
  /// can be large and because of the sensitivity of the data that you
  /// choose to store in it.
  static const basic = Task_View('BASIC');

  /// All information is returned.
  ///
  /// Authorization for `FULL` requires
  /// `cloudtasks.tasks.fullView` [Google IAM](https://cloud.google.com/iam/)
  /// permission on the `Queue` resource.
  static const full = Task_View('FULL');

  /// The default value for [Task_View].
  static const $default = viewUnspecified;

  const Task_View(super.value);

  factory Task_View.fromJson(Object? json) => Task_View(json as String);

  bool get isNotDefault => this != $default;

  @override
  String toString() => 'View.$value';
}

/// The status of a task attempt.
final class Attempt extends ProtoMessage {
  static const String fullyQualifiedName = 'google.cloud.tasks.v2.Attempt';

  /// Output only. The time that this attempt was scheduled.
  ///
  /// `schedule_time` will be truncated to the nearest microsecond.
  final Timestamp? scheduleTime;

  /// Output only. The time that this attempt was dispatched.
  ///
  /// `dispatch_time` will be truncated to the nearest microsecond.
  final Timestamp? dispatchTime;

  /// Output only. The time that this attempt response was received.
  ///
  /// `response_time` will be truncated to the nearest microsecond.
  final Timestamp? responseTime;

  /// Output only. The response from the worker for this attempt.
  ///
  /// If `response_time` is unset, then the task has not been attempted or is
  /// currently running and the `response_status` field is meaningless.
  final Status? responseStatus;

  Attempt({
    this.scheduleTime,
    this.dispatchTime,
    this.responseTime,
    this.responseStatus,
  }) : super(fullyQualifiedName);

  factory Attempt.fromJson(Object? j) {
    final json = j as Map<String, Object?>;
    return Attempt(
      scheduleTime: switch (json['scheduleTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      dispatchTime: switch (json['dispatchTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      responseTime: switch (json['responseTime']) {
        null => null,
        Object $1 => Timestamp.fromJson($1),
      },
      responseStatus: switch (json['responseStatus']) {
        null => null,
        Object $1 => Status.fromJson($1),
      },
    );
  }

  @override
  Object toJson() => {
    'scheduleTime': ?scheduleTime?.toJson(),
    'dispatchTime': ?dispatchTime?.toJson(),
    'responseTime': ?responseTime?.toJson(),
    'responseStatus': ?responseStatus?.toJson(),
  };

  @override
  String toString() => 'Attempt()';
}

/// The HTTP method used to deliver the task.
final class HttpMethod extends ProtoEnum {
  /// HTTP method unspecified
  static const httpMethodUnspecified = HttpMethod('HTTP_METHOD_UNSPECIFIED');

  /// HTTP POST
  static const post = HttpMethod('POST');

  /// HTTP GET
  static const get = HttpMethod('GET');

  /// HTTP HEAD
  static const head = HttpMethod('HEAD');

  /// HTTP PUT
  static const put = HttpMethod('PUT');

  /// HTTP DELETE
  static const delete = HttpMethod('DELETE');

  /// HTTP PATCH
  static const patch = HttpMethod('PATCH');

  /// HTTP OPTIONS
  static const options = HttpMethod('OPTIONS');

  /// The default value for [HttpMethod].
  static const $default = httpMethodUnspecified;

  const HttpMethod(super.value);

  factory HttpMethod.fromJson(Object? json) => HttpMethod(json as String);

  bool get isNotDefault => this != $default;

  @override
  String toString() => 'HttpMethod.$value';
}
